# MSG-433: 회원가입 약관 동의 저장과 게이트 판별

**Owner**: B (user 도메인 단독. `com.msg.fillmap.user.*`만 변경하고 auth 패키지는 건드리지 않는다)

> 요구사항 정본: `docs/prd/MSG-433-prd.md` (상태: 검토됨) · 상위 요구사항 ID: SRS FR-USER-15
> 이 스펙은 PRD의 FR 1~10에 "어떻게"만 붙인다. 요구사항 해석이 갈리면 PRD가 이긴다.

## 개요

회원가입 동의 5항목(만 14세 이상 확인, 서비스 이용약관, 개인정보 수집·이용, 위치기반서비스
이용약관, 마케팅 정보 수신)을 `users` 컬럼으로 저장하고(철회 없는 필수 3항목은 동의 시각을
영구 보존, 철회 가능한 위치와 마케팅은 현재 상태와 마지막 변경 시각), 클라이언트 동의
게이트[^1]가 판별에 쓸 전용 동의 상태 조회 API를 신설한다. 위치기반서비스 항목은 기존
`users.location_consent`(MSG-402)와 한 값으로 관리해 이중 저장을 만들지 않는다. 로그인 API와
프로필 응답은 무수정이다 (2026-08-19 정민 확정, MSG-402 스펙 D-2와 같은 방향).

## 배경과 목표

카카오 로그인 동의 화면은 "카카오가 필맵에 정보를 제공해도 되는가"까지만 다루므로, 사업자 약관
동의 5항목은 필맵이 직접 받아 증빙(언제 무엇에 동의했나)을 보관해야 한다. 지금 서버에는 위치
동의 한 칸만 있어 모바일 게이트(MSG-422, 웹 선례 MSG-407)가 전제하는 "로그인 직후 서버가
미동의로 응답하면 동의 화면을 띄우고, 제출하면 저장된다"는 흐름이 성립하지 않는다. 이 티켓으로
저장과 판별 재료를 완성한다. 미동의 사용자의 다른 API를 서버가 차단하지는 않는다 (FR-9, 차단은
클라이언트 게이트 몫이라는 FR-USER-14 원칙 그대로).

## 성공 기준

- 로그인 직후 동의 상태 조회 한 번으로 필수 동의 완료 여부와 항목별 여부를 알 수 있다 (FR-1).
- 동의 제출 한 번으로 5항목이 저장된다. 필수 3항목(만 14세, 서비스약관, 개인정보)은 동의 시각이
  영구 보존되고(철회 API가 없어 덮일 경로 자체가 없다), 철회 가능한 위치와 마케팅은 현재 상태와
  마지막 변경 시각이 남는다 (FR-2, FR-4 정밀화 반영).
- 필수 4항목 중 하나라도 동의가 아닌 제출은 400으로 거절되고 아무 항목도 저장되지 않는다 (FR-3).
- 제출 후 기존 `location_consent`가 켜져 있고, 프로필 편집의 위치 동의 토글과 값이 하나다 (FR-5).
- 마케팅 수신 동의는 동의 상태 조회에 동봉되고, 전용 API로 켜고 끌 수 있으며 실제로 값이 바뀔
  때만 변경 시각이 갱신된다 (FR-6).
- V37 적용만으로 기존 사용자 전원이 필수 동의 미완료로 시작한다. 백필 없음 (FR-7).
- 같은 내용 재제출이 성공한다 (FR-8 멱등[^2]). 철회가 없는 필수 3항목은 최초 동의 시각이
  그대로고, 위치와 마케팅은 같은 값 재제출이면 마지막 변경 시각이 유지된다
  (`IS DISTINCT FROM` 경로).
- 계정 삭제 시 users 행 삭제로 동의 기록도 함께 제거된다 (비기능 "데이터 정합", 컬럼이라 별도
  정리 불요).

## 설계 결정

| ID | 결정 | 근거 |
|----|------|------|
| D-1 | 게이트 판별은 전용 `GET /api/users/me/consents`. 프로필 응답과 로그인 응답에는 동봉하지 않는다 | 2026-08-19 정민 확정 (PRD FR-1). auth 무수정 원칙은 MSG-402 D-2(로그인 응답 미동봉)와 같은 방향이고, 프로필 DTO를 손대지 않아 기존 응답 계약이 전부 불변이다 |
| D-2 | 필수 3항목(만 14세, 서비스약관, 개인정보)은 시각 컬럼 하나가 여부와 시각을 겸한다. NULL이면 미동의, 값이 있으면 그 시각에 동의 | 이 3항목은 철회 API가 없다 (철회 = 탈퇴). 한 번 켜지면 꺼지지 않는 값이라 boolean을 따로 두면 여부와 시각이 갈라질 desync 지점만 생긴다. 컬럼 하나면 그 불일치가 표현 자체가 불가능하고, 동의 시각 영구 보존(FR-4)도 자동 성립한다. FR-10(만 14세는 체크 사실만, 생년월일 미수집)도 시각 하나로 충족된다 |
| D-3 | 위치기반서비스 항목은 신규 컬럼 없이 기존 `location_consent` + `location_consent_changed_at` 쌍을 그대로 쓴다 | FR-5의 단일 값 원칙 (이중 저장 금지). 제출이 이 값을 TRUE로 켜고, 기존 `PUT /api/users/me/location-consent` 토글은 무수정으로 유효하다. 토글로 철회하면 필수 동의 미완료로 되돌아가 다음 로그인 게이트가 다시 뜬다. 이것은 버그가 아니라 한 값 관리의 귀결이며 위치정보법 관점에서도 정합이다 (철회했으면 약관 미동의 상태가 맞다) |
| D-4 | 마케팅은 `marketing_consent BOOLEAN NOT NULL DEFAULT FALSE` + `marketing_consent_changed_at TIMESTAMP NULL` 쌍. 변경 API는 location-consent 미러 | 켜고 끄기를 반복하는 항목이라(FR-6) 시각 단독으로는 "동의했다가 철회한 상태"를 표현할 수 없다. 갱신 쿼리·DTO·서비스 전부 `updateLocationConsent` 선례를 그대로 확장한다. 철회 가능한 두 항목(위치·마케팅)은 철회 뒤 원 동의 시각이 남지 않고 마지막 변경 시각만 남는다. FR-USER-14가 위치 동의에 이미 확정한 보관 수준이고, 정밀화된 FR-4도 이 두 항목의 증빙을 현재 상태와 마지막 변경 시각으로 정의한다 |
| D-5 | 동의 제출은 원자 UPDATE[^3] 한 문장. 필수 3항목은 `COALESCE`[^4]로 최초 동의 시각을 보존하고, 위치와 마케팅은 `IS DISTINCT FROM`[^5] CASE로 실제 변경 시만 시각을 갱신한다 | MSG-402 D-4의 확장. 조회 후 비교 방식은 동시 제출에서 시각 이중 갱신이 가능하지만, 한 문장이면 행 잠금이 직렬화하고 멱등(FR-8)이 문장 수준에서 성립해 서비스에 분기 코드가 없다. 한 문장이므로 "전량 저장 또는 전량 미저장"(FR-3)도 자동 성립 |
| D-6 | 필수 미충족 400은 Bean Validation(`@NotNull` + `@AssertTrue`)으로 컨트롤러 진입 전에 거른다. 신규 에러 코드 없음 | 검증 실패가 서비스에 닿지 않으므로 "아무 항목도 저장되지 않는다"(FR-3)가 구조로 보장된다. 실패 케이스가 전부 기존 코드로 덮인다: 검증 실패는 공통 400, 인증 실패는 auth 2401·2402·2403, 사용자 부재는 1404. UserErrorCode 1xxx 대역에 추가할 상수가 없다 (MSG-402 D-7 동형) |
| D-7 | 동의 시각은 어떤 응답에도 싣지 않는다 | PRD FR-1이 요구하는 응답 항목은 항목별 동의 여부다. 시각은 법적 증빙용 서버 보관 값이다 (MSG-402 D-6 논리 재사용). 필요해지면 요구사항 변경이라 PRD부터 다시 거친다 |
| D-8 | 동의 상태 응답에 `requiredCompleted`(필수 4항목 AND)를 서버가 계산해 동봉한다 | PRD 클래스 다이어그램의 "필수 동의 완료 여부". 웹(MSG-407)과 모바일(MSG-422)이 각자 4항목 AND를 조립하다 항목이 늘 때 어긋나는 것을 막는다. 게이트 판별 규칙의 정본이 서버 한 곳이 된다 |
| D-9 | 제출 메서드는 `PUT /api/users/me/consents` | 재제출 멱등(FR-8)이 요구라 HTTP 의미론상 PUT이 맞다. 게이트 재진입과 네트워크 재시도가 흔한 경로다 |
| D-10 | 시각은 `UserServiceImpl`에 이미 주입된 `Clock`[^6]으로 만든다 (UTC, `truncatedTo(MICROS)`) | 인자 없는 `now()` 금지 컨벤션 (MSG-376). MSG-402가 만든 Clock 주입 구조를 그대로 재사용하므로 생성자 변경이 없다 |

## API 명세

세 엔드포인트 모두 인증 필수이고 항상 본인 계정만 대상이다 (경로에 대상 식별자 없음, 비기능
"보안/인가"). 인증·부재 에러는 공통이다:

| 상황 | HTTP | developCode | 비고 |
|------|------|-------------|------|
| 토큰 없음 | 401 | 2403 | AuthErrorCode UNAUTHENTICATED |
| 무효 토큰 | 401 | 2401 | AuthErrorCode INVALID_TOKEN |
| 만료 토큰 | 401 | 2402 | AuthErrorCode EXPIRED_TOKEN |
| 사용자 부재 | 404 | 1404 | USER_NOT_FOUND (탈퇴 직후 재호출 등) |

### 1. 동의 상태 조회 (신규): `GET /api/users/me/consents`

로그인 직후 게이트 판별용 (FR-1). 마케팅 상태 조회(FR-6)도 이 응답에 동봉된다.

**Response**: `SuccessResponse<ConsentStatusResponseDto>` (신규 DTO)

| 필드 | 타입 | 설명 |
|------|------|------|
| `ageOver14` | `Boolean` (required) | 만 14세 이상 확인 여부. `age_over14_consented_at IS NOT NULL` |
| `serviceTerms` | `Boolean` (required) | 서비스 이용약관 동의 여부. `service_terms_consented_at IS NOT NULL` |
| `privacyPolicy` | `Boolean` (required) | 개인정보 수집·이용 동의 여부. `privacy_consented_at IS NOT NULL` |
| `locationTerms` | `Boolean` (required) | 위치기반서비스 이용약관 동의 여부. 기존 `location_consent` 그대로 (D-3) |
| `marketing` | `Boolean` (required) | 마케팅 정보 수신 동의 여부 (선택 항목) |
| `requiredCompleted` | `Boolean` (required) | 필수 4항목 전부 동의면 true (D-8). false면 FE가 동의 게이트를 띄운다 |

`@Schema`의 `requiredProperties`에 6개 필드를 전부 넣는다. 빠뜨리면
`ResponseSchemaNullabilityTest`가 깨진다 (MSG-402 선례).

성공 예시 (기존 사용자 첫 조회, FR-7):

```json
{
  "developCode": 200,
  "message": "성공",
  "data": {
    "ageOver14": false,
    "serviceTerms": false,
    "privacyPolicy": false,
    "locationTerms": false,
    "marketing": false,
    "requiredCompleted": false
  }
}
```

### 2. 동의 제출 (신규): `PUT /api/users/me/consents`

가입 게이트의 "동의하고 시작하기" 제출 (FR-2). 재제출 멱등 (FR-8, D-9).

**Request**: `ConsentSubmitRequestDto` (신규)

| 필드 | 타입 | 검증 | 설명 |
|------|------|------|------|
| `ageOver14` | `Boolean` | `@NotNull` `@AssertTrue` | 만 14세 이상 자기 확인 (필수) |
| `serviceTerms` | `Boolean` | `@NotNull` `@AssertTrue` | 서비스 이용약관 (필수) |
| `privacyPolicy` | `Boolean` | `@NotNull` `@AssertTrue` | 개인정보 수집·이용 (필수) |
| `locationTerms` | `Boolean` | `@NotNull` `@AssertTrue` | 위치기반서비스 이용약관 (필수) |
| `marketing` | `Boolean` | `@NotNull` | 마케팅 정보 수신. true·false 모두 유효 (선택 항목) |

`@AssertTrue`는 null을 통과시키므로 `@NotNull`을 반드시 병기한다. 필수 필드가 false거나
누락이면 `@Valid` 실패로 400이고 서비스에 닿지 않는다 (FR-3, D-6). 원시 boolean이 아니라
Boolean인 이유는 누락이 false로 둔갑하지 않게 하기 위해서다 (`LocationConsentUpdateRequestDto`
주석 동일).

**Response**: `SuccessResponse<ConsentStatusResponseDto>` (제출 후 상태. FE가 재조회 없이 게이트
통과를 확정한다. `requiredCompleted`는 이 경로에서 항상 true)

**에러 케이스** (전부 기존 코드, D-6): 필수 항목 false 또는 누락은 400, 나머지는 공통 표.

### 3. 마케팅 수신 동의 변경 (신규): `PUT /api/users/me/marketing-consent`

가입 후 설정 화면의 켜고 끄기 (FR-6). `PUT /api/users/me/location-consent` 미러다 (D-4).

**Request**: `MarketingConsentUpdateRequestDto` (신규)

| 필드 | 타입 | 검증 | 설명 |
|------|------|------|------|
| `consented` | `Boolean` | `@NotNull` | true면 동의, false면 철회 |

**Response**: `SuccessResponse<ConsentStatusResponseDto>` (변경 후 상태)

같은 값 재저장은 성공하되 변경 시각이 그대로다 (멱등, location-consent와 동일 규칙).

### 기존 API와의 관계 (FR-5)

- `PUT /api/users/me/location-consent` (MSG-402): 무수정. 제출과 이 토글이 같은
  `location_consent` 한 값을 읽고 쓴다. 제출이 켜고, 토글이 끄면 동의 상태 조회의
  `locationTerms`와 `requiredCompleted`가 즉시 false가 된다 (D-3). FE 공유 필요 사항이다.
- `GET /api/users/me` 프로필과 로그인 API: 무수정 (D-1).
- 만 14세·서비스약관·개인정보 3항목의 철회 API는 없다. 이 동의들의 철회는 계정 삭제
  (`DELETE /api/users/me`)다 (D-2).

## 도메인 로직

`UserServiceImpl`에 메서드 3개 추가. 갱신 2개는 메서드 단위 `@Transactional` 필수다
(`@Modifying` 쿼리는 트랜잭션 없이 실행이 거부된다. 기존 관례 동일).

### 동의 상태 조회: `getConsentStatus(Long userId)`

`findUser(userId)`로 조회해 (부재 시 1404) 엔티티 필드를 `ConsentStatusResponseDto.from(user)`로
변환한다. 필수 3항목은 시각의 NULL 여부, 위치와 마케팅은 boolean, `requiredCompleted`는 4항목
AND를 DTO 팩토리에서 계산한다 (D-8).

### 동의 제출: `submitConsents(Long userId, ConsentSubmitRequestDto request)`

1. 주입된 `Clock`으로 `now`를 만든다 (UTC, MICROS 절단, D-10).
2. `userRepository.submitConsents(userId, request.marketing(), now)` 원자 UPDATE 한 문장 (D-5).
   영향 행 0이면 1404.
3. 재조회해 `ConsentStatusResponseDto.from(user)` 반환. `@Modifying(clearAutomatically = true)`로
   갱신 전 스냅숏을 비우고 읽는다 (MSG-402 선례).

리포지토리 쿼리 (native). 필수 4항목은 검증을 통과했으면 전부 동의 확정이라 파라미터는
`marketing` 값과 시각뿐이다:

```sql
UPDATE users
SET age_over14_consented_at = COALESCE(age_over14_consented_at, :now),
    service_terms_consented_at = COALESCE(service_terms_consented_at, :now),
    privacy_consented_at = COALESCE(privacy_consented_at, :now),
    location_consent = TRUE,
    location_consent_changed_at = CASE
        WHEN location_consent IS DISTINCT FROM TRUE THEN :now
        ELSE location_consent_changed_at
    END,
    marketing_consent = :marketing,
    marketing_consent_changed_at = CASE
        WHEN marketing_consent IS DISTINCT FROM :marketing THEN :now
        ELSE marketing_consent_changed_at
    END
WHERE id = :userId
```

- `COALESCE`가 필수 3항목의 최초 동의 시각을 영구 보존하므로 재제출이 증빙 시각을 덮지 않는다
  (FR-4, FR-8).
- 위치는 MSG-402 온보딩으로 이미 켠 사용자라면 CASE가 기존 변경 시각을 유지한다 (FR-5 정합).
- 마케팅은 재제출에서 값이 달라지면 시각이 갱신되고, 같으면 그대로다.
- 철회 가능한 위치와 마케팅은 철회 뒤 원 동의 시각이 남지 않고 마지막 변경 시각만 보관된다.
  이는 FR-USER-14가 위치 동의에 이미 확정한 보관 수준이며, 정밀화된 FR-4가 이 두 항목의 증빙을
  현재 상태와 마지막 변경 시각으로 정의한다 (D-4).
- 시각 샘플이 잠금 선행이라 동시 요청에서 저장 시각이 실제 순서와 어긋날 수 있는 수용 경합은
  MSG-402 작업 로그의 판정(동의 여부 자체는 항상 마지막 값으로 정확)을 그대로 승계한다.

### 마케팅 변경: `updateMarketingConsent(Long userId, boolean consented)`

`updateLocationConsent`와 완전 동형이다. 원자 UPDATE(`IS DISTINCT FROM` CASE) 한 문장, 영향 행
0이면 1404, 재조회 후 `ConsentStatusResponseDto` 반환.

미동의 상태를 읽고 분기하는 서비스는 어디에도 없다 (FR-9). glossary의 방문·점령 흐름에 변화가
없다.

## 데이터 모델

Flyway 마이그레이션 1건 신규:

```sql
-- V37__users_terms_consent.sql
ALTER TABLE users
	ADD COLUMN age_over14_consented_at TIMESTAMP,
	ADD COLUMN service_terms_consented_at TIMESTAMP,
	ADD COLUMN privacy_consented_at TIMESTAMP,
	ADD COLUMN marketing_consent BOOLEAN NOT NULL DEFAULT FALSE,
	ADD COLUMN marketing_consent_changed_at TIMESTAMP;
```

- **번호 주의**: 로컬 최신은 V36(2026-08-19 확인)이라 V37을 제안하지만, 착수 시점에
  `origin/develop`을 fetch해 번호 선점을 반드시 재확인한다 (MSG-356 V28 충돌 교훈).
- 기존 행은 시각 3종 NULL + 마케팅 DEFAULT FALSE로, 마이그레이션만으로 필수 동의 미완료가 된다.
  백필 없음 (FR-7, 비기능 "운영").
- 시각 컬럼의 저장 축은 UTC다 (naive `LocalDateTime` = UTC 규약, MSG-376).
- `User` 엔티티에 필드 5개를 매핑한다: `ageOver14ConsentedAt`, `serviceTermsConsentedAt`,
  `privacyConsentedAt` (`LocalDateTime`), `marketingConsent` (boolean),
  `marketingConsentChangedAt` (`LocalDateTime`). 상태 전이 메서드는 두지 않는다. 갱신 경로가
  리포지토리 원자 UPDATE 두 곳뿐이다 (locationConsent 필드 주석과 같은 이유).
- 신규 가입 생성자는 손대지 않는다. DB DEFAULT와 자바 기본값(null·false)이 미동의 시작을
  보장한다.
- 계정 삭제는 users 행 삭제라 동의 기록이 자동으로 함께 지워진다 (FR-USER-07 연쇄, 비기능
  "데이터 정합").

## 계약 변경

도메인 간 계약 인터페이스 변경은 없다. `GridQueryService`, `HotZoneService`,
`UserGridQueryService`, `UserOidcCommandService`의 시그니처가 바뀌지 않고, 변경이 user 패키지
내부에 닫힌다.

외부 API 계약은 바뀐다: 엔드포인트 3개 신설 (동의 상태 조회, 동의 제출, 마케팅 변경)과 위치
토글 철회가 게이트를 되살린다는 동작 (D-3). MSG-422(모바일)와 MSG-407(웹) 담당 FE에 공유가
필요하다.

## 변경 파일 목록

| 파일 | 변경 |
|------|------|
| `src/main/resources/db/migration/V37__users_terms_consent.sql` | 신규 (번호 재확인, 위 주의) |
| `user/entity/User.java` | 필드 5개 매핑 (상태 전이 메서드 없음) |
| `user/repository/UserRepository.java` | `submitConsents` · `updateMarketingConsent` `@Modifying` native 쿼리 2건 신규 |
| `user/dto/ConsentStatusResponseDto.java` | 신규 (D-8 `requiredCompleted` 계산 포함) |
| `user/dto/ConsentSubmitRequestDto.java` | 신규 (`@AssertTrue` 필수 4항목) |
| `user/dto/MarketingConsentUpdateRequestDto.java` | 신규 (`LocationConsentUpdateRequestDto` 미러) |
| `user/controller/UserController.java` | `GET /me/consents` · `PUT /me/consents` · `PUT /me/marketing-consent` 추가 |
| `user/service/UserService.java` + `UserServiceImpl.java` | 메서드 3개 추가 (Clock은 기존 주입 재사용, D-10) |

auth 패키지·프로필 DTO·`location-consent` 엔드포인트는 무변경이다.

## 테스트 시나리오

한국어 테스트 메서드명 후보:

- `기존_사용자는_필수_동의_미완료_상태로_조회된다` (FR-7, V37 직후 상태)
- `동의_제출_후_상태_조회에_5항목과_필수_완료가_반영된다` (FR-1, FR-2)
- `동의_제출_시_필수_3항목의_동의_시각이_UTC로_저장된다` (FR-4, 고정 Clock 값 검증)
- `동의_제출이_위치_동의를_켜고_변경_시각을_남긴다` (FR-5)
- `이미_위치_동의가_켜진_사용자의_제출은_위치_변경_시각을_덮지_않는다` (FR-5, MSG-402 경로 선행 사용자)
- `필수_항목_하나라도_false면_400이고_아무_항목도_저장되지_않는다` (FR-3, 항목별 파라미터라이즈)
- `필수_항목_누락_요청은_400이다` (null, `@NotNull`)
- `marketing_누락_요청은_400이다`
- `마케팅_false_제출은_성공하고_마케팅만_미동의로_남는다` (선택 항목)
- `같은_내용_재제출은_성공하고_필수_3항목의_최초_동의_시각이_그대로다` (FR-8, COALESCE 검증.
  같은 검증에서 위치와 마케팅은 동일 값 재제출이므로 마지막 변경 시각이 유지됨을 함께 단언한다,
  `IS DISTINCT FROM` 경로)
- `재제출에서_마케팅_값이_바뀌면_마케팅_변경_시각만_갱신된다`
- `마케팅_변경_API로_켜고_끄면_상태와_변경_시각이_반영된다` (FR-6)
- `마케팅_같은_값_재저장은_성공하고_변경_시각이_그대로다` (멱등)
- `위치_토글로_철회하면_필수_완료가_false로_돌아간다` (D-3, 기존 location-consent API와의 접점)
- `위치만_동의했던_기존_사용자는_철회와_재제출까지_게이트_생명주기가_이어진다` (FR-7과 D-3의
  연결을 직접 확인하는 통합 시나리오. `location_consent`가 true인데 신규 필수 시각 3개가 NULL인
  기존 사용자(MSG-402 온보딩 경유)를 만들어 네 단계를 한 흐름으로 잇는다:
  ① 동의 상태 조회에서 `requiredCompleted`가 false다
  ② 동의 제출 후 true가 된다
  ③ 기존 위치 토글로 위치를 철회하면 false로 복귀한다 (다음 게이트 재표시 재료)
  ④ 게이트 재제출로 다시 true가 되고, 이때 위치 변경 시각이 갱신된다)
- `동의_상태_조회_JSON은_6개_필드_required_계약을_지킨다` (`ResponseSchemaNullabilityTest` 계열)
- `토큰_없는_요청은_2403이다` (3개 엔드포인트)
- `없는_사용자의_제출은_1404다` (영향 행 0 경로)
- `미동의_사용자도_다른_API가_그대로_동작한다` (FR-9, 신규 차단 로직 부재 확인용 대표 1건)

갱신 검증은 영속성 컨텍스트 재사용에 숨지 않고 별도 트랜잭션 또는 clear 후 재조회로 DB 실저장
값을 확인한다 (MSG-402 교훈, `@Modifying` 경로). 응답 DTO에 시각 필드가 없어
`DtoTimeTypeGuardTest`는 해당 없음. 직렬화 검증은 스프링 조립 매퍼만 쓴다 (손조립 금지).

## 리스크 (스펙 비차단, PRD 미해결 승계)

- 만 14세 이상을 자기 확인 체크만으로 두는 것의 법적 충분성은 법무 확인 대기다. 결론이 다르면
  항목(생년월일 등)이 늘어나는 수준이라 이 스펙의 구조(컬럼 추가·제출 검증 추가)로 흡수 가능하다.
- 마케팅 수신동의 2년 주기 고지 의무 대응은 이번 범위 밖이다. `marketing_consent_changed_at`이
  그 시점 판정의 재료가 되도록 시각을 남기는 것까지가 이 티켓이다.

## 미해결 질문

서버 구현을 막는 미해결 질문은 없다. 저장 모델(users 컬럼 확장)과 게이트 판별(전용 조회)은
2026-08-19 정민 확정을 그대로 따랐다.

[^1]: 게이트: 조건을 채우기 전에는 다음 화면으로 못 가게 막는 클라이언트 관문. 미동의 차단을 서버가 아니라 클라이언트가 맡는 원칙은 FR-USER-14에서 확정됐고 이 티켓도 같다.
[^2]: 멱등(idempotent): 같은 요청을 여러 번 보내도 한 번 보낸 것과 결과가 같은 성질. 게이트 화면 재진입이나 네트워크 재시도로 제출이 중복될 수 있어 필요하다.
[^3]: 원자 UPDATE: 읽기와 판정과 쓰기를 UPDATE문 하나에 담아 DB가 한 번에 실행하게 하는 방식. 문장 실행 중 행 잠금이 걸려 동시 요청이 직렬화되고, 부분 저장 상태가 생길 수 없다.
[^4]: COALESCE: 인자 중 첫 번째 NULL 아닌 값을 고르는 SQL 함수. 여기서는 "이미 시각이 있으면 유지, 없으면 지금 시각"이 되어 최초 동의 시각 보존을 한 표현식으로 만든다.
[^5]: IS DISTINCT FROM: NULL을 보통 값처럼 취급하는 PostgreSQL의 불일치 비교. `=`는 NULL 비교가 unknown이 되어 CASE 분기가 새는데, 이 연산자는 NULL과 값의 비교도 참·거짓으로 떨어진다.
[^6]: Clock: 자바 표준 시계 추상화(`java.time.Clock`). 프로덕션은 `Clock.systemUTC()` 고정, 테스트는 고정 시각 주입으로 "지금"이 들어간 로직을 결정적으로 검증한다. 인자 없는 `now()` 금지 컨벤션의 실행 수단이다.

## 작업 로그

### 2026-08-19 구현 완료 (spec-driven-dev)

- auth-dev(Owner B)가 전 범위 구현. 변경: V37 마이그레이션, `User` 필드 5개, `UserRepository.submitConsents`·`updateMarketingConsent`(native), `UserService(+Impl)` 메서드 3개(기존 Clock 주입 재사용), `UserController` API 3종, DTO 3종, 테스트 2파일(통합 16·컨트롤러 19). auth 패키지·프로필 DTO·location-consent 엔드포인트 무수정.
- convention-reviewer 통과: 전체 빌드 테스트 1865건 실패 0, 인자 없는 now() 0건(전부 주입 Clock의 `LocalDateTime.now(clock).truncatedTo(MICROS)`), DtoTimeTypeGuardTest green, SuccessResponse 래핑·신규 에러코드 0 확인.
- **데이터 저장 위치**: `users` 행 컬럼만 — V37이 추가한 `age_over14_consented_at`·`service_terms_consented_at`·`privacy_consented_at`(TIMESTAMP NULL, NULL=미동의)·`marketing_consent`(BOOLEAN NOT NULL DEFAULT FALSE)·`marketing_consent_changed_at`(TIMESTAMP NULL) + 기존 `location_consent` 쌍. Redis·S3·outbox 무관, 탈퇴 시 행과 함께 소멸.
- **실행 쿼리** (submitConsents 원문): `UPDATE users SET age_over14_consented_at = COALESCE(age_over14_consented_at, :now), service_terms_consented_at = COALESCE(service_terms_consented_at, :now), privacy_consented_at = COALESCE(privacy_consented_at, :now), location_consent = TRUE, location_consent_changed_at = CASE WHEN location_consent IS DISTINCT FROM TRUE THEN :now ELSE location_consent_changed_at END, marketing_consent = :marketing, marketing_consent_changed_at = CASE WHEN marketing_consent IS DISTINCT FROM :marketing THEN :now ELSE marketing_consent_changed_at END WHERE id = :userId` — `@Modifying(clearAutomatically = true)`라 갱신 직후 findById가 DB 값을 읽는다. updateMarketingConsent는 마케팅 쌍만 같은 CASE 패턴.
- **예외 흐름**: 필수 4항목 false/누락과 marketing 누락은 `ConsentSubmitRequestDto`의 `@NotNull`/`@AssertTrue`가 컨트롤러 앞에서 잡아 `MethodArgumentNotValidException` → GlobalExceptionHandler가 400 BAD_REQUEST + 필드 메시지(서비스 미도달을 테스트가 verifyNoInteractions로 단언). UPDATE 0행이면 `ApiException(USER_NOT_FOUND)` → 404/1404. 무토큰은 필터 401.
- **빈 동작**: 신규 빈 없음. 서비스 메서드 `@Transactional`, 스케줄러·afterCommit 훅 없음.
- **실사용자(프론트 관점) 실측** (2026-08-19, 로컬 bootRun + curl 11단계): 가입 직후 전항목 false·requiredCompleted false → 필수 하나 false 제출 400(필드 메시지) → 필드 누락 400 → 정상 제출 후 requiredCompleted true → 마케팅 토글 → 기존 위치 토글 철회 시 requiredCompleted false 복귀(게이트 재표시 재료) → 재제출로 true → 무토큰 401. DB 실측: 필수 3항목 시각은 첫 제출값(07:50:07.829) 그대로 보존, 위치 변경 시각만 재동의 시점(.961) 갱신, 마케팅 시각은 동일 값 재제출이라 유지(.874) — COALESCE·IS DISTINCT FROM 실전 확인.
- **V37 번호 경합 주의**: MSG-434(알림함) 레인도 V37을 예약. 늦게 머지되는 쪽이 V38로 리네임해야 하고, 로컬 DB에는 이 V37이 이미 적용돼 있어 리네임 시 flyway_schema_history 정리가 필요하다.
- 커밋: `6058f6e MSG-433 feat: 약관 동의 5항목 저장 컬럼과 원자 갱신 쿼리 추가` · `57184c9 MSG-433 feat: 동의 상태 조회와 제출, 마케팅 수신 변경 API 추가`

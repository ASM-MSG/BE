# MSG-438: 행사 도메인 데이터 모델, 시드, 대표 격자

**Owner**: B. `com.msg.fillmap.event.*`는 신규 패키지라 CLAUDE.md 협업 원칙 목록에 아직 없다.
행사방은 위치별 영상, 댓글, 알림으로 이어지는 콘텐츠 도메인이고 기존 `videos` 테이블을 참조하는
확장 모델(아래 데이터 모델 참조)이므로 Owner B가 맞다. MSG-439 스펙의 Owner B 제안에 동의하며,
CLAUDE.md Owner B 목록에 `com.msg.fillmap.event.*`를 추가하는 커밋은 스키마를 확정하는 이
티켓이 수행한다(439가 편승을 기대하고 있는 지점). grid 도메인 코드는 건드리지 않는다. 이 티켓이
grid에서 가져다 쓰는 것은 격자 계산 규칙의 산술뿐이고, 그마저 아래에서 보듯 투영 호출 없이
인덱스 정수 연산으로 끝난다.

> 요구사항 정본: `docs/prd/event-room-location-videos.md` (확정, 2026-08-20 정민 승인),
> `docs/srs.md` FR-EVENT-07(시리즈와 회차 분리), FR-EVENT-08(격자 영역과 대표 격자 하나).
> 구 PRD `docs/prd/event-room.md`는 제보와 참여 모델이 폐기된 이전 정본이며, 뷰포트 칩 노출,
> 시 칩 묶음, 수동 시드 등재, 미션 축 비연계 결정만 승계된다(SRS FR-EVENT-01 근거란과 일치
> 확인함). 이 스펙은 그중 저장 계층의 "어떻게"만 정한다.

## 개요

행사방의 저장 기반을 만든다. 테이블 다섯 개(`event_series`, `event_occurrences`,
`event_locations`, `event_location_grids`, `event_videos`)와 대응 엔티티, 수동 시드 파일과
기동 시더, 그리고 대표 격자 3단 결정 로직까지가 범위다. API 노출은 MSG-439(조회)와
MSG-440(업로드) 몫이라 이 티켓에는 컨트롤러가 없다.

## 배경, 목표

행사 영상은 행사 위치의 대표 격자[^1] 하나에만 연결하고 영역 전체에 복제하지 않는다는 것이
정본 PRD의 핵심 결정이다(FR-EVENT-08). 그 결정이 성립하려면 저장 계층이 세 가지를 보장해야
한다. 첫째, 같은 행사가 다시 열려도 회차 간 데이터가 섞이지 않는 시리즈와 회차 분리
구조(FR-EVENT-07). 둘째, 영역 내 어느 격자를 눌러도 같은 위치로 해석되는 격자 귀속의
유일성(한 격자는 한 회차 안에서 위치 하나에만 속한다). 셋째, 모든 위치가 항상 유효한 대표
격자 하나를 갖는다는 불변식이다. 이 스펙은 세 보장을 가능한 한 DDL 제약으로 내리고, 제약으로
못 내리는 것만 시더 검증으로 막는다.

## 성공 기준

- 홀수 행과 홀수 열의 직사각형 영역을 시드하면 정중앙 격자가 대표로 저장된다 (PRD 테스트 1).
  판정은 시드 표현이 아니라 전개된 격자 집합의 기하 기준이라, 같은 영역을 사각형 몇 개로
  그렸든 결과가 같다.
- 비직사각형 영역에서는 운영자 지정값이, 지정도 없으면 영역 중심 최근접 포함 격자가 계산되어
  저장된다 (PRD 테스트 2, 3). 세 경로 모두 결과가 결정적이다(같은 입력이면 항상 같은 격자).
- 시드를 반복 실행해도 결과가 같다(멱등[^2]). 영역이 바뀐 재시드는 격자 행을 증분 반영하고
  대표 격자를 다시 계산하며, 격자를 한 위치에서 다른 위치로 옮기는 재시드도 한 트랜잭션에서
  성공한다. 단 영상이 이미 붙은 위치의 대표 격자 변경은 거부된다(아래 시드 참조).
- 한 회차 안에서 두 위치에 같은 격자를 넣은 시드는 DB 제약 위반으로 기동에 실패한다.
  다른 회차끼리는 같은 격자를 각자 가질 수 있다.
- 영상 연결 행의 회차와 위치의 회차가 어긋난 저장은 DDL이 거절한다 (FR-EVENT-07의 미혼합을
  쓰기 시점에 보장, MSG-439가 이 무결성 소유를 438 DDL로 지목한 항목).

아래 "테스트 시나리오"가 이 기준의 검증 수단이다. 테스트에는 `// 검증: FR-EVENT-07` 형식
주석을 단다(finalize 2-1).

## API 명세

없음. 이 티켓은 저장 계층 전용이다. 런타임 API가 없으므로 `EventErrorCode` enum도 이 티켓에서
만들지 않는다(첫 사용자인 MSG-439가 만든다). 단 **developCode 13xxx 대역 표
행(response-pattern.md) 추가 커밋은 이 티켓이 선행 수행한다**. 대역 선점은 표 갱신 커밋이
먼저라는 규칙(MSG-178, 185 경합 사례) 때문이고, 439가 이 커밋의 존재를 하드 게이트로
확인한다.

## 도메인 로직

### 대표 격자 3단 결정 (FR-EVENT-08, PRD §4.1)

시딩 시점에 계산해 `event_locations.representative_grid_id`에 저장한다. 조회와 업로드는
저장값만 읽는다. 계산 입력은 시드의 사각형 목록이 아니라 **전개된 격자 집합**(사각형 합집합을
셀 단위로 펼치고 중복 제거한 것)이다. 같은 영역을 어떤 사각형 조합으로 표현했든 집합이 같으면
결과가 같아야 하므로, 판정을 표현이 아니라 기하에 건다. 결정 순서:

1. **홀수 직사각형 정중앙**: 집합의 경계 상자(`minGridY..maxGridY`, `minGridX..maxGridX`)에
   대해 `집합 크기 == 행 수 × 열 수`이면 영역은 직사각형이다(표현 무관 판정). 행 수
   `maxGridY - minGridY + 1`과 열 수 `maxGridX - minGridX + 1`이 모두 홀수면, 대표는
   `((minGridY + maxGridY) / 2, (minGridX + maxGridX) / 2)`다(합이 짝수라 정수 나눗셈이
   정확히 떨어진다). 이 경우 시드에 운영자 지정값이 있는데 정중앙과 다르면 모순 데이터로
   보고 시딩을 실패시킨다. FR-4가 정중앙을 무조건으로 요구하므로 지정값을 조용히 무시하면
   시드 파일과 저장값이 어긋난 채 남기 때문이다.
2. **운영자 지정**: 영역이 1의 조건을 벗어나면 시드의 `representativeGridId`를 쓴다. 지정
   격자가 영역 밖이면 시딩을 실패시킨다(FR-3 "유효한 대표 격자". DDL 복합 FK가 최종
   방어선이지만 지연 제약 위반 메시지는 읽기 어려우므로 시더가 먼저 명확한 메시지로 막는다).
3. **영역 중심 최근접 계산**: 지정도 없으면 영역 중심에 가장 가까운 소속 격자를 고른다.
   집합 크기 `n`, 좌표 합 `Sy = Σ gridY`, `Sx = Σ gridX`에 대해
   `(n·gridY - Sy)² + (n·gridX - Sx)²`를 최소화하는 격자다(중심 `(Sy/n, Sx/n)`과의 거리
   제곱에 `n²`을 곱한 동치식이라 **long 정수 산술만으로 비교**되고 부동소수점 오차가 없다).
   동률이면 `gridY`가 작은 것, 그다음 `gridX`가 작은 것(남서 우선)으로 결정성을 확보한다.

투영 호출이 필요 없는 이유: 격자 계산 규칙(EPSG:5179[^3], glossary)에서 인덱스 공간은 미터
평면을 100분의 1로 축소한 균일 평면이라, 인덱스 거리 비교가 실거리 비교와 동치다. 셀 중심
오프셋 0.5도 양변에서 상쇄되므로 순수 정수와 평균 연산으로 끝난다. `GridEncoder`를 부르지
않는다.

구현 위치 제안: `com.msg.fillmap.event.seed.RepresentativeGridResolver` (순수 함수, 시더만
호출). 입력은 전개된 영역 격자 집합과 운영자 지정값, 출력은 `"{gridY}_{gridX}"` 격자 id다.

### 행사 상태는 파생값 (저장 컬럼 없음)

PRD §8은 `status`와 `uploadClosesAt` 컬럼을 제안하지만 **둘 다 저장하지 않는다**. 두 값은
`startsAt`, `endsAt`와 서버 시각만으로 완전히 계산되고, 저장하면 일정 변경(행사 알림의 존재
이유가 일정 변경이다) 때 파생값과 어긋나는 잠복 버그가 생기며, 상태 전환 배치가 필요해진다.
파생으로 두면 PRD §10의 "상태 전환 작업은 재실행해도 안전해야 한다"는 요구가 작업 자체의
소멸로 충족된다(FR-22 서버 시각 판정과도 일치).

- `EventOccurrence.uploadClosesAt()` = `endsAt.plusDays(UPLOAD_GRACE_DAYS)`, 상수
  `UPLOAD_GRACE_DAYS = 30`.
- `EventOccurrence.statusAt(LocalDateTime now)` 도메인 메서드를 이 티켓이 구현한다(439가
  제안한 시그니처 채택). 반환 enum `EventStatus { UPCOMING, LIVE, UPLOAD_GRACE, ARCHIVED }`
  (비영속). 경계는 전부 반개구간[^4]이다: `now < startsAt`이면 UPCOMING,
  `startsAt <= now < endsAt`이면 LIVE, `endsAt <= now < uploadClosesAt()`이면 UPLOAD_GRACE,
  그 이후는 ARCHIVED. 정각은 다음 상태에 속한다(439 API 2의 정의와 동일, 정의는 이 메서드
  한 곳에만 둔다).
- `now`는 호출자가 주입한다. 엔티티 안에서 `LocalDateTime.now()`를 부르지 않는다
  (시각 컨벤션 MSG-376).

### EventVideo는 videos의 확장이다

행사 영상 레코드는 독립 파일 컬럼(PRD §8의 `videoUrl`, `thumbnailUrl` 등)을 갖지 않고 기존
`videos` 행을 1:1로 참조한다. 근거는 PRD §10의 "영상 접근 권한, 신고, 삭제 등 기존 공통 영상
정책을 그대로 적용한다"이다. 신고(`reports.video_id`), 처리 파이프라인(인코딩, AI 블러,
READY 전이), 소프트 삭제[^5](`VideoStatus.DELETED`), 재생 접근 제어가 전부 `videos` 행을
축으로 돌아가므로, 독립 테이블이면 그 전부를 복제해야 한다. PRD §8의 필드 제안 중
`videoUrl`, `thumbnailUrl`, `durationSeconds`, `capturedAt`, `createdAt`은 `videos`의 기존
컬럼(`encodedUrl`, `thumbnailUrl`, `duration_sec`, `recorded_at`, `created_at`)이 대신하고,
`interactionLocked`는 회차 상태의 파생값이라 저장하지 않는다. **영상 길이 제한도 기존과
동일하다(최대 30초, 2026-08-20 사용자 확정)**: `videos.duration_sec`(NOT NULL)가 이미 있고
US-004 카드의 길이 표시 재료도 이 컬럼이다. 상한 검증은 기존 업로드 검증을 440이 재사용한다.

이 선택의 귀결 세 가지를 명시한다.

- **노출 술어는 기존 전역 게이트와 동일**: 439의 `videoCount`와 440의 피드가 공유할 술어는
  `videos` 조인 후 `status = ACTIVE AND visibility = PUBLIC AND processing_status = READY`다.
  삭제(DELETED), 블라인드(BLINDED), 비공개, 처리 미완료가 한 술어로 걸러지므로 숫자와 피드의
  불일치, 존재 노출이 구조적으로 없다. 440 업로드는 공개범위를 PUBLIC으로 고정한다(행사
  피드는 공개 전제, 공개범위 선택 UI가 PRD에 없음). **PUBLIC 고정의 자연 귀결로, 기존
  공개범위 변경 API는 `event_videos` 행이면 요청을 거부한다**(행사 영상을 FRIENDS나
  PRIVATE로 바꾸면 행사 피드에서 사라져 고정이 무의미해진다). 강제와 차단 구현 모두 440
  쓰기 경로 몫이다.
- **전역 격자 피드에도 보인다 (2026-08-20 사용자 확정: "영상을 올리면 그 격자에 남아야
  한다")**: 행사 영상은 대표 격자에 놓인 정상 `videos` 행이므로 기존 격자 전역 조회(대표
  영상, 전역 목록)에 그대로 잡히고, 이것이 확정된 제품 정책이다. 별도 필터 코드가 없다.
- **점령은 만들지 않는다 (2026-08-20 사용자 확정)**: 행사 업로드는 위치 증거가 전혀
  없으므로(PRD §4.3, 현재 위치와 GPS 메타데이터 비필수) 도감의 "방문한 장소" 의미(glossary)를
  지키기 위해 대표 격자에 점령(`user_grids`)을 생성하지 않는다. **삭제도 대칭이다**: 행사
  영상 삭제는 점령 롤백 경로(`VideoServiceImpl:356`의 `decrementVideoCount` 호출과 이어지는
  `deleteUserGridIfEmpty`)를 타지 않아야 한다. 업로드가 안 올린 카운트를 삭제가 내리면,
  같은 격자를 정규 업로드로 점령한 사용자의 `video_count`가 행사 영상 삭제로 잘못 줄어
  조기 롤백까지 갈 수 있다. 점령 계정에서 행사 영상을 양방향 모두 제외하는 것이 이 스펙의
  계약이고, 구현과 테스트는 쓰기 경로 소유자인 440 몫이다.

**기존 업로드 파이프라인 부수효과 계약**. 판정 기준은 "기존 쿼리가 실제로 무엇을 세는가"다.
`videos` 행 기반 시스템은 훅 호출 여부와 무관하게 행이 생기는 순간 자동으로 잡히므로, 제외가
필요한 곳은 어디에 무슨 코드가 필요한지까지 적는다. 포함으로 정한 스트릭, 핫스코어, 업로드 수
뱃지는 글로서리 정본("재방문 포함 아무 업로드"가 스트릭 인정 이벤트, "재방문 업로드도 전부
신호"가 핫스코어)에서 도출한 **정본 준수 기본값이며, 사용자 번복 가능**하다.

| 부수효과 | 판정 | 근거와 성립 메커니즘 |
|---|---|---|
| 인코딩, AI 블러, READY 전이 | 적용 | `videos` 행 자체가 파이프라인 대상 (PRD §10 파일 정책 동일) |
| 처리 종결 알림(outbox) | 적용 | 같은 파이프라인의 종결 이벤트 |
| 길이 상한 검증(30초) | 적용 | 2026-08-20 사용자 확정, 기존 업로드 검증 재사용(440) |
| 전역 격자 피드 노출 | 적용 | 2026-08-20 사용자 확정. `videos` 행이라 기존 격자 조회에 자동 포함 |
| 스트릭 갱신 | 적용 | 글로서리 정본 "아무 업로드" 준수 기본값. 440이 기존 스트릭 훅을 그대로 호출 |
| 핫스코어 신호 | 적용 | 글로서리 정본 "모든 업로드 신호" 준수 기본값. 440이 기존 신호 훅을 그대로 호출 |
| 업로드 수 뱃지(UPLOAD_COUNT) | 적용 | 집계문(`UserBadgeRepository.countMyVideos:48`)이 사용자의 `videos` 행 전량을 세므로 자동 포함, 판정 훅도 호출(스트릭과 같은 "아무 업로드" 논리) |
| 획득 뱃지 즉시 응답(newBadges) | 적용 | UPLOAD_COUNT가 행사 업로드로도 획득될 수 있으므로 업로드 응답에 `newBadges` 동봉 계약 유지 (FR-BADGE-05, `VideoUploadResponseDto:35` 선례, 구현 440) |
| 점령, 지역 축 뱃지(TOTAL_GRIDS, REGION_PERCENT) | 제외 | 440이 `awardCollectionBadges` 훅을 호출하지 않아 성립한다. 판정 원천도 갱신되지 않는다: TOTAL_GRIDS는 `user_grids` 집계(`UserBadgeRepository.countMyGrids:52`)인데 점령 미생성이고, REGION_PERCENT는 `region_stats` 스냅숏(`UserBadgeRepository.findMyRegionProgress:62`)인데 점령 롤백·수집률 갱신 경로를 타지 않는다 |
| 점령 upsert(user_grids) | 제외 | 2026-08-20 사용자 확정. 쓰기 훅이라 440이 호출을 생략하면 성립 |
| 점령 롤백, video_count 증감 | 제외 | 귀결 3의 대칭 계약. 440이 삭제 흐름(`VideoServiceImpl:356`의 `decrementVideoCount` 호출부터)을 분기 |
| 미션 진행도, 완료, 방문 스팟, 미션 영상 목록·수 | 제외 (계약) | 승계 확정 "미션 비연계". 여섯 쿼리 전부 `videos`를 직접 조인하므로 훅 생략으로는 성립하지 않고, **각각에 `event_videos` 안티조인[^7] 술어 추가가 필요**하다: 진행도(`MissionRepository.findProgress:46`), 완료 판정(`MissionRepository.findCompleted:96`), 방문 스팟(`MissionGridRepository.findVisitedGridIds:29`), 미션 영상 첫 페이지(`VideoRepository.findMissionVideos:142`), 다음 페이지(`VideoRepository.findMissionVideosAfter:166`), 격자별 수(`VideoRepository.countMissionVideosByGrid:187`) |
| 도감 지표, 지역별 목록, 커버·썸네일 | 제외 (계약) | 점령 미생성 확정의 일관성. 네 경로가 `videos`를 직접 세거나 고르므로 같은 안티조인 술어가 필요하다(구현 440): 방문 행정동 집계(`UserGridRepository.getCollectionSummary:43` 서브쿼리), 지역별 내 영상 목록(`UserGridRepository.getRegionVideos:139`), 친구 도감 썸네일 fallback(`UserGridRepository.getCollectionGridsForFriend:157` 서브쿼리), 커버 재선정(`VideoRepository.reselectCover:388`) |

**구현 노트 (제외 계약의 구현 배정과 배포 게이트)**: 미션 쿼리의 안티조인 여섯 쿼리는 439와
440 어느 티켓의 범위도 아니다. 미션 도메인 쿼리 측 후속 구현이 필요하며 티켓 배정은
오케스트레이터 몫이다. **배포 게이트: 이 안티조인 여섯 쿼리는 MSG-440 배포 전에, 늦어도
440과 같은 배포 단위에 들어가야 한다.** 440이 먼저 나가면 첫 행사 업로드부터 미션 진행도에
행사 영상이 섞여 "미션 비연계" 확정이 즉시 깨진다. 계약 자체(행사 영상은 미션 진행에 잡히지
않는다)는 이 스펙이 정본이다. 도감 지표, 지역별 목록, 커버·썸네일의 안티조인은 440 몫이다.

`videos.grid_id`가 대표 격자와 일치한다는 보장은 서비스 계층이 소유한다(440 쓰기 경로와 그
테스트). DDL로도 표현할 수는 있다: `event_videos`에 `grid_id`를 중복으로 두고
`videos(id, grid_id)`와 `event_locations(id, representative_grid_id)` 양쪽에 복합 FK를 걸면
된다. 채택하지 않는 근거: `videos`에 `UNIQUE(id, grid_id)` 추가 마이그레이션과 중복 컬럼이
필요한데, FK가 막아줄 시나리오(영상이 있는 위치의 대표 격자 변경)는 시더 가드(아래 시드
참조)가 이미 막아 추가 보장이 중복되고, 남는 쓰기 지점이 440 하나뿐이라 서비스 강제와
테스트로 충분하다.

`videos`의 NOT NULL 요건 충족도 계약으로 남긴다(구현 440): 행사 업로드는 좌표 입력이 없으므로
`geom`은 대표 격자 중심점(`GridEncoder`의 셀 중심 위경도 변환)으로 채우고, `grids` FK는 기존
lazy insert(전역 격자 등록) 경로를 그대로 재사용해 충족한다.

## 데이터 모델

Flyway `V39__event_room_schema.sql` 하나로 테이블 다섯 개를 만든다. 댓글과 도움돼요 테이블은
MSG-441 몫(V40 이후)이고, 알림 구독은 MSG-442 몫이다.

```sql
-- V39__event_room_schema.sql
-- MSG-438: 행사방 저장 기반. 시리즈/회차 분리(FR-EVENT-07), 격자 영역과 대표 격자(FR-EVENT-08).
-- 격자 컬럼은 grids 테이블을 FK 하지 않는다(lazy insert 전략상 행 존재가 보장되지 않음).

CREATE TABLE event_series (
	id         BIGSERIAL PRIMARY KEY,
	series_key VARCHAR(50) NOT NULL UNIQUE,  -- 시딩 UPSERT 자연키, 예: "biff"
	name       VARCHAR(100) NOT NULL
);

CREATE TABLE event_occurrences (
	id              BIGSERIAL PRIMARY KEY,
	event_series_id BIGINT NOT NULL REFERENCES event_series(id),
	occurrence_key  VARCHAR(60) NOT NULL UNIQUE,  -- 자연키, 예: "biff-busan-2026"
	title           VARCHAR(100) NOT NULL,
	city_name       VARCHAR(30) NOT NULL,   -- 시 칩 묶음 기준 라벨, 예: "부산" (FR-EVENT-01)
	starts_at       TIMESTAMP NOT NULL,     -- UTC (전 테이블 공통, MSG-376 체계)
	ends_at         TIMESTAMP NOT NULL,
	visible_from    TIMESTAMP NOT NULL,     -- 칩 노출 시작 = starts_at - 14일 (파생 고정, 시더 계산)
	min_grid_y      INTEGER NOT NULL,       -- 노출 영역 정수 사각형(뷰포트 겹침 판정 재료, zones 선례)
	max_grid_y      INTEGER NOT NULL,
	min_grid_x      INTEGER NOT NULL,
	max_grid_x      INTEGER NOT NULL,

	CONSTRAINT chk_event_occ_period    CHECK (starts_at < ends_at),
	-- 파생 규칙을 DDL로 고정: 어떤 쓰기 경로도 2주 전 정책을 우회하지 못한다 (2026-08-20 확정)
	CONSTRAINT chk_event_occ_visible   CHECK (visible_from = starts_at - INTERVAL '14 days'),
	CONSTRAINT chk_event_occ_y_range   CHECK (min_grid_y <= max_grid_y),
	CONSTRAINT chk_event_occ_x_range   CHECK (min_grid_x <= max_grid_x)
);

CREATE TABLE event_locations (
	id                     BIGSERIAL PRIMARY KEY,
	event_occurrence_id    BIGINT NOT NULL REFERENCES event_occurrences(id),
	location_key           VARCHAR(60) NOT NULL UNIQUE,  -- 자연키, 예: "biff-2026-cinema-center"
	name                   VARCHAR(100) NOT NULL,        -- "영화의전당"
	type                   VARCHAR(20) NOT NULL,         -- EventLocationType enum 문자열
	operating_hours        VARCHAR(100),                 -- 표시용 문자열, 없는 위치(퍼레이드) NULL
	display_order          INTEGER NOT NULL DEFAULT 0,   -- 위치 목록 정렬 1차 키(439 계약)
	representative_grid_id VARCHAR(20) NOT NULL,

	CONSTRAINT uq_event_locations_id_occ UNIQUE (id, event_occurrence_id)  -- 비정규화 FK 대상
);

CREATE TABLE event_location_grids (
	event_location_id   BIGINT NOT NULL,
	event_occurrence_id BIGINT NOT NULL,     -- 비정규화: 회차 내 격자 유일성 제약 재료
	grid_id             VARCHAR(20) NOT NULL,

	PRIMARY KEY (event_location_id, grid_id),
	-- 한 격자는 한 회차 안에서 위치 하나에만 귀속(439 계약 3). 행 단위 저장이라 EXCLUDE 불요.
	-- DEFERRABLE: 격자를 위치 A에서 B로 옮기는 재시드에서 Hibernate flush 가 insert 를 delete 보다
	-- 먼저 실행하므로, 즉시 제약이면 순서 통제가 필요해진다. 커밋 시점 검증으로 순서 의존을 없앤다.
	CONSTRAINT uq_event_grid_per_occ UNIQUE (event_occurrence_id, grid_id)
		DEFERRABLE INITIALLY DEFERRED,
	-- 비정규화 정합: 격자 행의 회차 = 소속 위치의 회차임을 DDL이 보장
	CONSTRAINT fk_event_grid_location FOREIGN KEY (event_location_id, event_occurrence_id)
		REFERENCES event_locations(id, event_occurrence_id) ON DELETE CASCADE
);

-- 대표 격자는 반드시 영역 소속(FR-3). 순환 참조라 지연 제약: 같은 트랜잭션 안에서
-- 위치 insert 후 격자 insert 가 가능하고, 커밋 시점에 소속이 검증된다.
ALTER TABLE event_locations
	ADD CONSTRAINT fk_event_loc_rep_grid FOREIGN KEY (id, representative_grid_id)
		REFERENCES event_location_grids(event_location_id, grid_id)
		DEFERRABLE INITIALLY DEFERRED;

CREATE TABLE event_videos (
	video_id            BIGINT PRIMARY KEY REFERENCES videos(id) ON DELETE CASCADE,
	event_occurrence_id BIGINT NOT NULL,
	event_location_id   BIGINT NOT NULL,

	-- 영상의 회차 = 소속 위치의 회차(FR-EVENT-07 미혼합의 쓰기 시점 보장)
	CONSTRAINT fk_event_video_location FOREIGN KEY (event_location_id, event_occurrence_id)
		REFERENCES event_locations(id, event_occurrence_id)
);

-- 위치별 count(439)와 피드(440)의 조회 키. 사용자 콘텐츠라 증가 테이블이므로 인덱스를 단다.
CREATE INDEX idx_event_videos_location ON event_videos(event_location_id);
```

- `video_id`가 PK이므로 한 영상은 최대 하나의 행사 위치에만 연결된다(FR-7 "각각 한 번만").
- 행사 이미지 필드는 **이번 범위 없음 확정(2026-08-20 사용자)**이라 컬럼을 두지 않는다.
  US-002의 행사 이미지는 이번 범위에서 서버 재료가 없다.
- `event_location_grids`에 역조회(`grid_id` 선두) 인덱스를 달지 않는다. 수동 시드 소량(도시별
  행사 한두 개, 위치당 격자 수십)이라 어떤 접근도 인덱스를 주도로 타지 않는다(zones V8 선례와
  같은 근거). 행사 수가 커지면 그때 단다.
- `event_series`와 회차, 위치, 격자 행의 시드 파일 삭제는 DB 반영하지 않는다(아래 시드 참조).
  행 삭제가 필요하면 수동 SQL이다. 아카이브 보존이 요구사항(US-010)이라 자동 삭제가 더
  위험하다.

### 엔티티 (`com.msg.fillmap.event.entity`)

영속 계층 컨벤션의 2026-08-19 개정(연관관계 허용)을 따르는 신규 코드다. 이 워크트리의
`project-conventions.md` 사본이 개정 전이면 메인 체크아웃의 개정본이 기준이다.

| 엔티티 | 핵심 매핑 |
|---|---|
| `EventSeries` | 단독 |
| `EventOccurrence` | `@ManyToOne(LAZY)` EventSeries. `statusAt(now)`, `uploadClosesAt()` 도메인 메서드 보유 |
| `EventLocation` | `@ManyToOne(LAZY)` EventOccurrence, `representativeGridId` 문자열 |
| `EventLocationGrid` | 복합 키(위치 id + grid_id), `@ManyToOne(LAZY)` EventLocation, 회차 id 컬럼 동반 |
| `EventVideo` | `@Id Long videoId` + `@MapsId @OneToOne(LAZY)` Video(공유 PK, 타 도메인 읽기 전용 참조), `@ManyToOne(LAZY)` EventLocation, `event_occurrence_id`는 기본 컬럼 매핑(생성 시 서비스가 위치의 회차 값으로 세팅, 정합은 복합 FK가 검증) |

전 연관 LAZY 명시, 역방향 컬렉션 없음, equals와 hashCode에서 연관 필드 제외. `Video` 참조는
읽기 전용이며 상태 변경은 video 도메인 서비스만 한다(컨벤션 "타 도메인 엔티티 읽기 전용"
조항).

`EventLocationType` enum: `POPUP`(팝업), `EXPERIENCE_ZONE`(체험존), `PARADE`(퍼레이드),
`PHOTO_ZONE`(포토존), `ETC`(기타). 넷은 PRD §2의 예시에서, ETC는 공연장처럼 넷에 안 걸리는
위치의 도피처로 둔다. 표시 라벨 변환은 FE 몫이다(439가 enum 문자열을 그대로 내려보낸다).

## 시드

zones 선례(`seed/zones.json`, 기동 시 멱등 UPSERT, MSG-259 D-8)를 따르되 계층 구조라 파일
형태와 갱신 로직은 이 스펙이 새로 정한다.

- 파일: `src/main/resources/seed/events.json`. 등재 기준은 BTS 부산 콘서트급 초대형 행사만,
  도시별 한두 개다(FR-EVENT-01 "행사 추가는 시드 행 추가만으로 끝난다"). 등재할 행사의
  선정은 사용자가 실제 영향력을 직접 보고 결정한다(2026-08-20 확정). **첫 시드 3건도 같은
  날 사용자가 선택했다**(아래 시드 실값 초안 참조).
- 시더: `com.msg.fillmap.event.seed.EventSeeder` (`ApplicationRunner`).
  `fillmap.event.seed.enabled`(기본 false), dev와 prod 프로파일에서 상시 on(zones와 동일
  운영). regions 등 타 시더와 FK 의존이 없지만 결정성을 위해 `@Order(30)`을 준다(Region 10,
  Zone 20 다음).
- 갱신 로직: 자연키(series_key, occurrence_key, location_key)로 찾아 있으면 갱신, 없으면
  생성한다. zones의 native UPSERT와 달리 JPA 조회 후 저장으로 구현한다. 격자 영역 대조(추가
  행 insert, 제거 행 delete)를 위해 읽기가 어차피 필요하고, ON CONFLICT 같은 PostgreSQL 전용
  기능이 필요한 지점이 없어 native 최소화 권고(2026-08-19 멘토링)에 부합한다. 전체가
  `@Transactional` 한 번이라 두 지연 제약(격자 유일, 대표 격자 소속)도 커밋 시점에 함께
  검증된다. **시더끼리의 동시 실행은 트랜잭션 서두의 advisory lock으로 직렬화한다**
  (`pg_advisory_xact_lock`에 고정 키. 행이 아니라 임의의 키에 거는 PostgreSQL 잠금이라 아직
  존재하지 않는 행에 대한 경쟁도 막고, 트랜잭션 종료 시 자동 해제된다): 롤링 배포 중 두
  인스턴스의 `ApplicationRunner`가 동시에 돌 때 신규 행은 존재하지 않아 행 잠금을 걸 수 없고,
  동시 INSERT는 자연키 UNIQUE 위반으로 한쪽 기동을 실패시키기 때문이다. advisory lock은
  PostgreSQL 전용이라 native 허용 대상이다(`VideoRepository.acquirePendingKeyConfirmLock:349`
  선례).
- `visible_from`은 시드 입력이 아니다. **시더가 항상 `startsAt - 14일`로 계산해 저장한다**
  (2026-08-20 사용자 확정 "행사 시작 2주 전"). 시드 JSON에 필드 자체가 없어 우회할 수 없고,
  DDL CHECK(`visible_from = starts_at - INTERVAL '14 days'`)이 다른 쓰기 경로까지 막는다.
  **기존 회차의 일정 변경 재시드에서는 회차 갱신 메서드가 `startsAt`과 `visibleFrom`을 항상
  함께 세팅한다**(한 flush의 단일 UPDATE로 두 컬럼이 같이 나가므로 행 단위 CHECK 등식이
  중간 상태 없이 유지된다).
- 대표 격자는 매 실행 3단 규칙으로 재계산해 저장한다. 계산이 결정적이라 재계산이 곧
  멱등이고, 영역이 바뀐 재시드에서 갱신을 따로 다룰 필요가 없다. **단 그 위치에
  `event_videos`가 1건이라도 있으면, 재계산 결과든 지정값이든 기존 저장값과 다른 대표
  격자로의 변경을 거부하고 명확한 메시지로 기동을 실패시킨다.** 기존 영상은 옛 격자에, 신규
  영상은 새 격자에 붙어 위치의 피드와 격자 노출이 갈라지는 드리프트를 막기 위해서다.
  **이 확인은 440 업로드와의 동시성 계약과 함께다**(시더끼리는 위 advisory lock이 담당):
  시더는 대표 격자 재계산 전에 해당 `event_locations` 기존 행을 `SELECT FOR UPDATE`(JPA
  `@Lock(PESSIMISTIC_WRITE)` 파생 쿼리)로 잠근 뒤 `event_videos` 존재를 확인하고, 440 업로드
  경로도 `videos.grid_id`를 결정하기 위해 대표 격자를 읽을 때 같은 행 잠금(또는 잠금 후
  재독)을 따른다. 같은 행 하나를 같은 순서로 잠그므로 교착이 없다. 영상 이전(기존
  `videos.grid_id`와 `geom`을 새 대표 격자로 일괄 UPDATE)은 도입하지 않는다. 대표 격자를
  정말 옮겨야 하는 날이 오면 그 이전 로직이 업그레이드 경로다.
- 시드 형식 검증은 fail-fast다: `areaRects`가 비었거나, 어느 사각형이든 `min > max`로
  역전됐으면 즉시 기동 실패다(빈 영역은 대표 격자가 성립할 수 없고, 역전은 전개 결과가
  빈 집합이 되어 오류가 조용히 숨는다). 격자 인덱스는 0 초과 100,000 미만만 허용한다
  (EPSG:5179 국내 정의역을 넉넉히 포함하는 방어 상한, 정수 제곱 산술의 오버플로 여유 확보).
  위치당 **고유 셀 수**는 2,500(5km × 5km 상당)을 상한으로 한다(zones의 남북 26행 캡과 같은
  성격의 입력 상한). 검사는 두 단계다: 단일 사각형의 `행 수 × 열 수`가 2,500을 넘으면 전개
  전에 즉시 거부하고(단일 사각형의 고유 셀 수는 정확히 `행 수 × 열 수`라 과잉 거부가 없는
  OOM 1차 가드), 여러 사각형의 합집합은 고유 셀 집합에 하나씩 추가하다가 2,501번째 고유
  셀에서 즉시 실패한다. 겹침을 중복으로 세지 않으므로 같은 영역의 다른 표현이 다른 판정을
  받지 않는다(표현 무관 계약 유지).
- 파일에서 사라진 시리즈, 회차, 위치는 삭제하지 않는다(위 데이터 모델 참조). 위치 안의 격자
  행만 파일과 완전 일치로 동기화한다. 격자 행은 `event_locations.representative_grid_id`의
  지연 FK가 참조하므로, 삭제가 안전한 이유는 "참조가 없어서"가 아니라 같은 트랜잭션에서
  대표 격자를 재계산해 갱신하고 커밋 시점에 FK가 성립함을 검증하기 때문이다.
- 파일을 못 읽으면 기동 실패(fail-fast, ZoneSeeder와 동일). 빈 배열은 정상이다.

### 첫 시드 실값 초안 (2026-08-20 사용자 선택 3건)

행사 3건과 기간은 공식 사이트 실측이다(2026-08-20 확인): 부산국제영화제 biff.kr "6-15
October, 2026", 부산불꽃축제 bfo.or.kr "2026. 11. 7.(토) 광안리해수욕장, 이기대, 동백섬
일원", 서울세계불꽃축제 hanwhafireworks.com "2026년 9월 5일 토요일, 여의도 한강공원".
시작·종료 시각(시 단위)과 위치 영역 크기는 이 스펙의 제안값이라 조정 가능하다.

```json
[
	{
		"seriesKey": "biff",
		"seriesName": "부산국제영화제",
		"occurrences": [
			{
				"occurrenceKey": "biff-busan-2026",
				"title": "부산국제영화제",
				"cityName": "부산",
				"startsAt": "2026-10-06T10:00:00+09:00",
				"endsAt": "2026-10-15T22:00:00+09:00",
				"exposure": { "minGridY": 16780, "maxGridY": 16890, "minGridX": 11380, "maxGridX": 11530 },
				"locations": [
					{
						"locationKey": "biff-2026-cinema-center",
						"name": "영화의전당",
						"type": "ETC",
						"operatingHours": null,
						"displayOrder": 1,
						"areaRects": [
							{ "minGridY": 16872, "maxGridY": 16876, "minGridX": 11476, "maxGridX": 11480 }
						],
						"representativeGridId": null
					},
					{
						"locationKey": "biff-2026-haeundae-event-zone",
						"name": "해운대 이벤트존",
						"type": "EXPERIENCE_ZONE",
						"operatingHours": null,
						"displayOrder": 2,
						"areaRects": [
							{ "minGridY": 16859, "maxGridY": 16861, "minGridX": 11509, "maxGridX": 11515 }
						],
						"representativeGridId": null
					},
					{
						"locationKey": "biff-2026-nampo-biff-square",
						"name": "남포동 비프광장",
						"type": "ETC",
						"operatingHours": null,
						"displayOrder": 3,
						"areaRects": [
							{ "minGridY": 16791, "maxGridY": 16793, "minGridX": 11392, "maxGridX": 11394 }
						],
						"representativeGridId": null
					}
				]
			}
		]
	},
	{
		"seriesKey": "busan-fireworks",
		"seriesName": "부산불꽃축제",
		"occurrences": [
			{
				"occurrenceKey": "busan-fireworks-2026",
				"title": "부산불꽃축제",
				"cityName": "부산",
				"startsAt": "2026-11-07T18:00:00+09:00",
				"endsAt": "2026-11-07T22:00:00+09:00",
				"exposure": { "minGridY": 16820, "maxGridY": 16870, "minGridX": 11460, "maxGridX": 11515 },
				"locations": [
					{
						"locationKey": "busan-fireworks-2026-gwangalli",
						"name": "광안리해수욕장",
						"type": "ETC",
						"operatingHours": null,
						"displayOrder": 1,
						"areaRects": [
							{ "minGridY": 16853, "maxGridY": 16855, "minGridX": 11468, "maxGridX": 11480 }
						],
						"representativeGridId": null
					},
					{
						"locationKey": "busan-fireworks-2026-igidae",
						"name": "이기대",
						"type": "PHOTO_ZONE",
						"operatingHours": null,
						"displayOrder": 2,
						"areaRects": [
							{ "minGridY": 16829, "maxGridY": 16831, "minGridX": 11477, "maxGridX": 11479 }
						],
						"representativeGridId": null
					},
					{
						"locationKey": "busan-fireworks-2026-dongbaekseom",
						"name": "동백섬",
						"type": "PHOTO_ZONE",
						"operatingHours": null,
						"displayOrder": 3,
						"areaRects": [
							{ "minGridY": 16853, "maxGridY": 16855, "minGridX": 11503, "maxGridX": 11505 }
						],
						"representativeGridId": null
					}
				]
			}
		]
	},
	{
		"seriesKey": "seoul-fireworks",
		"seriesName": "서울세계불꽃축제",
		"occurrences": [
			{
				"occurrenceKey": "seoul-fireworks-2026",
				"title": "서울세계불꽃축제",
				"cityName": "서울",
				"startsAt": "2026-09-05T14:00:00+09:00",
				"endsAt": "2026-09-05T21:30:00+09:00",
				"exposure": { "minGridY": 19460, "maxGridY": 19495, "minGridX": 9480, "maxGridX": 9515 },
				"locations": [
					{
						"locationKey": "seoul-fireworks-2026-yeouido",
						"name": "여의도 한강공원",
						"type": "ETC",
						"operatingHours": null,
						"displayOrder": 1,
						"areaRects": [
							{ "minGridY": 19477, "maxGridY": 19479, "minGridX": 9494, "maxGridX": 9502 }
						],
						"representativeGridId": null
					}
				]
			}
		]
	}
]
```

격자 인덱스의 출처와 검증 의무: 아래 기준 위경도를
`src/test/resources/fixtures/grid-epsg5179-samples.json`의 앵커 2점(부산 `16920_11627`,
서울 `19570_9459`)에서 셀 꼭짓점으로 얻은 국소 선형 근사로 변환한 **초안값**이다. 근사 오차가
최대 한 칸 수준일 수 있으므로, **구현 단계에서 각 기준 위경도를 `GridEncoder.encode`로 전수
재계산해 초안과 대조하고 어긋난 칸을 교정하는 것이 시더 구현의 완료 조건이다**(전 위치가
홀수 직사각형이라 대표 격자는 시더가 정중앙으로 자동 결정한다).

**재계산 결과 (2026-08-20 구현 단계 실측)**: 7개 중 6개는 초안과 일치했고 이기대 하나가
`16830_11479` → `16830_11478`로 한 칸 어긋나, 위 시드 JSON의 이기대 사각형을 서쪽으로 한 칸
옮겨(`minGridX` 11477, `maxGridX` 11479) 정중앙이 실측값과 맞도록 교정했다. 아래 표의 값은
교정 후 값이다.

| 위치 | 기준 위경도 (lat, lon) | 초안 중심 격자 |
|---|---|---|
| 영화의전당 | 35.1717, 129.1240 | `16874_11478` |
| 해운대 이벤트존 | 35.1587, 129.1604 | `16860_11512` |
| 남포동 비프광장 | 35.0987, 129.0287 | `16792_11393` |
| 광안리해수욕장 | 35.1532, 129.1187 | `16854_11474` |
| 이기대 | 35.1318, 129.1233 | `16830_11478` (구현 단계 재계산으로 교정, 초안은 `16830_11479`) |
| 동백섬 | 35.1529, 129.1520 | `16854_11504` |
| 여의도 한강공원 | 37.5284, 126.9327 | `19478_9498` |

- **시각은 오프셋 필수 ISO-8601**로 적고 시더가 UTC `LocalDateTime`으로 변환해 저장한다.
  시드 작성자는 KST로 생각하는데 naive 표기를 허용하면 "naive = UTC" 관례(MSG-376)와 충돌해
  9시간 어긋난 잠복 버그가 되기 때문이다. 오프셋 없는 표기는 파싱 실패로 기동이 멈춘다.
- `areaRects`는 사각형 합집합이다. 사각형 하나면 직사각형 영역이고, 비직사각형 영역은
  사각형 여러 개로 그린다. 같은 위치 안 사각형끼리 겹침은 합집합 의미라 허용하고(격자 행
  중복 제거), 위치끼리 겹침은 `uq_event_grid_per_occ` 위반으로 기동이 실패한다. 정중앙
  판정은 전개 집합 기하 기준이라 여러 사각형으로 그린 영역이라도 전개 결과가 홀수
  직사각형이면 정중앙 규칙을 탄다(도메인 로직 1 참조).
- 서울세계불꽃축제는 시작이 9-05라 `visibleFrom`이 8-22로 계산된다. 시드가 배포되는 즉시
  칩이 노출되는 셈이라, 배포 시점이 8-22 이후면 의도된 동작이고 그보다 앞설 일은 없다
  (오늘이 8-20이다). 노출을 늦추고 싶으면 시드에서 이 행사만 빼면 된다.

## MSG-439 계약 입력 5건 판정

439 스펙의 선행 의존 체크리스트와 대조한 결과다. 초기 판정에서 "439 표기 교체 필요"로 지목했던
3건(uploadClosesAt 파생값, 저장 status 컬럼 없음, 행사 이미지 동봉 없음)은 **439 세션이 이미
반영했음을 현행 MSG-439.md 기준으로 확인했다.** 아래 항목 1의 지목 이력은 그 반영의 근거
기록으로 남긴다.

1. **회차 조회 재료 (채택)**: `min/max_grid_y/x` 노출 사각형, `city_name`, `visible_from`을
   439가 가정한 이름 그대로 `event_occurrences`에 둔다. 표기 교체 불필요. 지목했던 정정
   3건은 439에 반영 완료됐다: `uploadClosesAt`은 저장값이 아니라 파생값(`uploadClosesAt()`)
   노출이고(응답 필드 계약 불변), 저장 `status` 컬럼은 없어 파생 전용이며(439는 이미
   파생값만 사용), **행사 이미지는 이번 범위 없음 확정**(2026-08-20 사용자)이라 컬럼도
   응답 동봉도 없다. `statusAt(now)` 제안은 채택했고 정의는 이 티켓의 엔티티 한 곳이다.
2. **EventVideo 노출 술어 재료 (채택, 표현 확정)**: `videos` 1:1 참조 모델이므로 술어는 기존
   전역 게이트 그대로 `status = ACTIVE AND visibility = PUBLIC AND processing_status = READY`다
   (439가 가정한 술어와 일치). 소프트 삭제는 `VideoStatus.DELETED`로 표현되어 같은 술어에
   포함된다. 439의 `videoCount`와 440 피드가 이 술어 하나를 공유하면 된다.
3. **격자 귀속 유일성 (채택, DDL 층으로 확정)**: EXCLUDE 제약이 필요 없다. PRD §8이 영역을
   사각형이 아니라 격자 행 단위(`EventLocationGrid`)로 저장하므로, 회차 id를 비정규화[^6]한
   `UNIQUE (event_occurrence_id, grid_id)` 하나로 잡힌다(재시드 순서 의존을 없애기 위해
   지연 제약이며 커밋 시점 검증이다). 비정규화 값의 정합은 복합 FK가 보장하므로 시드 검증
   층에 의존하지 않는다(시드 아닌 쓰기 경로가 생겨도 제약이 유지된다).
4. **표시 순서 필드 (채택)**: `display_order INTEGER NOT NULL DEFAULT 0`. 439 위치 목록 정렬
   1차 키로 쓰면 된다(동률 타이브레이커 `locationId`는 439 결정 유지).
5. **SRS FR-EVENT-02 "일정과 프로그램 안내" 문구 (스펙 범위 아님 확인)**: 정본 PRD US-002에
   없는 구 PRD 잔재가 맞다. 이 스펙의 어떤 테이블에도 일정, 프로그램 필드를 두지 않으며,
   SRS 문구 보정(해당 구절 삭제)을 이 티켓의 docs 커밋 계획에 포함한다(439가 438 편승을
   제안한 그 항목).

## 계약 변경

없음. `GridQueryService`, `HotZoneService`, `UserGridQueryService`, `UserOidcCommandService`
어느 것도 시그니처가 바뀌지 않는다. 신규 코드는 event 패키지 안에 닫혀 있고, 타 도메인 참조는
`EventVideo`의 `Video` 읽기 전용 연관 하나뿐이다.

## 문서 커밋 계획 (구현과 같은 브랜치)

1. `response-pattern.md` developCode 표에 `13xxx | event` 행 추가 (선행 커밋, 439 게이트 대상).
2. CLAUDE.md 협업 원칙 Owner B 목록에 `com.msg.fillmap.event.*` 추가 (MSG-438 근거 병기).
3. `docs/srs.md` FR-EVENT-02에서 "일정과 프로그램 안내" 구절 삭제(2026-08-20 사용자 승인됨),
   FR-EVENT-07과 08 상태를 구현 완료 시점에 갱신하고 rtm 재생성 (finalize 2-1).

## 테스트 시나리오

한국어 테스트 메서드명 후보. 대표 격자 계산은 순수 함수 단위 테스트, 시더와 DDL 제약은 통합
테스트다(공유 로컬 DB, 실행 전 정리).

대표 격자 (`RepresentativeGridResolver`):

- `홀수_행렬_직사각형_영역의_대표_격자는_정중앙이다` (검증: FR-EVENT-08, PRD 테스트 1)
- `여러_사각형으로_표현된_홀수_직사각형_영역도_정중앙이_대표가_된다` (표현 무관 판정)
- `홀수_직사각형에서_정중앙과_다른_운영자_지정값은_모순으로_거절된다`
- `짝수_행_직사각형은_정중앙_규칙_대상이_아니라_지정값을_쓴다` (9×8 경계)
- `비직사각형_영역에서는_운영자_지정_대표_격자를_쓴다` (PRD 테스트 2)
- `운영자_지정_대표_격자가_영역_밖이면_거절된다` (FR-3)
- `지정이_없으면_영역_중심_최근접_포함_격자가_선택된다` (PRD 테스트 3. 중심이 비포함
  격자(오목 영역)에 떨어지는 케이스 포함, 비교식은 long 정수 산술
  `(n·gy - Sy)² + (n·gx - Sx)²`)
- `중심_최근접_동률이면_남서쪽_격자로_결정된다` (결정성)

시더와 DDL:

- `시드를_두_번_실행해도_결과가_같다` (멱등, 검증: FR-EVENT-08)
- `첫_시드_실값의_기준_위경도가_초안_격자와_일치한다` (GridEncoder.encode 전수 재계산으로
  시드 실값 초안 표의 7개 위치 검증, 어긋나면 시드 값 교정이 선행)
- `영역이_줄어든_재시드는_제거된_격자_행을_지우고_대표_격자를_다시_계산한다`
- `영상이_있는_위치의_대표_격자가_바뀌는_재시드는_실패한다` (드리프트 가드)
- `대표_격자_변경_재시드와_행사_업로드가_경쟁해도_드리프트가_없다` (동시성 통합 테스트,
  시더의 `event_locations` 행 잠금과 440 잠금 계약의 검증. 두 트랜잭션을 겹쳐 실행해 어느
  직렬화 순서에서도 기존 영상의 `grid_id`와 최종 대표 격자의 불일치가 남지 않음을 확인)
- `격자를_다른_위치로_옮기는_재시드가_한_트랜잭션에서_성공한다` (지연 UNIQUE, flush 순서
  무관 검증)
- `기존_회차의_시작_시각을_바꾸는_재시드가_성공하고_visible_from도_함께_이동한다`
  (CHECK 등식과 동시 갱신 계약)
- `한_회차에서_두_위치에_같은_격자를_넣은_시드는_실패한다` (uq_event_grid_per_occ)
- `다른_회차는_같은_격자를_각자_가질_수_있다` (제약 범위가 회차 단위임을 검증,
  검증: FR-EVENT-07)
- `격자_행의_회차와_위치의_회차가_다르면_저장이_거절된다` (복합 FK)
- `영상_연결의_회차와_위치의_회차가_다르면_저장이_거절된다` (복합 FK, 검증: FR-EVENT-07)
- `대표_격자가_영역_격자_행_없이_커밋되면_지연_FK가_거절한다`
- `빈_영역이나_역전된_사각형_시드는_기동이_실패한다` (형식 fail-fast)
- `좌표_범위나_고유_셀_수_상한을_넘는_시드는_기동이_실패한다` (단일 사각형 전개 전 가드와
  합집합 고유 셀 2,500 상한. 겹치는 사각형 표현이 고유 셀 기준으로는 상한 안일 때 통과하는
  케이스 포함)
- `visible_from은_항상_시작_2주_전으로_계산돼_저장된다` (파생 고정, DDL CHECK 포함 검증)
- `시드_시각의_KST_오프셋_표기는_UTC로_저장된다` (MSG-376 체계)
- `오프셋_없는_시드_시각은_파싱_실패로_기동이_멈춘다`
- `시드_파일이_없으면_기동이_실패하고_빈_배열은_정상이다` (ZoneSeeder 선례)
- `파일에서_사라진_회차는_삭제되지_않는다` (아카이브 보존)

파생 상태 (`EventOccurrence`):

- `상태_파생_계산은_반개구간이다` (endsAt과 uploadClosesAt 정각이 각각 UPLOAD_GRACE와
  ARCHIVED에 속하는 4상태 경계, 439와 공유하는 정의의 정본 테스트)
- `업로드_마감_시각은_종료_30일_후로_파생된다`

부수효과 계약 표의 검증(포함 항목의 훅 호출, newBadges 동봉, 점령·롤백 제외, 공개범위 변경
차단, 도감 지표·지역별 목록·커버·썸네일 안티조인, 업로드 측 잠금 계약)은 쓰기 경로 소유자인
MSG-440 테스트 몫이고, 미션 쿼리 안티조인 여섯 쿼리의 검증은 그 후속 구현 티켓 몫이다(위 구현
노트의 배포 게이트 포함). 시더끼리의 동시 실행 직렬화(advisory lock)는 위 동시성 통합
테스트와 별개로, 실환경 재현이 어려워 잠금 획득 호출의 존재를 단위 검증하는 수준으로 남긴다.

## 미해결 질문

행사 업로드의 점령 여부(미생성 확정), 전역 격자 피드 노출(포함 확정), 영상 길이 재료
(`videos.duration_sec` 실재로 해소, 30초 제한 동일 확정), `visibleFrom` 정책(시작 2주 전
파생 고정 확정), 첫 시드 행사 선택(2026-08-20 사용자가 3건 확정: 부산국제영화제,
부산불꽃축제, 서울세계불꽃축제)은 모두 종결됐다. 스트릭, 핫스코어, 업로드 수 뱃지 포함은
글로서리 정본에서 도출한 기본값이다(정본 준수 기본값, 사용자 번복 가능). 남은 것 한 건:

1. **시드 초안값의 세부 확정**: 격자 인덱스는 앵커 보간 초안이라 구현 단계의
   `GridEncoder.encode` 전수 재계산으로 확정한다(테스트
   `첫_시드_실값의_기준_위경도가_초안_격자와_일치한다`가 게이트). 시작·종료 시각(시 단위)과
   영역 사각형 크기도 제안값이라 사용자가 조정할 수 있다. 어느 쪽도 구현 착수를 막지 않는다
   (시드 행 값 교정만으로 끝난다).

## 작업 로그

### 2026-08-20 구현 완료 (spec-driven-dev: auth-dev + convention-reviewer)

- 모듈 순서: 마이그레이션과 엔티티 → 리포지토리 → 대표 격자 결정 → 시더와 첫 시드 → 통합
  테스트. 신규 테스트 45건(대표 격자 11, 파생 상태 3, 시더와 DDL 26, 드리프트 가드 4,
  기동 진입점 1), 전체 스위트 green.
- 검증 방식에서 상태 비교만으로는 못 잡는 성질 네 가지를 따로 고정했다. 넷 다 판정 근거를
  DB 쪽에 두어 애플리케이션 계층 계측이 못 보는 경로까지 닫았다.
  - **멱등**: 결과 비교가 아니라 2회차 실행 구간의 행 변경 수 0으로 본다.
    `pg_stat_xact_user_tables`의 `n_tup_ins + n_tup_upd + n_tup_del`이라 JPA든 native든
    벌크든 쓰기 경로와 무관하게 잡히고, 값이 같아지는 UPDATE도 행을 건드리면 세므로
    "결과가 같다"와 "쓰지 않았다"가 구분된다.
  - **지연 제약**: 재시드 성공만으로는 flush 순서에 따라 즉시 제약으로도 통과할 수 있다.
    시더가 다음 위치를 처리하며 내는 조회가 auto-flush를 일으켜 삭제가 먼저 나갈 수 있기
    때문이다. `pg_constraint`의 `condeferrable`과 `condeferred`를 직접 단언해 DDL 속성을
    고정하고, 입력 순서를 뒤집은 재시드 케이스를 함께 둔다.
  - **기동 진입점**: 프록시를 거친 `run()` 호출로 후반 실패 시 전량 롤백을 본다.
  - **동시성**: 시간 대기가 아니라 `pg_blocking_pids(상대 pid)`에 이 세션의 pid가 들어올
    때까지 기다린 뒤 커밋한다. 상대 스레드는 자기 백엔드 pid를 잠금 대기 전에 알린다.
    전역 미허용 잠금 수를 세면 무관한 세션의 대기나 다른 잠금 대기를 경쟁 지점 도달로
    오인하는데, 이 술어는 "내가 잡은 잠금이 상대를 세우고 있다"까지 확정한다.
- 시드 실값 확정: 기준 위경도 7개를 `GridEncoder.encode`로 전수 재계산한 결과 6개는 초안과
  같았고 이기대만 `16830_11479` → `16830_11478`로 한 칸 어긋나, 시드 사각형의 열 범위를
  서쪽으로 한 칸 옮겼다(11478~11480 → 11477~11479). 미해결 질문 1번이 이것으로 종결이다.

#### 데이터 저장 위치

- 테이블 5종을 `V39__event_room_schema.sql`이 만든다: `event_series`, `event_occurrences`,
  `event_locations`, `event_location_grids`, `event_videos`. 인덱스 신규는
  `idx_event_videos_location` 하나이고, 나머지 접근은 PK와 자연키 UNIQUE를 탄다.
- 첫 시드 실값은 `src/main/resources/seed/events.json`(시리즈 3, 회차 3, 위치 7)이다. 적재
  게이트는 `fillmap.event.seed.enabled`이고 코드 기본값 false, dev와 prod yml에서만 true다
  (로컬과 테스트는 영향 없음. 테스트는 시더를 직접 생성해 돌린다).
- Redis와 S3 사용 없음. 회차 상태와 업로드 마감은 저장 컬럼이 아니라 파생 계산이라 저장
  위치 자체가 없다.

#### 실행 쿼리 (실측 SQL, `logging.level.org.hibernate.SQL=DEBUG`)

시더 트랜잭션 서두의 직렬화 잠금 (native, PostgreSQL 전용):

```sql
SELECT pg_advisory_xact_lock(hashtextextended('event_seed', 0))
```

대표 격자를 다시 계산하기 전에 위치 행을 잠그는 파생 쿼리
(`findWithLockByLocationKey`, `@Lock(PESSIMISTIC_WRITE)`):

```sql
select el1_0.id, el1_0.display_order, el1_0.location_key, el1_0.name, el1_0.event_occurrence_id,
       el1_0.operating_hours, el1_0.representative_grid_id, el1_0.type
from event_locations el1_0
where el1_0.location_key=?
for no key update of el1_0
```

**실측에서 드러난 사실 하나**: Hibernate 6의 PostgreSQL 방언은 `PESSIMISTIC_WRITE`를
`FOR UPDATE`가 아니라 **`FOR NO KEY UPDATE`**로 낸다. 필요한 직렬화는 그대로 성립한다.
`FOR NO KEY UPDATE`끼리, 그리고 같은 행의 UPDATE와 서로 막는다. 오히려 다른 테이블의 외래키
검사가 잡는 `FOR KEY SHARE`와는 충돌하지 않아 불필요한 대기가 없다. **MSG-440은 같은 파생
쿼리를 써야 한다.** 잠금 모드가 다르면 서로 안 막힐 수 있다.

드리프트 가드가 보는 영상 존재 여부(`existsByLocationId`)와 격자 영역 동기화:

```sql
select ev1_0.video_id from event_videos ev1_0 where ev1_0.event_location_id=? fetch first ? rows only
select elg1_0.event_location_id, elg1_0.grid_id, elg1_0.event_occurrence_id
  from event_location_grids elg1_0 where elg1_0.event_location_id=?
delete from event_location_grids where event_location_id=? and grid_id=?
insert into event_location_grids (event_occurrence_id, event_location_id, grid_id) values (?, ?, ?)
```

격자 동기화는 파일과 완전 일치가 될 때까지 없어진 행만 지우고 새 행만 넣는다. 전량 삭제 후
재삽입이 아니라서, 안 바뀐 칸은 쿼리가 나가지 않는다. 시리즈, 회차, 위치는 자연키로 조회한
뒤 갱신하거나 새로 넣는 것이 전부고 native UPSERT를 쓰지 않는다.

#### 예외 흐름

- 이 티켓에는 런타임 API가 없다. 그래서 `EventErrorCode`도 `ApiException`도 만들지 않았고
  `GlobalExceptionHandler`가 변환할 응답 자체가 없다(첫 사용자인 MSG-439가 만든다).
- 시드 데이터의 문제는 전부 `IllegalStateException`으로 던진다: 파일 없음, 오프셋 없는 시각,
  빈 영역, 역전된 사각형, 허용 범위 밖 인덱스, 고유 셀 수 상한 초과, 홀수 직사각형과 어긋난
  지정값, 영역 밖 지정값, 영상이 붙은 위치의 대표 격자 변경. 메시지에는 어느 자연키가
  문제인지와 기대값을 함께 담는다.
- 이 예외는 `ApplicationRunner`가 던지는 것이라 스프링이 컨텍스트를 닫고 기동을 실패시킨다.
  잘못된 시드로 뜬 서버가 조용히 돌아가는 상태가 없다는 뜻이다.
- DDL이 잡는 위반은 예외가 두 갈래다. 즉시 제약(복합 FK 2종, CHECK 4종)은 그 문장에서
  바로 터지고, 지연 제약 2종은 커밋 시점에 터진다. 어느 쪽이든 시더 트랜잭션 전체가
  롤백돼 절반만 반영된 상태가 남지 않는다.

#### 빈 동작

- `EventSeeder`는 `ApplicationRunner` 구현이라 기동 완료 직후 한 번 돈다. `@Order(30)`은
  Region(10)과 Zone(20) 다음이라는 뜻이고, 이 시더는 그 둘과 FK 의존이 없어 순서 자체가
  필요하지는 않다. 결정성을 위해 자리를 고정했다.
- 게이트는 메서드 첫 줄이다. `enabled`가 false면 시드 파일을 읽지도 않으므로, 파일이 없거나
  깨져 있어도 로컬과 테스트 기동에 영향이 없다.
- `run`이 `@Transactional` 하나로 전체를 감싼다. 시리즈부터 마지막 격자 행까지 한 경계 안에
  있어서 지연 제약 2종(회차 내 격자 유일, 대표 격자 영역 소속)이 그 커밋 한 번에 함께
  검증된다. 격자를 위치 A에서 B로 옮기는 재시드가 성립하는 것도 이 덕분이다. Hibernate는
  insert를 delete보다 먼저 flush하는데, 지연 제약이라 중간 상태가 문제되지 않는다.
- 테스트는 대부분 시더를 스프링 빈으로 받지 않고 직접 생성해 트랜잭션을 테스트가 연다
  (ZoneSeeder 선례). 롤백 격리가 되는 대신 커밋이 없으므로, 지연 제약을 봐야 하는 테스트는
  `SET CONSTRAINTS ALL IMMEDIATE`로 검증 시점을 트랜잭션 안으로 당긴다. 다만 이 방식은
  `run()`의 트랜잭션 경계 자체를 검증하지 못하므로(테스트가 경계를 대신 열어 준다),
  `EventSeederRunnerTest` 하나만 프록시를 거친 빈의 `run()`을 트랜잭션 없이 호출한다.
  시드 후반이 깨질 때 앞서 쓴 시리즈와 회차, 위치가 한 행도 남지 않는 것이 그 경계의 증거다.
- 동시성 통합 테스트와 위 기동 테스트만 실제로 커밋한다(`NOT_SUPPORTED` +
  `TransactionTemplate`, 합성 키 명시 정리 동반). 잠금 계약과 트랜잭션 경계는 뮤테이션으로
  확인했다. `findWithLockByLocationKey`를 잠금 없는 조회로 바꾸면 동시성 테스트 두 건이,
  `run()`의 `@Transactional`을 떼면 기동 테스트가 실패한다.
- 시더에 `Clock`을 주입하지 않았다. 현재 시각을 쓰는 지점이 없기 때문이다(`visible_from`은
  `startsAt - 14일` 파생이고 V39에 생성 시각 컬럼이 없다). 인자 없는 `now()`도 없다.

---

[^1]: 대표 격자: 행사 위치의 영역 격자 중 영상 레코드가 실제로 연결되는 단 하나의 격자.
    영역 전체에 영상을 복제하지 않기 위한 장치다 (FR-EVENT-08).
[^2]: 멱등(idempotent): 같은 작업을 여러 번 실행해도 한 번 실행한 것과 결과가 같은 성질.
    기동마다 도는 시더가 이 성질을 가져야 재시작이 안전하다.
[^3]: EPSG:5179: 한국 국토 평면 좌표계. 미터 단위 평면이라 좌표를 100으로 나누면 100m 격자
    인덱스가 나오고, 인덱스 공간의 거리 비교가 실거리 비교와 비례해 동치다.
[^4]: 반개구간: 시작점은 포함하고 끝점은 제외하는 구간 표기. 경계 정각이 정확히 한 상태에만
    속하게 만들어 상태 판정의 빈틈과 겹침을 없앤다.
[^5]: 소프트 삭제(soft delete): 행을 지우는 대신 상태 값(여기서는 `VideoStatus.DELETED`)으로
    삭제를 표시하는 방식. 참조 무결성과 이력을 보존하면서 노출만 끊는다.
[^6]: 비정규화: 다른 테이블에서 조회로 얻을 수 있는 값을 성능이나 제약 표현을 위해 중복
    저장하는 설계. 여기서는 격자 행에 회차 id를 중복 저장해 "회차 내 격자 유일" 제약을 단일
    UNIQUE로 표현했고, 중복 값의 정합은 복합 FK가 지킨다.
[^7]: 안티조인(anti join): 다른 테이블에 대응 행이 **없는** 행만 남기는 조회 방식. SQL로는
    `NOT EXISTS (SELECT 1 FROM event_videos ev WHERE ev.video_id = v.id)` 형태이며, 여기서는
    "행사 영상이 아닌 videos만 센다"를 표현한다.

# SRS 변경 이력

`docs/srs.md`의 요구사항이 언제 왜 바뀌었는지를 시간순으로 남긴다. 본문에서 떼어낸 이유는
병합 충돌 때문이다. 모든 SRS 작업이 이 표 끝에 행을 하나씩 붙이는데, 브랜치 둘이 같은 자리에
쓰면 매번 충돌한다. 이 파일은 `.gitattributes`에서 union 병합으로 두어 양쪽 행이 모두 남는다.

**추가만 한다.** 기존 행을 고치거나 지우지 않는다. union 병합은 수정을 중복 행으로 남기므로,
이미 적은 이력이 틀렸으면 지우지 말고 정정하는 행을 새로 붙인다.

**GitHub에서 충돌로 뜨면 로컬에서 푼다.** union 병합은 로컬 git만 적용한다. GitHub의 서버 머지는
`.gitattributes`의 merge driver를 읽지 않아서, 브랜치 둘이 이 표에 행을 붙이면 PR 화면에는 매번
충돌로 나온다. 그때는 웹에서 손으로 합치지 말고 **로컬에서 `git merge origin/develop` 후 푸시**한다.
union이 붙어 양쪽 행이 그대로 남는다. 2026-08-14에 같은 자리에서 두 번 걸렸다.

| 날짜 | 변경 | 사유 |
|------|------|------|
| 2026-08-10 | 초판. 기존 구현 역산으로 기능 요구 17영역 209건, 비기능 27건 등재. 위키 대조로 지도 홈 표현 정책(MAP) 신설, 미해결 8건과 정본 불일치 6건 식별 | MSG-358 |
| 2026-08-10 | 문서 구조를 ISO/IEC/IEEE 29148(구 IEEE 830) 표준 목차로 재편. 서론, 전체 설명(제품 관점·기능 요약·사용자 특성·제약·가정과 의존성), 외부 인터페이스, 아키텍처, 추적성 매트릭스, 부록 추가. 용어집과 아키텍처는 기존 정본을 링크로 대체 | MSG-358, 성민 지적 |
| 2026-08-10 | FR-BADGE-12(미션 뱃지 종류별 지급)·FR-BADGE-13(뱃지 그림) 등재. FR-MISSION-07에 대체 예정 표시 | `docs/prd/MSG-363-prd.md` 승인 |
| 2026-08-10 | 구현 반영. FR-BADGE-12를 구현됨으로, FR-MISSION-07을 폐기됨으로(FR-BADGE-12가 대체) 바꾸고 FR-BADGE-01의 활성 축 목록을 종류별 3축으로 정정. FR-BADGE-13은 그림 형식·버킷 미확정이라 계획 유지. FR-VIDEO-15에 공개범위 기본값 정정 근거 추가(요구·동작 불변) | MSG-363 구현(V29·V30) |
| 2026-08-10 | 확정 반영과 근거 정정. FR-MISSION-09의 스팟 5~8곳·완료 3곳을 시작안에서 확정으로 명시하고, FR-VIDEO-01에 30초 여유 허용 확정을 반영(FR-MEDIA-03에 실측 컷 충돌 표시, 허용 폭은 미해결 질문으로 등재). FR-GRID-04 근거의 MSG-193을 MSG-93으로 정정. FR-AUTH-11 상태를 구현됨에서 미충족으로 조정해 NFR-SEC-07과 맞췄다 | 2026-08-10 성민 확인, 코드 재확인 |
| 2026-08-10 | 미해결 정리. FR-AUTH-12(iOS 출시 시 애플 로그인) 신설로 미해결 질문의 Apple 항목을 닫고, FR-MISSION-13에 종류별 반경 방식 확정과 길찾기 버튼 미도입을 반영. 미해결 표의 미션 노출 항목은 방식이 정해졌으므로 값만 남기는 형태로 좁혔다 | 2026-08-10 성민 확정, MSG-368·MSG-369 발행 |
| 2026-08-11 | FR-VIDEO-18 상태를 진행 중에서 구현됨으로 (PR 머지 반영) | MSG-371 |
| 2026-08-11 | FR-GRID-13(축소 뷰포트 행정 단위 집계) 등재. 구현·테스트가 이미 있는데 FR 행이 없던 역방향 갭 해소. 8장에 테스트 비대상 목록 신설(7건, 사유 명기)하고 rtm 생성 스크립트가 이 목록을 읽어 검증 공백에서 분리하게 함 | MSG-375 (첫 전수 매핑 후속) |
| 2026-08-13 | NFR-OPS-08(영상 종결 지표는 같은 사건을 한 번만 집계) 등재. MSG-343 계측이 SRS 미등재로 들어와 지표 정확성 요구가 빠져 있던 것을 소급 등재 — 그 부재 속에 중복 집계 결함이 함께 들어온 것이 등재 계기 | MSG-382 (Codex 리뷰 적발 후속) |
| 2026-08-13 | NFR-OPS-08 문장을 "같은 사건 한 번만 집계"에서 "종결 후 재도착 신호 차단"으로 좁힘. 지표 증가가 전이 커밋 앞이라 남는 커밋 실패 시 과대 집계는 MSG-343이 수용한 근사 오차라, 넓은 문장이 이를 요구 위반으로 만드는 것을 막는 범위 한정 | MSG-382 후속 (PR #161 리뷰 반영) |
| 2026-08-13 | 미션 지도 탐색 개편 반영. FR-MISSION-14~19 등재(목록 항목 구성, 마커·클러스터 표시와 선택 시 판정 범위, 미션 상세, 미션 영역 영상, 진행도, 이름 없는 경유점 폴백)와 NFR-DATA-07 등재(외부 이미지 보관·형식 통일). FR-MISSION-08은 축제 소스를 TourAPI로, FR-MISSION-10은 팝업 판정 범위를 반경 40m로 바꿔 진행 중으로 조정. FR-MISSION-02에 진행도 분리 조회 단서, FR-MISSION-13에 목록 위치가 플랫폼별로 다르다는 단서 추가. 미해결에 외부 데이터 출처 표기 조건 등재 | `docs/prd/mission-map-explore.md` (2026-08-13 성민 승인) |
| 2026-08-13 | FR-MAP-10(지도 홈 기본 화면 사이드 패널 격자 카드 최신순 최대 20장) 등재. 클라이언트가 기존 행정동 격자 카드 조회의 정렬·개수 파라미터로 판정하는 화면 규칙이라 서버 변경 없음. FE 연동 가이드의 "카드 3장·조회수순" 안내를 대체 | MSG-387, 2026-08-13 정민 확정 |
| 2026-08-13 | FR-MAP-10에 카드 주인 확정 반영(기본 화면 카드도 내 격자, 20장 스크롤 디자인 확인). FR-MAP-07 근거에 카드 목록 포함 확인을 추가. MSG-374가 남긴 카드 기준 섞임의 결정이 닫혔고, 내 것 기준 카드 조회는 서버에 없어 후속 티켓 전까지 전역 조회를 임시 사용 | MSG-387 PRD 승인 (2026-08-13 정민 답변 2건) |
| 2026-08-13 | FR-MAP-09(칩 활성 격자 선택의 활발한 시간대 차트) 등재. 노출 위치가 지도 홈 기본 패널에서 칩 활성 한정으로 좁혀진 확정과 함께, 집계 윈도우 전체 누적·24구간 고정 응답을 확정값으로 담았다 | MSG-372, 2026-08-13 정민 확정 |
| 2026-08-13 | NFR-PERF-01 적용 범위를 격자 시간대 분포 조회까지 확장. 새 조회가 뷰포트 한정 SLO를 말없이 상속하던 추적성 공백을 닫았다(신선도 30초는 뷰포트 한정 명시). 측정 방법은 NFR-PERF-02 그대로 | MSG-372 (Codex 리뷰 후속) |
| 2026-08-13 | FR-MAP-09 상태를 구현됨으로 갱신하고 시간대 집계·24구간 조립·HTTP 계약 테스트를 연결했다 | MSG-372 구현 및 Codex 리뷰 후속 |
| 2026-08-14 | FR-MISSION-13에서 칩 개수 배지와 "0이면 칩 숨김"을 폐기했다. 지도를 움직일 때마다 종류 넷의 개수를 다시 세어 칩 상태를 맞춰야 하는데 그 상태 관리 비용이 배지의 값어치보다 크다고 보았다. 칩은 종류를 고르는 역할만 하고 항상 표시된다. 같은 행의 구현 티켓 참조도 MSG-368에서 MSG-398로 옮겼다(368은 문서 등재만 하고 닫혔다) | 2026-08-14 성민 확정, MSG-398 |
| 2026-08-14 | FR-MISSION-17을 계획에서 진행 중으로 옮기고 후보 술어·페이징·HTTP 계약 테스트를 연결했다. 영상 목록 조회는 들어갔지만 총 영상 개수와 코스 스팟별 방문 여부·영상 수가 남아 구현됨으로 올리지 않았다. 그 둘은 미션 상세 조회(MSG-399) 몫으로 갈렸고, 개수 쿼리가 목록의 후보 술어와 술어 동등이어야 한다는 조건이 붙는다. **상태 변경이 머지(PR #168) 뒤로 밀린 것은 SRS 갱신을 마무리 절차에 넣지 않아서다** — 그 사이 rtm이 구현된 요구를 계획으로 싣고 있었다 | MSG-390 구현 후속 |
| 2026-08-14 | FR-MISSION-10의 포스터 요구를 충족으로 갱신했다. 팝가 상세의 `og:image` 원본을 JPEG로 변환해 우리 버킷에 두고 dev 활성 팝업 227건 전부에 포스터와 소개문이 찼다. 소개문 출처가 같은 페이지의 `content`로 확정돼 MSG-383이 남긴 미해결도 닫혔다. 반경 40m 축소가 MSG-385로 남아 행 상태는 진행 중을 유지한다 | MSG-394 |
| 2026-08-14 | NFR-DATA-07 적용 범위를 축제 한정에서 미션 세 종류로 넓혔다. 팝업 포스터와 코스 대표 사진까지 같은 규칙(JPEG 통일·우리 버킷 URL만 저장·리더 검증)으로 미러링한다. 객체 이름 문법 검증을 더해 URL 구분자로 래스터 제한을 우회하는 경로를 막았다. prod 버킷 정책이 남아 진행 중을 유지한다 | MSG-394 |
| 2026-08-14 | FR-MISSION-08의 실적재 상태를 갱신했다. 축제 dev 적재가 끝나 활성 222건에 이미지 221건이 찼고, 남은 것은 prod다. 요구 문장과 상태는 그대로다 | MSG-394 (MSG-384 후속 확인) |
| 2026-08-15 | FR-MAP-06에서 사이드 패널 헤더의 "이 지역 격자 N개 · 영상 M개" 카운트 줄을 폐기했다. 헤더는 동 이름만 표시한다(웹 ver 13 지도 홈 실측과 일치). 집계 응답의 동 전체 내 격자 수·영상 수 필드는 서버 계약으로 유지하되 화면이 소비하지 않으며, 필드 제거 여부는 별도 논의로 남긴다. FR-MAP-07의 예시 문장에서도 헤더 카운트를 뺐다(내 것 기준 원칙 자체는 불변) | 2026-08-15 확정, MSG-388 디자인 검증 중 발견 |

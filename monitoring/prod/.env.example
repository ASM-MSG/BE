# MSG-344 상시 관측 스택 환경변수. 이 파일을 .env 로 복사해 실제 값을 채운다 (.env 는 gitignore).
# cp monitoring/prod/.env.example monitoring/prod/.env

# Slack incoming webhook URL — 성민이 Slack 에서 발급 (#dev-monitoring 예정)
SLACK_WEBHOOK_URL=

# Grafana admin 로그인 비밀번호
GRAFANA_ADMIN_PASSWORD=

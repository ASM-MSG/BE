/gradlew text eol=lf
*.bat text eol=crlf
*.jar binary

# SRS 변경 이력은 추가만 하는 표라 양쪽 행을 모두 남긴다 (같은 자리 append 충돌 방지)
docs/srs-changelog.md merge=union

# 하네스 변경 이력도 같은 append-only 표 (MSG-531)
.claude/CLAUDE-changelog.md merge=union

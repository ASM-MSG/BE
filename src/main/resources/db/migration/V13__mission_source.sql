-- ============================================================================
-- V13: missions.source — 적재 출처 컬럼 (MSG-224 D7, Codex 리뷰 파생)
--
-- EVENT 타입은 축제(MSG-224)·팝업(MSG-235)이 공유라 타입만으로는 러너가 "자기 산출물"을
-- 식별할 수 없다 — 종료 정리가 팝업을 오삭제하고, dedupe 중심 복원(min+4)이 1격자 팝업에서
-- 가짜 키를 만든다. source 로 러너별 소유권을 구분한다 (2026-07-31 성민 확정).
--
-- NULL = 수동/미상 적재 → 모든 러너의 정리·dedupe 대상에서 제외 (안전 기본값).
-- CHECK 없음: 소스 추가(MSG-225 코스·MSG-235 팝업)마다 마이그레이션을 강제하지 않기 위해.
-- backfill 없음: 러너 도입 전 데이터는 전부 수동이므로 NULL 이 맞다.
-- ============================================================================
ALTER TABLE missions ADD COLUMN source VARCHAR(30);

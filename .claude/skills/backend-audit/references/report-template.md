# 보고서 템플릿 — docs/audit/{YYYY-MM-DD}-backend-audit.md

아래 절 순서를 그대로 쓴다. 없는 절은 "해당 없음"으로 남긴다 — 절이 빠지면 "안 본 것"과
"없는 것"이 구분되지 않는다.

```markdown
# 백엔드 감사 {YYYY-MM-DD}

- 기준 커밋: `{short-sha}` ({branch})
- 렌즈: {돌린 렌즈 ID 나열 / 전체}
- 실측 환경: 로컬 postgis 컨테이너 (행 수 적음 — 플랜은 참고치) / 미실측 렌즈: {…}
- 발단: 2026-09-07 박원형 멘토링 후속 (다음 멘토링 10-08). 이전 보고서: {경로 또는 없음}
- **이 문서는 결함 목록이 아니라 검토 목록이다.** 판단 칸의 "추측:"은 확인 안 된 해석이다.

## 1. 요약

| 렌즈 | 확인 항목 | P1 | P2 | P3 | 한 줄 결론 |
|---|---|---|---|---|---|

가장 먼저 검토할 것 최대 3개 (우선순위와 영향 범위 순, P1이 없으면 없다고 쓴다):
1. …

## 2. 파일별 병합 항목 (여러 렌즈에 걸친 것)

| 파일 | 렌즈 | 관찰 | 근거 | 판단 | 우선순위 |
|---|---|---|---|---|---|

## 3. 렌즈별 발견

### 3.1 native — 네이티브 쿼리 분류
(요약 → 표 → 규칙으로 차단됨 → 한계)
### 3.2 plan — 실제 SQL·실행 계획
### 3.3 tx — 트랜잭션 업무량·책임 범위
- 범위: 선언 {n}개 / 적용 대상 메서드 {n}개 / 템플릿 실행 구간 {n}개 / 검토 {n}개 / 미확인 {n}개. 최상위 호출자와 공유 흐름을 구분하며 이 합계를 물리 TX 개수로 쓰지 않는다.
- 외부 I/O가 없는 조회·쓰기·배치도 검토한다. 소스 호출 수와 실제 SQL 횟수·연결 점유 시간은 구분한다.

| 흐름 ID·진입점(파일:줄) | 수행 업무(실행 순서) | 저장소·SQL(정적/실측) | 반복 상한·잠금·I/O | 같은 TX 근거 | 분리 후보·필요 조건 | 판단·우선순위 |
|---|---|---|---|---|---|---|

<details>
<summary>전체 트랜잭션 진입점 검토 범위</summary>

| 진입점(파일:줄) | 실제 TX 경계 | 흐름 ID 또는 유지 근거 | 상태(검토/공유/미확인) |
|---|---|---|---|

</details>

### 3.4 osiv
### 3.5 tz — 시간대
### 3.6 calls — 호출 구조·진입점
### 3.7 naming
### 3.8 iface — 인터페이스 분리

## 4. 팀 설명 자료 — "왜 이렇게 했나"

| 핵심 흐름 | 진입점 | 결정 문서 | 비고 |
|---|---|---|---|
| 영상 저장 | `VideoServiceImpl.…` | docs/spec/MSG-… §… | |
| 격자 뷰포트 조회 | | | |
| 이벤트 영상 목록·집계 | | | |
| 뱃지 지급 | | | |

근거 문서가 없는 흐름: {…} — 팀 코드 리뷰 시간에 담당자가 설명하고 스펙 작업 로그에 남길 대상.

## 5. 이전 감사 대비
해소 {n} · 신규 {n} · 유지 {n} (첫 감사면 "해당 없음"). 기준 변경에 따른 재분류는 별도이며 코드 수정으로 세지 않는다.

## 6. 후속 후보 (티켓 아님 — 사람이 결정)
- …

## 7. 한계
- 미실측 항목, 렌즈당 25개 상한으로 접은 패턴, 오판 가능성.
```

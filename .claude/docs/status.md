# Implementation Status — 지금 코드에 실제로 있는 것

> **이 문서가 구현 현황의 단일 진실 원천이다.** `infrastructure.md`·`architecture.md`는
> **목표 설계**를 present tense로 서술한다 — 거기 나온 패키지·서비스·인터페이스가 코드에
> 존재한다고 가정하지 말 것. 여기서 ✅로 표시된 것만 실제로 import·호출할 수 있다.
>
> 상태 기준: `develop` 최신 (머지되는 PR이 자기 티켓 줄을 함께 갱신하므로 별도 날짜 관리 없음).

## 도메인 패키지

패키지별 섹션 + **티켓당 한 줄 불릿**. 새 티켓은 해당 패키지 끝에 자기 줄 하나만 append한다
(한 줄에 여러 티켓 조각을 잇지 말 것 — 병렬 PR의 같은 줄 충돌 방지, MSG-169).

### 헤더의 두 표기는 서로 다른 축이다 (MSG-337)

- **Owner A/B** = **도메인 분담 라벨**. 두 곳에서 쓰인다. ① 계약 인터페이스 경계 판정 —
  본문의 "read 계약 A→B", "B-내부 read", "크로스오너" 서술이 전부 이 축이고 CLAUDE.md
  "두 도메인의 접점은 인터페이스로만" 원칙의 근거다. ② spec-driven-dev의 개발 에이전트 배정
  (A→`grid-dev`, B→`auth-dev`, 공동→둘 다). **이 두 용도에는 이 라벨이 맞는 값이다.**
- **구현 XXX** = 그 패키지를 실제로 짠 **주 구현자**(git 커밋·라인 기여 기준, 한쪽이 뚜렷하지
  않으면 "공동"). 사람 이력이라 사실 기록일 뿐이고, 위 두 판정에는 쓰지 않는다.

`zone`의 Owner A 배정 근거는 `docs/spec/MSG-234.md`(§Owner 판정 — 산출물이 전부 지도 인프라 안)이고
`docs/spec/MSG-259.md`가 확정했다. CLAUDE.md 협업 원칙의 패키지 목록에 이 패키지가 빠져 있었는데,
미배정이 아니라 목록 누락이라 2026-08-07에 채웠다.

**둘은 자주 어긋난다.** 실제로 Owner B 도메인(friend·moderation·badge·streak·usergrid) 대부분을
강정민이, Owner A 도메인인 hotzone을 성민이 구현했다. 어긋남은 오류가 아니라 분담과 실제 작업
배분이 다르게 흘렀다는 사실이다. 구현자의 정본은 git 이력과 지라 담당자이고 (2026-08-07 전수
대조에서 둘은 100% 일치했다), 이 헤더는 그 요약이다.

### `response` (구현 강정민) — ✅ 완성
- `ApiResponseDto`, `SuccessResponse`, `ErrorCode`, `ErrorCodeIfs`
- MSG-311: 공통 응답 실데이터 키 `body` → `data` rename (필드명 자체 변경 — @JsonProperty 미사용, 테스트 17파일 190곳 치환. **breaking — FE 동시 배포 전제**)

### `global` (구현 공동) — ✅ 완성
- `ApiException`, `GlobalExceptionHandler`, `config/SecurityConfig`, `config/S3Config`(S3Presigner 빈)·`config/AwsProperties` (MSG-64)
- MSG-167: `GlobalExceptionHandler`에 `MissingServletRequestParameterException → 400 BAD_REQUEST` 전역 매핑 (필수 파라미터 누락이 catch-all에 삼켜져 500이던 결함 정정)
- MSG-244: `config/ProdRedisPasswordValidator`(prod 프로파일 전용 기동 검증 — 바인더가 미해석 `${REDIS_PASSWORD}`를 리터럴로 통과시키는 결함 보완, 공백/미해석 리터럴 완전 일치 시 기동 실패) + prod Redis 포트 6380·헬스체크 호스트 보간 정합(application-prod.yml·docker-compose.server.yml)
- MSG-260: `config/ProdRequiredEnvValidator`(prod 필수 env 8종 일괄 기동 검증 — 공백/미해석 리터럴 완전 일치 시 누락 변수명 전부 나열하며 기동 실패, `ProdRedisPasswordValidator`는 흡수·삭제)
- MSG-342: catch-all `handleException`이 ERROR 로그 1줄(HTTP 방법·URI·developCode·전체 스택)을 남김 — 쿼리 문자열·본문·헤더는 배제. 검색 실패 warn 2곳(카카오·집계)은 검색어가 예외 메시지·응답 본문·URL로 되돌아오는 경로까지 막아 클래스명·상태 코드·userId만 남김. yml 로그 레벨 경로 오타(`com.fillmap`) 3곳 정정 — dev·local에서 앱 DEBUG 로그가 실제로 나오기 시작
- MSG-376: `config/UtcLocalDateTimeJsonCodec`(`@JacksonComponent` — 부트 4.1에서 `@JsonComponent` 개명, Jackson 3 `ValueSerializer`/`ValueDeserializer`) — 전 API 응답 `LocalDateTime`을 UTC `Z` 표기로 직렬화(변환 없이 표기만, FR-3), 요청은 오프셋 있으면 UTC 환산·없으면 UTC 간주(FR-4/5). 신규 DTO는 `LocalDateTime`을 쓰는 순간 자동 준수 — `DtoTimeTypeGuardTest`(classpath 스캔)가 시각 타입 이탈 차단. 부수: 인자 없는 `LocalDateTime.now()` 4곳 UTC 고정(Report·Friendship·Video·AiBlurPoller — Codex 적발), 컨벤션 시각 절 신설. DB TZ 혼재는 MSG-379가 종결
- MSG-379: 시각 저장의 실행 환경 의존 잔재 정리 — 컨테이너 TZ 3곳 UTC 전환(compose 파일, 서버 반영은 수동 런북 — CD가 compose 미배포), `bootRun`에 `-Duser.timezone=UTC` 기본, **V33** 20컬럼 `DEFAULT`를 `(statement_timestamp() AT TIME ZONE 'utc')`로 존 무관화(리포지토리 무수정 — DEFAULT 위임 INSERT 6경로가 한 번에 닫힘), `PushTokenRepository` 맨 `now()` 교체(src/main 마지막 세션 존 의존 쓰기), `UserGrid`·`UserBadge` 휴면 `@CreationTimestamp` 제거, `AiBlurPoller` Clock 주입(테스트 고정 클럭 전량 전환). 검증: `TimestampZoneIndependenceIntegrationTest`가 KST 세션 강제 하 존 무관 저장 확인. 잔여: dev DB KST 시딩분 보정(승인 게이트)·서버 컨테이너 재생성 런북 실행

### `auth` (Owner B · 구현 공동) — ✅ 완성
- 기본 골격: `controller`(+`/reissue`), `service`(AuthService·OidcLoginService·RefreshTokenService), `dto`(+Reissue*), `jwt`(TokenProvider·필터·JwtProperties·RefreshTokenProvider/Store·RedisInvalidatedTokenStore), `oidc`(Kakao OIDC), `support/RefreshTokenCookies`, `exception/AuthErrorCode`
- MSG-135: 리프레시 토큰(디바이스별 Redis `refresh:{userId}:{deviceId}`, 2주 슬라이딩, 로테이션+재사용감지)·블랙리스트 Redis 이관·하이브리드 전송(웹 쿠키/앱 body)
- MSG-178: logout에 `LogoutRequestDto`(fcmToken 선택, `@RequestBody(required = false)` — body 없는 기존 호출 하위 호환) — 세션 삭제와 같은 처리에서 `PushTokenService.unregister(userId, fcmToken)` 호출(auth → notification 단방향 주입, 공유 기기 알림 잔존 P1 차단)
- MSG-195: `jwt/CustomAccessDeniedHandler` 신설(403 공통 응답 형식 — EntryPoint 미러) + SecurityConfig `/api/admin/**` `hasRole("ADMIN")` matcher — **코드베이스 최초 role 인가 지점**(배관은 V1부터 완비돼 있었고 검사 지점만 신설)
- MSG-345: 웹 카카오 인가 코드 교환 — `GET /api/auth/oauth/kakao/authorize`(로그인 진입점: redirectUri 필수·state 패스스루, 인가 URL을 서버가 조립(client_id·scope=openid·nonce)해 302 + `OAUTH_NONCE` HttpOnly 쿠키 600s. `support/NonceCookies`가 `oauth.kakao.nonce-cookie-secure`로 분기: true=`Secure;SameSite=None`·false=`SameSite=Lax`, local은 example부터 false. `SuccessResponse` 규칙의 유일한 302 예외) + `POST /api/auth/oauth/kakao/code`(`KakaoCodeLoginRequestDto{code,redirectUri}`, 쿠키 nonce ↔ id_token 클레임 대조 후 기존 `OidcLoginService.login` 무수정 재사용). `oidc/KakaoAuthCodeExchanger`(완성 RestClient 빈 2호 `kakaoTokenRestClient` — **by-name 주입 필수**, connect 1s/read 3s, `error=invalid_grant`만 2423·그 외 4xx와 5xx/IO는 2502, KOE는 로그 전용)·`KakaoOidcProperties`(+tokenUri·authorizeUri·nonceCookieSecure)·`AuthErrorCode`(+2423·2502). 교환 왕복은 트랜잭션 경계 밖
- MSG-496: 행사 운영자(ORG) 계정 기반 — `UserRole`에 ORG 추가(V45 `chk_users_role` CHECK 개정: USER|ORG|ADMIN), 로그인 응답(`LoginResponseDto` +role, requiredProperties 포함 — 생성 3곳+`applyTransport` 재조립 통과, DevAuth는 Oidc 재사용)·`GET /api/users/me`(`UserProfileResponseDto` +role)에 역할 노출, SecurityConfig `/api/org/**` `hasRole("ORG")` + **catch-all `anyRequest().hasAnyRole("USER","ADMIN")` 교체**(ORG의 일반 API 접근 차단 — FR-5 문자 구현, 2026-08-28 사용자 확정. `GET /api/users/me`·`POST /api/auth/logout`만 명시 authenticated로 ORG 공유). ORG 경로 프리픽스 `/api/org/**` 확정(MSG-497~500 URL 고정). JWT role 클레임·`ROLE_{role}` 매핑은 기존 배관 재사용(코드 변경 0), 개발용 ORG 계정은 수동 런북(스펙 작업 로그). 검증 `OrgAuthorizationTest` 12건 등 신규 21건
- MSG-497: 행사 운영자 계정 보안 — 비밀번호 API 4종(`GET /api/auth/password/status`(mustChange)·`POST change`(LOCAL 한정, 소셜 2445, 잔여 재설정 토큰 revoke 인TX)·`POST reset-request`(ORG·ADMIN LOCAL만 발송, 존재/부재/소셜/발송실패 전 분기 동일 응답, SET NX EX 쿨다운)·`POST reset`(선점→TX(해시+플래그+무효화 마커)→커밋 후 마커 갱신·deleteAll best-effort, 실패 시 Lua compare-and-restore)), 재설정 토큰 Redis 2키(`pwreset:{sha256}`+`pwreset:user:{id}`, Lua 원자화, TTL 30분, 사용자당 1개)·사용자 무효화 마커(`blacklist:user:{id}`, TTL=리프레시 2주, iat<=무효화초 fail-closed, ORG·ADMIN 스코프, reissue 회전 save 후 검사), 첫 메일 유틸(`global/mail` SES v2, LoggingMailSender `@Profile("!prod")`, prod fail-fast 2종, 발신 contact@fillmap.kr — MSG-499 재사용 예정), org 계정 설정(`GET·PATCH /api/org/profile`, `POST /api/org/email-change-request` — V46 `org_email_change_requests` PENDING 부분 유니크 + ON CONFLICT UPSERT), mustChange 인터셉터(`/api/org/**` preHandle, fail-closed). V46(users.password_must_change·contact_phone). AuthErrorCode 2441~2445·UserErrorCode 1420. 신규 테스트 76건, Codex 스펙 4R(20건)·구현 3R(4건) 수렴
- MSG-499: 비밀번호를 쓰는 **세 경로의 행 잠금 통일** — `PasswordService.changePassword`·`resetPassword`의 사용자 조회를 `UserRepository.findWithLockById`(PESSIMISTIC_WRITE, PG 실측 `FOR NO KEY UPDATE`)로 교체. 499가 신설한 초기 비밀번호 재발송이 같은 행을 쓰는데 한쪽만 잠그면 낡은 스냅숏이 최신 커밋을 덮어(lost update) 방금 사용자가 정한 비밀번호가 무효화되고 mustChange가 되살아난다. 재발송·변경·재설정 어느 순서로 인터리빙돼도 마지막 커밋 하나만 남는 것을 실 스레드 경합 테스트로 고정(변경 승 → 재발송 1423·발송 0회, 재발송 승 → 변경 2442). MSG-497 로직 자체는 무수정(조회 한 줄 교체)

### `user` (Owner B · 구현 강정민) — 🟡 부분
- `entity`(User·AuthProvider·UserRole), `repository/UserRepository`, `exception/UserErrorCode`
- MSG-205: 계정 삭제(`DELETE /api/users/me` — `controller/UserController`·`service/UserService`(+Impl) 첫 신설, V15 reports FK ON DELETE(CASCADE/SET NULL), 삭제 트랜잭션 = S3 키 수집(`findAllS3KeysByUserId`)→`deleteUser` 0행 1404, afterCommit = S3 1000키 청크 best-effort·refresh `deleteAll`·액세스 토큰 블랙리스트 각각 독립 try. CASCADE 보정 로직 없음, D5 경합 수용 — 스펙 §D1~D5)
- MSG-203: 프로필 조회/닉네임 수정 — `GET /api/users/me`·`PUT /api/users/me/nickname`, `dto/{UserProfileResponseDto,NicknameUpdateRequestDto}` (email 은 카카오 유저 null — MSG-310, 색상 수정은 기획 제외)
- MSG-185: `User.friendCode`(V18 — 생성자 자동 부여, 혼동 문자 제외 32종 8자)·`UserRepository.findByFriendCode` — friend 도메인이 소비
- MSG-402: 위치기반서비스 이용 동의 저장 — V32(`users.location_consent` NOT NULL DEFAULT FALSE·`location_consent_changed_at` NULL), `User` 필드 2개만(상태 전이 메서드 없음 — 갱신은 `UserRepository.updateLocationConsent` native 원자 UPDATE 한 곳: `IS DISTINCT FROM`+CASE로 값이 실제 바뀔 때만 시각 갱신, `@Modifying(clearAutomatically=true)`, 0행→1404), `PUT /api/users/me/location-consent`(`LocationConsentUpdateRequestDto` — `Boolean @NotNull`, 응답은 프로필 DTO 재사용)·`UserProfileResponseDto` +locationConsent(requiredProperties 포함). 서비스는 메서드 `@Transactional`+`Clock.systemUTC()` 고정 주입. **가입 경로(auth) 무수정** — 신규·기존 전원 off 시작, 온보딩 진입 게이트는 FE 몫(FR-USER-14). 동시 반대 토글의 시각 경합은 주석 수용(javadoc, Codex 리뷰 판정). **2026-08-19 MSG-433에서 철회 불가로 개정** — 이 줄의 토글·updateLocationConsent 서술은 작성 시점 기록이고, 현행은 켜기 전용 `consentToLocation`(아래 MSG-433 줄)
- MSG-373: 프로필 이미지 등록·변경·제거 + 가입일 노출 — presign 발급 `POST /me/profile-image/presigned-url`(jpg·jpeg·png·webp만, heic 1415 거부 — D-4 재확정, 5MB 초과 1413, 서명에 크기·타입 고정), 확정 `PUT /me/profile-image`(prefix 소유권→확장자 화이트리스트 재검증→HeadObject 실존→실측 크기 순, `profiles/pending/{userId}/`→`profiles/original/{userId}/{새 uuid}` 서버측 복사, `users.profile_image_url`에 **완성 공개 URL** 더티 체킹 저장 — 친구 축 3종 프로젝션 무수정 자동 반영이 §D-1의 강제), 제거 `DELETE /me/profile-image`(null 복원, 멱등), `UserProfileResponseDto` +2(profileImageUrl·createdAt — 조회·닉네임 수정·이미지 API가 공유), 이전 객체는 afterCommit best-effort 삭제, 계정 삭제 키 수집에 `findProfileImageUrlById` 역산 키 추가. 영상과 달리 advisory lock·이중 확정 차단 없음(§D-3 의도), 마이그레이션 0. **ops 선행 2건**(deploy.md): original 프리픽스 공개 정책 + pending 라이프사이클
- MSG-433: 회원가입 약관 동의 저장 — **V37** users 신규 컬럼 5개(철회 없는 필수 3항목 만14세·서비스약관·개인정보는 `*_consented_at` 시각 컬럼이 동의 여부(NULL=미동의)와 시각을 겸함, 마케팅은 `marketing_consent`+`_changed_at` 쌍. 위치기반은 기존 `location_consent` 쌍 재사용 — 신규 컬럼 0, FR-USER-15 단일 값), API 3종 `GET /api/users/me/consents`(항목별 상태+`requiredCompleted` 서버 계산 — 게이트 판별 재료)·`PUT /me/consents`(멱등 제출 — 필수 4항목 `@NotNull @AssertTrue` Bean Validation으로 서비스 진입 전 400 전량 거절, 원자 UPDATE 한 문장: 필수 3항목 COALESCE 최초 시각 보존·위치/마케팅 IS DISTINCT FROM CASE)·`PUT /me/marketing-consent`(location-consent 미러), DTO 3종(`ConsentStatusResponseDto`·`ConsentSubmitRequestDto`·`MarketingConsentUpdateRequestDto`). **위치 동의는 철회 불가**(2026-08-19 팀 합의, FR-USER-14 개정) — 기존 `PUT /me/location-consent`는 켜기 전용이 되어 `consented=false`는 `LOCATION_CONSENT_IRREVOCABLE`(**1400**, BAD_REQUEST) 거절, 리포지토리 갱신 쿼리도 `consentToLocation`(TRUE 상수화)으로 교체, 철회가 없으니 requiredCompleted 게이트 재등장 문제도 근본 소멸. **동의 컬럼 7종은 `@Column(updatable=false)`** — 더티 체킹 전 컬럼 UPDATE가 낡은 스냅숏으로 동의를 되덮는 우회 차단(Codex P1, 가드 제거 시 실패하는 회귀 테스트 동반). auth 패키지·프로필 DTO·로그인 응답 무수정 — 게이트 판별은 전용 조회(MSG-402 D-2 미동봉 방향 유지). FE(MSG-422·407) 공유: 프로필의 위치 끄기 UI 제거(디자인 4개 페이지에서 기제거)
- MSG-499: 행사 운영자 계정 발급 — **V48** `org_account_requests`(발급 요청 큐: org_name·contact_name·contact_phone·email·event_name·content·status(PENDING|ISSUED|REJECTED CHECK)·reject_reason·issued_user_id FK·created_at(최초 접수, 보존)·updated_at(마지막 접수 겸 심사의 낙관적 검증 토큰)·processed_at + **부분 유니크** `uq_org_account_requests_pending(email) WHERE status='PENDING'` + `idx_org_account_requests_status_updated`) · `users.org_name` 추가. `entity/{OrgAccountRequest,OrgAccountRequestStatus}`(연관관계 없이 issuedUserId Long, 상태 전이 `issue`·`reject` 더티 체킹), `User.createOrgUser`(LOCAL·ORG·passwordMustChange **true를 세우는 유일한 경로**, nickname=담당자명)·`resetInitialPassword`(해시만 교체, 플래그 유지 — `changePassword`는 플래그를 내려 재발송에 못 씀). API 8종: 공개 접수 `POST /api/org-account-requests`(**permitAll·비로그인**, native ON CONFLICT UPSERT 한 문장이라 재접수가 대기 1행으로 수렴·created_at 보존, `PUBLIC_AUTH_PATHS` 무등록 — MSG-443 규칙) + 관리자 7종 `/api/admin/{org-account-requests(목록·상세·approve·reject),organizations(직접 발급·{userId}/resend-password·목록)}`. `service/OrgAccountRequestService`(접수·큐·상세·승인·반려, 심사 진입부는 `findWithLockById` 잠금 → PENDING 확인 → **updated_at 에코 대조**(불일치 1426, 익명 재접수·변조가 검토 내용을 바꾼 경우))·`OrgAccountIssueService`(발급 코어 — SecureRandom 16자(혼동 문자 제외, 영문·숫자 각 1자 이상)·bcrypt 저장·**커밋 후 발송**(TransactionTemplate 명시 경계)·발송 실패 격리 `emailSent:false`, 직접 발급·재발송·목록). 재발송 = 재발급(새 비밀번호 교체, 이전 값 즉시 무효). **평문은 메일 본문 한 번 외에 응답·로그·DB 어디에도 없다** — 실패 로그는 예외 타입만 남긴다(예외를 로거에 넘기면 본문을 담은 메시지가 스택으로 샌다, Codex 1R). 발급 메일의 발급 시각은 KST 마이크로초까지(같은 분 두 번 재발송의 구분이 복구 계약). `UserErrorCode` 1421~1426. 신규 테스트 48건
- MSG-500: 아이디(공식 이메일) 변경 요청 심사 — MSG-497이 접수까지 만들고 어느 화면에도 안 걸려 있던 요청을 닫는다. **V51** `org_email_change_requests` +2컬럼(`processed_at`·`reject_reason`) + CHECK 2개(`(status='PENDING') = (processed_at IS NULL)` · `(status='REJECTED') = (reject_reason IS NOT NULL)` — 둘 다 **양방향 동치식**, 반쪽 OR 식은 반려 행의 사유 NULL을 통과시킨다). API 3종은 기존 `AdminOrgAccountController`에 얹는다(계정 축의 한 화면이라 컨트롤러 신설 없음): `GET /api/admin/email-change-requests`(상태 필터·건수 3종 전체 집계·users 세타 조인으로 현재 아이디와 요청 이메일 병기 — 심사는 둘의 대조가 전부) · `POST .../{id}/approve` · `POST .../{id}/reject`. `service/AdminEmailChangeRequestService`(신설) — **검토 시점 가드가 핵심**: 접수 UPSERT가 재요청을 같은 PENDING 행의 제자리 갱신으로 처리하므로 상태만 보면 관리자가 **본 적 없는 이메일**을 승인한다. 전이 UPDATE 술어에 `created_at = :requestedAt`을 넣어 원자 비교하고 어긋나면 1429, 0행이면 재조회로 1427(부재)·1428(처리됨)·1429를 가른다. 승인은 전이와 `users.email` 교체가 한 트랜잭션이고 **둘 다 벌크 UPDATE**다 — `UserRepository.updateEmail`은 email 한 컬럼만 세팅(이 프로젝트는 `@DynamicUpdate` 미사용이라 더티 체킹은 전 컬럼 UPDATE가 되어 동시 비밀번호·담당자 수정을 낡은 스냅숏으로 되덮는다). 이메일 선점은 선검사로 1409를 주되 **선검사는 경합에 진다** — 벌크 UPDATE라 `uq_users_email` 위반이 실행 즉시 `DataIntegrityViolationException`으로 떠서 같은 1409로 번역된다(커밋까지 미뤄지면 잡을 자리가 없다). 승인 진입부에서 **users 행을 먼저 비관 잠금**한다(`findWithLockById` 재사용) — 계정 삭제가 users 삭제 후 FK CASCADE로 요청 행을 잠그므로, 요청 행을 먼저 잠그면 반대 순서가 되어 동시 실행이 AB-BA 데드락(한쪽 500)으로 끝난다(Codex 적발). 반려는 users를 안 잡아 교차가 없다. 승인만 메일을 보낸다(**새 이메일로** — 로그인 수단이 바뀌는 사건이라 미통지 시 계정 접근을 잃는다, 커밋 후 발송·실패는 `emailSent:false`), 반려는 수기 통보 유지. 처리로 PENDING이 빠지면 부분 유니크가 풀려 같은 계정의 재접수가 자연히 다시 열린다. `UserErrorCode` 1427~1429 신설(1421·1422·1426을 재사용하지 않는 것은 메시지가 "발급 요청"을 지목해서고, 상태·페이지 검증은 같은 도메인이라 1424·1425 그대로). 테스트 17건(심사 15 + 비트랜잭션 2 — 동시 승인 1409 수렴·email 외 컬럼 보존)

### `grid` (Owner A · 구현 강정민) — 🟡 부분
- MSG-73: `GridEncoder`·`GridConstants`(순수 유틸), `entity/{UserGrid,UserGridId,Grid}`, `repository/GridRepository`, `service/GridQueryService`(+impl, read 계약 A→B), `controller/GridController`, `dto/*`, `exception/GridErrorCode`(4xxx)
- MSG-90: viewport cursor 페이지네이션(`GridCursor` Base64URL 커서, `OccupiedGridPage`, `OccupiedGridPageResponseDto`, keyset 행값비교+lookahead, `?strategy` 파라미터·`ViewportStrategy` 제거 — A 고정, repo B 쿼리는 보존)
- MSG-167: 격자 중심점 행정동 라벨 저장 — V5 `grids.region_code`(nullable FK→regions, 쓰기 시 1회 판정·조회는 equi) + 멱등 백필(`region_code IS NULL`만, regions 미시딩 no-op). 판정 규칙 = 93/155 중심점 축(`ST_Covers … ORDER BY region_code LIMIT 1`). 인덱스 미추가·Grid 엔티티 미매핑(native)
- MSG-238: V7 `idx_grids_region_code`(단순 btree — 167 §D5 예약 발동, region_code 주도 조회 최초 등장의 물리 기반. partial 기각)
- MSG-347: 격자 계산 EPSG:5179 전환 — `GridConstants`(STEP 상수 제거 → `CELL_SIZE_METERS`·`CRS_DEF_EPSG5179` 계약 문자열), `GridEncoder` 내부 교체(Proj4J 1.4.3, `CoordinateTransform` static 공유 — 스레드 안전 판정 근거 주석+병렬 2000회 회귀 테스트) + `viewportRange` 헬퍼 신설(SW·NE 2점 → 꼭짓점 4점 min/max, GridQueryServiceImpl 2곳·HotZoneServiceImpl 1곳 소비 — 5179 격자축 비평행로 인한 가장자리 셀 누락 방지), V28 데이터 전량 이행(스냅숏 구/신 판별·ON CONFLICT·mission target_count 클램프·region_stats 전량 재계산·검증 DO 4항목), `seed/zones.json` 48건 역산 재산출(`scripts/zones-draft/requantize-zones-5179.py` — 확정 시드 역산, 검수 보존), FE 정합성 픽스처 200건(`fixtures/grid-epsg5179-samples.json` crsDefinition 메타 포함 + `GridSampleFixtureTest`), 이행 통합 테스트 `usergrid/GridEpsg5179MigrationTest` 7종(V28 본문 런타임 실행·롤백). 의존성 proj4j implementation·proj4j-epsg testImplementation. FR-13(좌표 범위 검증)은 기구현 확인(`KoreaCoordinates`·3400) + 경계값 테스트만 추가. **2026-08-09 후속 수정**: V28의 좌표 변환이 proj4 문자열 직접 전달이라 호출당 0.846ms가 들어 dev 배포 이행이 수 분간 멈췄다(헬스체크 실패) — SRID 5179 전달(0.008ms, 106배)로 교체하고, 이행 전 두 경로를 221점에 실제로 돌려 좌표를 대조하는 검사를 신설(`proj4text` 컬럼 비교는 무효 — PostGIS가 그 컬럼 대신 PROJ 내장 EPSG DB를 쓰는 것을 실측 확인). dev 실측 `execution_time 245348`ms · `mission_grids` 48,134행
- MSG-349: 지도 응답 3종에 행정동 이름 동봉(MSG-341 "폴백 미제공" 결정 3행을 뒤집음) — 뷰포트 실경로 쿼리 3종에 `LEFT JOIN regions`(+`OccupiedGridProjection.getRegionName`, 접근 B는 무변경·게터 null), 단일 격자 `getCell`은 중심점 재판정(`RegionQueryService.resolveByPoint` 주입, 미점령 격자도 이름 — grids 행 없이도. `KoreaCoordinates.isOutOfService` 가드로 6400 흡수, by-grid MSG-153 §D6 동형), 핫구역은 `GridRepository.findRegionNames`(IN 최대 50, INNER JOIN — regions.region_name NOT NULL+FK라 null 값 불가) 일괄 1회 + `GridRegionNameProjection` 신설. DTO 3종 required+nullable 병기. regionName은 구역 안 격자에도 항상(FR-9 — 디자인 위치줄 시/구 재료). ZoneController·ZoneResponseDto의 "FE-local 계산" 잔재 문구 정정
- MSG-356: 줌아웃 행정 단위 집계 조회 — `GET /api/grids/aggregation`(`unit=DONG|SIGUNGU|SIDO` 대소문자 무관, 페이지 없음, 빈 뷰포트 200+빈 배열), `dto/RegionUnit`(codePrefixLength·nameTokenIndex·maxSpanDeg = 동 10/3/1.0·구 5/2/4.0·시 2/1/10.0)·`dto/RegionAggregateResponseDto`·`service/RegionAggregateView`·`repository/RegionAggregateProjection` 신규, `GridQueryService.getOccupiedAggregatesInViewport`(계약 4번째 메서드 — 기존 3종 불변, B는 friend 위임 소비)·`GridRepository.aggregateOccupiedInRange`(행정동 코드 접두 GROUP BY native — 같은 식 2회 바인딩 시 PG 거절 실측이라 출력 서수(GROUP BY 1) 사용, LEFT JOIN regions·NULL 키 한 그룹이 무귀속 버킷(FR-7), 이름은 split_part 토큰 MIN, 좌표는 center_geom ::geometry 평균)·`GridErrorCode.INVALID_AGGREGATION_UNIT(4405)`. `validateBounds(bounds, maxSpanDeg)` 공통화 — WGS84 범위 검증(±90/±180, NaN·무한대 흡수)이 뒤집힘·상한보다 선행하며 기존 개별·페이지 조회에도 소급(스펙 명시 승인, MSG-73/90 계약 회귀 없음 — 테스트 확인). 벤치 `GridAggregationExplainBenchmark`(GRID_BENCHMARK 게이트) — 1만 행 전국 SIDO EXPLAIN ANALYZE 10.9ms. 전환 축척 표(동 250~500m 등)는 FE 정책 — 서버는 단위 파라미터만. ADR: 위키 "ADR 줌아웃 클러스터링 행정 단위 집계 H3 기각"(cf-34537474)
- MSG-374: 지도 집계 응답을 배열에서 `{currentRegion, items}` 객체로 변경. `currentRegion`은 뷰포트 중심 행정동의 코드·동 이름과 그 동 전체의 내 점령 격자 수·영상 수를 제공하며 bbox·unit과 독립이다. 중심점은 서비스 범위 밖 사전 가드 후 `RegionQueryService.resolveByPoint`로 판정하고, 해상·범위 밖이면 null이다. `GridRepository.summarizeOccupiedByRegion`은 `grids.region_code` 기준 `COUNT(*)::int`·`COALESCE(SUM(user_grids.video_count), 0)`으로 센다. 기존 `aggregateOccupiedInRange`·`RegionAggregateResponseDto`·친구 집계 배열 계약은 불변이며 친구 API 설명만 차이를 명시했다. 신규 에러코드·마이그레이션·저장 쓰기 0건
- **없는 것**: `GridOccupationService`(write는 MSG-66이 흡수), `HotZoneService`(MSG-233 §D5로 `hotzone` 독립 패키지 배치 확정 — grid 아님)

- MSG-439: `GridQueryService.resolveRegionNames(Collection<String> gridIds)` — 계약 메서드 비파괴 추가(A→B read, event 위치 표시명 재료). `gridId → 행정동 이름` 사전이고 무귀속·범위 밖은 키 부재(null 값 없음). **grids 행 존재와 무관하게 격자 중심점 행정동 판정 보장** — lazy insert라 행사 대표 격자는 행이 없는 게 정상이며, `GridRepository.findRegionNames`(grids INNER JOIN이라 행 없는 격자 누락)와는 이름부터 분리(javadoc 사유 명시). 구현은 격자당 `resolveByPoint`(ST_Covers) 1회 루프 — 소수 격자 전제, 수백 건 소비처가 생기면 점 배열 벌크 쿼리 승격(javadoc 상한). 계약 테스트 3건(행 부재 내륙·무귀속 해상·빈 목록)

### `usergrid` (Owner B · 구현 강정민) — 🟡 부분
- MSG-152: `repository/{UserGridRepository,CollectionSummaryProjection}`(user_grids·videos 네이티브 집계), `service/UserGridQueryService`(+impl, read 계약 B→A)·`CollectionSummaryView`, `controller/CollectionController`(`GET /api/collections/summary`), `dto/CollectionSummaryResponseDto`
- MSG-246: 도감 요약 `visitedRegionCount` 정정(`getCollectionSummary` 서브쿼리 — dead `videos.region_code` 대신 `JOIN grids` 후 `COUNT(DISTINCT g.region_code)`, MSG-167 by-grid 귀속 정합. 테스트 시딩도 프로덕션 형상(videos.region_code NULL)으로 재작성)
- MSG-153: 갤러리 격자 목록(`GET /api/collections/grids` — `first_collected_at DESC` 30 고정·무커서, `GridEncoder.decode`로 grids 미조인, `ThumbnailUrlPresigner` 소비, `CollectionGridProjection`/`CollectionGridView`/`CollectionGridResponseDto`·`getCollectionGrids` B-내부 read)
- MSG-460: 검색 무입력 전체 지역 목록 개인화 커서 페이지(`GET /api/regions/explore` — 고정 20개와 lookahead 1개). 로그인 사용자의 `user_grids.last_uploaded_at` 행정동별 최댓값이 있는 지역을 최근순으로 먼저 보여 주고, 나머지는 전역 공개 격자 수 내림차순이다. 커서는 사용자와 정렬 경계를 묶는다. 잘못 추가됐던 개인 도감 페이지 API는 제거하고 MSG-388의 기존 무제한 행정동 조회를 복원했다
- MSG-461: 위 `getRegionExplorePage`의 `content_regions` CTE에서 **`regions` 조인을 집계 밖으로** 뺀 성능 수정(6,810ms → 266ms, dev 실측 2026-08-25). 조인이 집계 앞에 있어 행정동 조회가 격자 수만큼 돌고(`Memoize loops=144536`) `GROUP BY (region_code, region_name)`으로 키가 넓어 144,542행 정렬이 `work_mem`을 넘겨 디스크로 샜다(`external merge Disk 2312kB`). `region_code`로만 접어 1,339행을 만든 뒤 이름을 붙여 조인이 1,339번이 되고 정렬이 메모리에서 끝난다(`quicksort Memory 110kB`). CTE에 `region_code IS NOT NULL`도 붙였다 — 바깥 INNER 조인이 어차피 버릴 무라벨 격자 7,470개를 세기 전에 걷어낸다(결과 집합 동일, 전체 응답은 오차 범위). **정렬 규칙·응답 계약·커서·마이그레이션 전부 불변**이고 `RegionExplorePageProjection`·`RegionExploreCursor`·서비스·컨트롤러는 무수정이다. 티켓의 최초 진단("전역 집계 반복이 원인")은 분해 실측으로 반증됐다 — 집계 자체는 257ms다. 남는 한계는 집계가 격자 수에 비례하는 것이고 NFR-PERF-09에 승격 조건(격자 50만 초과 또는 p95 1초 초과 → 머티리얼라이즈드 뷰)을 뒀다. 의사결정 기록 `docs/explainers/MSG-461-region-aggregate.html`
- MSG-167: 갤러리 목록에 `regionName` 추가(`grids`·`regions` LEFT JOIN equi, geospatial 0 — 153 "grids 미조인"을 라벨 위해 뒤집음, 정렬·30상한 등 나머지 계약 불변), `CollectionGridProjection`/`View`/`ResponseDto`에 regionName 1필드. 동 단위 내 영상 조회(`GET /api/collections/videos?regionCode=` — `videos⨝grids` 격자 축 귀속·ACTIVE만·`created_at DESC, id DESC`·no-LIMIT·빈 배열 200, `RegionVideoProjection`/`View`/`RegionVideoResponseDto`(gridId 포함)·`getRegionVideos` B-내부 read)
- MSG-186: 친구용 격자 목록 `getCollectionGridsForFriend(ownerUserId)` — `UserGridQueryService`에 **메서드 1개 비파괴 추가**(기존 3종 무수정, friend 도메인만 소비하는 B-내부 read — Owner A 미소비 non-breaking). `getCollectionGrids` 재사용 금지가 이유: 본인용은 cover를 visibility 무필터로 읽어 PRIVATE 썸네일이 친구에게 샌다. 정렬·상한은 본인 갤러리와 동일(`first_collected_at DESC, grid_id DESC` 30)이고 **썸네일 선정 한 곳만 갈린다** — `LEFT JOIN LATERAL`(격자당 1행)로 `status='ACTIVE' AND visibility='PUBLIC' AND processing_status='READY' AND thumbnail_url IS NOT NULL` 4조건 중 cover 우선(`ORDER BY (v.id = ug.cover_video_id) DESC, created_at DESC, id DESC LIMIT 1`) → 없으면 NULL(격자 사실만). visibility 필터가 MSG-285 FRIENDS 확장 지점(쿼리 주석 명시 — `IN ('PUBLIC','FRIENDS')` 한 곳 수정). `FriendCollectionGridProjection`/`FriendCollectionGridView` 신규(cover 2필드 대신 `thumbnailKey` 1개 — 영상 ID 미노출), 매핑은 기존 `GridEncoder.decode`+`ThumbnailUrlPresigner.presign` 재사용. 요약은 `getCollectionSummary(userId)` 시그니처 그대로 재사용(수정 0줄)
- MSG-315: 주간 활동 요약 배치 — `service/WeeklySummaryScheduler`(cron `0 0,30 19 * * SUN` zone KST — 스트릭 리마인드 20~23시 창 회피, 19시 30분 재발화 = 당일 재시도이고 `WEEKLY:{ISO주차}` dedupe가 중복 흡수. **클래스에 `@Transactional` 없음** — `record`가 REQUIRED라 붙이면 한 명 실패가 전원 롤백, 사용자 단위 catch로 격리), `UserGridRepository.findWeeklyActivity`(user_grids·videos UNION ALL 후 user_id 합산 1쿼리 — 결과가 곧 활동자라 대상 필터 불요, 영상만 `status <> 'DELETED'`)·`WeeklyActivityProjection`. **집계 임계값은 KST 주 시작을 UTC로 환산해 바인딩** (초판의 `ZoneId.systemDefault()` 환산은 MSG-376 후속 5d8f26a에서 정정 — 재료 컬럼은 naive=UTC 규약이고 V33이 DEFAULT 경로까지 존 무관화)
- MSG-181: 격자 점령 사용자 역조회 `getGridOccupants(gridId)` — `UserGridQueryService`에 메서드 1개 비파괴 추가(MSG-186 선례 동형, notification만 소비하는 B-내부 read), `GridOccupantProjection`/`GridOccupantView`(userId, regionName — LEFT JOIN grids→regions 1왕복), V24 `idx_user_grids_grid`(최초의 grid 축 접근 경로)
- MSG-362: 도감 요약 확장 + 날짜별 업로드 기록 — `getCollectionSummary` 한 문장에 스트릭·뱃지 서브쿼리 3개 가산(왕복 1회 유지. currentStreak 는 `last_recorded_date` 가 KST 오늘·어제일 때만 저장값, 표현식은 `upsertOnUpload` 와 글자 동일, 행 없음 COALESCE 0 — 끊긴 옛 값 차단 §D3), `CollectionSummaryView`/`ResponseDto` 필드 3개(friend 프로필 `summary` 는 중첩 재사용이라 무수정 자동 노출 — FR-4), `GET /api/collections/upload-history` 신설(`getUploadHistory` — `created_at` 을 `:storedZone`('UTC' 고정 — 초판 JVM 기본 존은 5d8f26a 정정)→KST 이중 AT TIME ZONE 으로 접어 업로드 있는 날만 오름차순, status 무필터 = DELETED·BLINDED 포함이 의도(FR-8, 스트릭 무차감 정합), `idx_videos_user_created` 구동·신규 인덱스 0), `UploadHistoryProjection`/`View`/`ResponseDto` 신규, `UserGridQueryService` 메서드 1개 비파괴 추가(MSG-186 선례 동형, collections 만 소비)
- MSG-388: 갤러리 격자 목록을 지도 홈 패널 카드 조회로 확장 — `GET /api/collections/grids`에 선택 파라미터 3개(`regionCode` 격자 축 행정동 필터, `sort` 신규 enum `CollectionGridSort`(COLLECTED 기본·UPLOADED), `limit`). 무파라미터 호출은 기존 계약 불변(수집순 30, 테스트로 회귀 고정 — FR-7)이고 패널은 `regionCode&sort=UPLOADED&limit=20`, 전체 보기는 limit 생략(=`LIMIT NULL` 무제한). 쿼리는 기존 `getCollectionGrids` 하나를 파라미터화 — nullable은 `CAST(:param AS 타입)` 필수(PG 타입 추론 실패), sort는 리포지토리 경계에서 String(`sort.name()` — 네이티브 enum 바인딩은 Hibernate 6가 ordinal로 넘겨 텍스트 비교 상시 false), CASE 이중 정렬 키 + `grid_id DESC` 타이브레이크. 커버 조인 재편: READY 게이트를 조인 조건에서 썸네일 SELECT 표현식으로 옮겨 `coverDurationSec`(신규, Projection/View/ResponseDto additive)은 게이트 밖 — 인코딩 중 카드도 duration 뱃지 유지, null은 커버 부재뿐. `UserGridQueryService.getCollectionGrids` 시그니처 확장(계약 인터페이스지만 소비 지점 전수 확인 결과 CollectionController뿐 — B 내부 read, non-breaking 실측). 서비스 limit 결정은 `resolveLimit` early return(삼항이면 Integer/int 혼합 언박싱 NPE — 전체 보기 경로 전멸, TDD 적발). 마이그레이션·신규 인덱스 0
- **없는 것**: — (155/156 소비용 프리미티브 구상은 불필요해져 폐기 — 155 자기완결·156 별도 서비스로 종결)

### `mission` (Owner B · 구현 성민) — 🟡 부분
- MSG-166: V6 스키마 검증 테스트(`MissionSchemaMigrationTest` — 엔티티 없던 시점)
- MSG-222: 활성 미션 조회(`GET /api/missions/active` — `entity/{Mission,MissionType,MissionGrid,MissionGridId}`(조회 전용), `MissionRepository.findActive`(기간 경계 독립 판정)·`MissionGridRepository`, `MissionQueryService`(+impl — 유형→shape 단일 분기: COURSE→PATH(path 원문+spots)/EVENT→BOX(bbox 합성)/THEME·CONTINUOUS→CELLS/AREA→REGION(코드만), 1h 전역 캐시 단일 volatile CacheEntry+더블체크 락, 단일 인스턴스 전제), `dto/MissionResponseDto`+`sealed MissionShape` 4종. 기본 클럭 `Clock.systemUTC()` — KST JVM 9h 스큐 정정, MSG-223 리뷰 파생)
- MSG-223: 미션 완료 판정·스탬프(`entity/{UserMission,UserMissionId}`(UserBadge 미러·비회수)·`UserMissionRepository`(`insertIgnoreConflict` ON CONFLICT·`countMyStamps` — 종류 무관 총수, **MSG-363에서 `countMyStampsByType`으로 교체**), `MissionRepository.findAwardCandidateIds/findCompleted`(native, `recorded_at` 판정·무기간 IS NULL 생략·`AT TIME ZONE 'UTC'` 정규화), `MissionAwardService`(+impl — 신규 INSERT 성공분만 응답·MISSION_COUNT 뱃지 배선 1회 호출, **MSG-363에서 종류별 3축 분기로 교체**), 업로드 확정 훅(streak 다음·점령 분기 바깥, `VideoUploadResponseDto.completedMissions`), V12 뱃지 시딩 1·5·10)
- MSG-224: 축제 미션 적재(`seed/{FestivalRecord,FestivalJsonlReader,FestivalMissionSeeder}` — 플래그 게이트 `fillmap.mission.festival.seed.enabled` 기본 off·`@Order(30)`, 시드+격주 수동 갱신 단일 러너·`@Transactional` 원자성, 9×9 격자 81행·target_count=1, dedupe=중심격자+기간(min+4 복원), 종료 정리 `deleteEndedFestivalsWithoutStamps`(native, `AT TIME ZONE 'UTC'`·스탬프 잔존). **V13 `missions.source`**(VARCHAR(30) NULL) — EVENT 타입이 팝업(MSG-235)과 공유라 `source='FESTIVAL'`로 소유 식별(Codex 리뷰 파생, 타 소스·NULL 불가침). `Mission` 시드 `@Builder` 6필드·`created_at` insertable=false 전환)
- MSG-225: 코스 미션 시드(`seed/{CourseRecord,CourseSeedReader,CourseMissionSeeder}` — 플래그 게이트 `fillmap.mission.course.seed.enabled` 기본 off·`@Order(40)`, 무기간 INSERT-only(정리 단계·클럭 없음), dedupe=제목×`source='DURUNUBI'`, path=GeoJSON LineString 원문 jsonb. reader는 전량 거부 검증 계약 — LineString·좌표 쌍·스팟 5~8·seq 연속·gridId 포맷/정규형/중복·crsIdx/name 문자열(Codex 3라운드 파생 4건 포함). `Mission` 빌더 path 확장·`MissionGrid` seq 생성자. 리포지토리·조회·판정 경로 무수정 — 무기간 판정은 MSG-223 엔진 `IS NULL OR`로 자연 성립(계약 라운드트립 테스트 실증). 산출 파이프라인은 레포 밖 `~/fillmap-data/durunubi/spot_pipeline.py`)
- MSG-235: 팝업 미션 적재(`seed/{PopupRecord,PopupJsonlReader,PopupMissionSeeder}` — 플래그 게이트 `fillmap.mission.popup.seed.enabled` 기본 off·`@Order(50)`, 주 1회 수동 갱신 단일 러너·**정리→적재 순**(id 단독 키라 연장 팝업 공백을 같은 실행에서 흡수 — 축제와 반대), 멱등=**V14 `missions.source_key`**(팝가 id, `(source,source_key)` 부분 유니크 백스톱)·INSERT-only, 9×9·target_count=1·**type=POPUP 신설**(V14 CHECK 확장, 조회 `case EVENT, POPUP → BOX`, 판정 무수정). reader는 코스 전량 거부 계약 승계(id 정수·중복, 좌표 33~39/124~132, 날짜 순서 — periodType 미사용·날짜 직접 판정). 정리 쿼리 `deleteEndedBySourceWithoutStamps(:source)` 파라미터화(축제 메서드 흡수). 산출은 레포 밖 `~/fillmap-data/popups/crawl_popga.py`)
- MSG-363: 미션 뱃지 판정을 종류별 3축으로 — `MissionAwardServiceImpl`이 완료 미션 종류와 무관하게 `award(MISSION_COUNT, 총수)` 1회를 부르던 자리를 "이번에 **INSERT 성공한** 스탬프의 종류마다 그 종류 총수로 판정"으로 교체. `BADGE_AXIS`(EVENT→EVENT_COUNT·COURSE→COURSE_COUNT·POPUP→POPUP_COUNT `Map.of`) 미등재 유형(AREA·THEME·CONTINUOUS)은 스탬프만 발급되고 뱃지 판정 없음이 정상 경로라 `MissionType.valueOf` 대신 null 스킵(FR-6). 종류는 `findCompleted`가 이미 select 하는 `m.type`을 재사용 — 미션 재조회 0. 집합은 `LinkedHashSet`(같은 종류 2건 완료여도 metric이 종류 총계라 축당 1회, 삽입 순서 고정으로 `newBadges` 배열 순서 안정) + `findCompleted`에 `ORDER BY m.id` 추가(집계 결과 순서는 PG 미보장). `UserMissionRepository.countMyStamps` → `countMyStampsByType`(user_missions PK prefix + missions PK 조인, 추가 인덱스 0 — 총수 호출자가 하나도 안 남아 구 메서드는 제거). 지배 경로(미션 미해당 업로드)는 후보 0건 조기 종료라 쿼리 수 불변, 한 종류 완료도 변경 전과 동수·두 종류 동시 완료 시에만 판정 3종이 한 벌 더(실질 상한 2벌). 임박 알림 하루 1건 상한은 축이 늘어도 유지(같은 트랜잭션에서 두 번째 축이 첫 축의 native INSERT 기록을 보고, advisory 잠금은 재획득 성공). 마이그레이션·API·에러코드 0 — 뱃지 쪽 스키마는 badge 섹션 V29
- MSG-383: 미션 메타데이터 8컬럼 적재·노출 — V31로 `missions`에 `description`·`place_name`·`source_url`·`operation_time`·`image_url`·`distance_meters`·`duration_minutes`·`difficulty` 추가(전 컬럼 nullable, `chk_missions_course_metrics`는 코스 아닌 행에서 코스 3종이 전부 NULL이기를 요구). `MissionMetadata` 값 record + `Mission.applyMetadata`(더티 체킹 갱신, 반환 boolean으로 실제 변경분만 집계), `SeedText`(`text`·`truncatePlaceName`·`normalizeBreaks` 공용 — 세 reader가 같은 컬럼을 채워 규칙을 한 곳에). **시더 skip 경로를 메타데이터 갱신으로 확장해 재실행이 곧 백필**(멱등 키·`title`·기간·`target_count`·`path`·`mission_grids` 불변, 스탬프 무영향). `MissionRepository.findBySource`에 `ORDER BY m.id`(중복 백필 대상 결정성 — Codex 2R 지적). `CourseSeedReader`는 결측 허용·형식 위반 거부: 비숫자·비유한(`NaN`/`Infinity`, 문자열 경로 포함)·INTEGER 범위 초과·난이도 1~3 밖·거리와 소요시간 0 이하를 던진다. `SeedResult`에 `updated`, 코스에 `missingMetadata` 추가. `image_url`은 이 티켓에서 항상 NULL이고 **우리 S3 URL만 저장한다는 계약**만 정해 MSG-384로 넘김(NFR-DATA-07). `MissionResponseDto`에 8필드(required + nullable 병기 — 이 레포의 required는 키 존재 의미). 조회 서비스·컨트롤러 무수정. **한계**: 리더가 종료 원본을 걸러내 스탬프로 살아남은 종료 미션은 백필 대상 밖(스펙 작업 로그에 명시)
- MSG-384: 축제 소스를 TourAPI로 전환·대표 이미지 적재 — 마이그레이션·API·에러코드 **0**(V31 컬럼을 채우기만 한다). `FestivalRecord.imageKey`(버킷 상대 키) 신설, 시더가 `AwsProperties`로 공개 URL을 조립해 `missions.image_url`에 넣는다(`UserServiceImpl.toPublicUrl`과 같은 형식, 공용 추출 안 함 — 그 클래스의 "세 번째 사용처" 규칙). **이미지 보존 병합** `preserveImage`: 새 스냅샷의 `imageKey`가 비면 기존 `image_url`을 유지한다 — `applyMetadata`가 8필드 통째 대입이라 원본이 내려간 갱신에서 우리 사본 주소까지 잃는 것을 막는다(**이미지 한 필드만의 예외**, 소개문·장소는 새 값으로 덮음. 병합을 호출부에 둔 것은 팝업·코스가 `applyMetadata`를 공유해서). `SeedResult.updatedIds` — 제자리 갱신된 미션 id 목록을 로그로 남긴다(전환 삭제 SQL의 `NOT IN` 재료. **값이 안 바뀐 매칭 행도 담는다** — 기준이 "달라졌나"가 아니라 "키가 맞아 제자리에 남았나"라, 빼면 대체 INSERT 없이 그 축제가 소멸). 리더에 **필수 필드 방어**(이름 공백·`KoreaCoordinates.isOutOfService`를 한 사유로 집계, 종료 필터 뒤에 배치 — 운영 절차 jq와 같은 모집단) + **이미지 키 3중 검증**(접두사 · 접두사 뒤 단일 객체명 · 래스터 확장자 허용목록 — 목적은 형식 통일이 아니라 보안. 공개 `missions/`에 `.svg`/`.html`이 올라가면 우리 도메인에서 스크립트가 실행되고, `missions/festival/../course/x.jpg`는 클라이언트 URL 정규화로 접두사 밖을 가리킨다). 좌표는 `asDouble()` 대신 `isNumber()` 판정 — Jackson 3에서 `""`·`"abc"`는 0.0이 아니라 coercion 예외라 한 행이 파일 전체를 날린다(리뷰 실측). **옛 소스 삭제 코드는 앱에 없다** — 전환 당일 운영자 SQL 한 번이고 순서가 적재 → 확인 → 삭제라 적재가 실패해도 옛 미션이 남는다(FR-MISSION-11). `MissionRepository` 무변경. 회귀 2건 동반 수정: 통합 테스트 합성 좌표를 남반구 → 서해 먼바다(34.0/125.0, 오프셋 최대 35.5)로 이동(좌표 검증이 남반구 픽스처를 전부 스킵), 롤백 테스트 독약 행을 위도 `4.5e14` → 이름 NUL(`\0`)로 교체(같은 이유로 파서에서 걸러져 영속화 단계에 도달하지 못함). `deploy.md`에 `missions/*` 공개 읽기 정책 런북 추가(적재 전 curl 200 확인 — 403이면 461건에 열리지 않는 주소를 채우게 된다)
- MSG-394: 팝업 포스터·코스 대표 이미지 적재 + 코스 격자 5179 이행 — 마이그레이션·API·에러코드 **0**(V31 컬럼을 채우기만 한다). 공용 추출 3건이 MSG-384가 예고한 "세 번째 사용처"에서 실행됐다: `SeedText.imageKey(row, prefix)`(접두사 · 단일 객체명 · **객체명 문법 `[A-Za-z0-9._-]+`** · 래스터 확장자), `AwsProperties.publicUrl(key)`, `MissionMetadata.withImageFallback(existing)`. **`Mission.applyMetadata`는 무수정** — 세 시더가 공유하므로 병합은 각 호출부가 명시적으로 고른다. 객체명 문법은 Codex가 잡은 URL 구분자 우회 방어다(`payload.html#x.jpg`가 확장자 검사를 통과한 뒤 `#` 프래그먼트로 `payload.html`을 요청하게 된다). `PopupRecord`에 `description`·`imageKey`, `CourseRecord`에 `imageKey` 추가. **레포 밖 수집 3종**: `crawl_popga.py --enrich`(소개문 키는 `content`, 포스터는 `files[0].path`에서 `_thumbnail_MAIN_W480` 접미사를 떼면 원본 1249×1562. **추가가 아니라 id 키 재작성** — `validate_popups.py`가 먼저 나온 **유효한** 행을 남기므로 뒤에 붙이면 보강분이 통째로 버려진다), `fetch_poi_images.py`(`areaBasedList2`를 `contentid`·`firstimage`까지 보존해 `poi-images.json`에 쓴다. **`poi-all.json`은 안 덮는다** — 스팟 선정 입력을 8월 2일로 얼려 격자가 흔들리지 않게), `attach_course_images.py`(사진 있는 첫 스팟, 매 실행 전량 재계산). `scripts/requantize-courses-5179.py`(레포 안, 1회성)가 구 셀 중심 `(g+0.5)×step`을 5179로 재인코딩해 V28 산술을 재현한다 — **DB 989행과 전수 일치**가 검증이고 허용 오차가 없다. `spot_pipeline.py`도 5179로 이행(픽스처 200건 자가검증, `self_check` 기준값 166 → **164** 실측). 억제 목록 `scripts/mission-image-suppressions.json`(레포 안 커밋, 수집기만 읽고 앱은 안 읽는다 — 수집 단계에서 빠지면 산출물에 키가 없고 D4 보존 규칙이 NULL을 유지한다)
- MSG-398: 미션 목록 뷰포트 자르기·진행도 조회(`GET /api/missions/active`에 `type`·bbox 4종 필수 — `@Parameter(required=true)`+`@RequestParam(required=false)` 짝 5종, 검증 순서 WGS84 정의역→뒤집힘→0.5도 상한(12400/12401), `KoreaCoordinates`는 조회 창에 미사용(데이터 좌표 전용 기준선). type은 컨트롤러 파싱(EVENT·POPUP·COURSE만, 12402), `getActiveMissions()` **완전 제거** — 하위 호환 분기 없음, 전환은 FE 선행 배포(dev 접근 로그로 5파라미터 확인 후 develop 머지). 스냅숏이 `CachedMission(dto, type, bounds)`로 미션당 정수 사각형 동봉 — 코스는 `path` 점마다 `KoreaCoordinates` 검증 후 encode/decode min/max(범위 밖 점은 예외 없이 스팟 사각형 폴백+경고 1회 — `refreshLock` 안 전체 중단 방지), 그 외·폴백은 `mission_grids` decode. 교차 판정 pad = 투영 보정 1칸(중앙자오선 127.5° 품는 뷰포트의 남쪽 행 누락 보정, 실측 최대 이탈 29.6m) + `ceil(margin/100)`칸, `MissionViewportProperties`(margin-meters 3종 기본 0). 신규 `GET /api/missions/progress?missionIds=`(≤300, 초과 12403, 빈 입력은 DB 미접근 빈 배열) — `MissionRepository.findProgress` native: `findCompleted`와 **글자 단위 같은 videos 술어**(`status <> 'DELETED'`·`recorded_at` 기간 양끝 포함)로 LEFT JOIN, `LEAST(COUNT(DISTINCT v.grid_id), target_count)`+`BOOL_OR`(user_missions). 진행도 기준은 도감 점령→판정 술어로 2026-08-15 성민 확정 전환(D8 — 안 간 팝업이 옛 영상만으로 1/1 되는 문제). `MissionErrorCode` 12xxx 신설, `findActive`에 `ORDER BY m.id`, `CourseSeedReader.validatePath`에 좌표 범위 검증(위반 시 전량 거부). 실측: 웜 p95 4.0ms·콜드 첫 요청 211.8ms(재계산 ~208ms)로 **D1 판정선(300ms) 미발동** — 스케줄러·워밍업·readiness 미도입, lazy refresh 유지. 진행도 169건 p95 23.6ms, `idx_videos_user_created` user_id 선두 프로브 EXPLAIN 확인. 측정 스크립트 `load-test/measure-msg398.sh`)
- MSG-399: 미션 상세 조회(`GET /api/missions/{missionId}` — `MissionDetailResponseDto`(mission 중첩 재사용 + progress + videoCount + spotStats), 404는 `MISSION_NOT_FOUND(12404)`. 조립은 쿼리 5회 고정: findById → findByMissionIds → **MSG-398 findProgress 재사용**(상세용 SQL 복제 없음) → 신규 `MissionGridRepository.findVisitedGridIds`(진행도와 videos 조인 글자 단위 동일, DISTINCT) → 신규 `VideoRepository.countMissionVideosByGrid`(MSG-390 후보 술어 + GROUP BY, `MissionVideoCountProjection`). COURSE만 spotStats(seq ASC NULLS LAST·gridId ASC, 0 채움), 비COURSE 빈 배열. 기간 끝난 미션도 조회 가능(활성 조건은 목록 전용). `getMissionDetail`만 메서드 레벨 REPEATABLE_READ — 다섯 읽기 스냅숏 공유(Codex 지적 수용). 전역 캐시 미사용(사용자별 값). 정합 테스트가 MSG-390 목록 건수와 videoCount 동등을 페이지 크기 밖 데이터로 고정. EXPLAIN 실측: visited 5.1ms(`idx_videos_user_created`)·격자별 COUNT 6.7ms(`idx_videos_grid_popular` 부분 인덱스), 세부는 스펙 작업 로그)
- MSG-385: 팝업 판정 반경 40m 축소 — 마이그레이션·API·에러코드 **0**. `GridEncoder.radiusRange` 순수 추가(사방 r 열린 정사각형 걸침, 반열림 정합 `ceil-1` — Owner A 접점 유일), 시더 `judgeGrids`(`JUDGE_RADIUS_METERS=40`, 1·2·4칸)로 `insertMission`의 9×9 전개 대체, dedupe 경로에 `regrid` 차등 교체(교집합 유지·차집합 `deleteAll(엔티티)`·`saveAll`, 두 집합 공히 비면 무변경 멱등 — 좌표 변경 팝업도 같은 경로로 이동, PK flush 순서 함정 없음), 스냅샷에 없는 미종료 행(판정 UTC `ofInstant(clock.instant())`, 미래 시작 포함) 중 칸 수 > 4는 `centroidGrid`(체비쇼프 최근접, 동률 y→x asc) 1칸 축소 + 완전성 가드(`records*2 < notEnded`면 축소 통째 스킵 — 잘린 스냅샷의 일괄 오축소 방어), `SeedResult`에 `regridded`·`shrunk`. 축제·판정·조회 전 경로 무수정(`FestivalMissionSeeder` blob 동일 3회 확인). 테스트 격리는 `notEndedPopups` 심(서브클래스가 super 통과 후 스코프 제한 — 공유 DB 가드 오발동 방지). **dev 재산출 완료(2026-08-15)** — regridded 225·shrunk 0, 팝업 격자 약 1만 8천 → 722행(1칸 4·2칸 83·4칸 138), 활성 227 → 225(종료 정리 2), 0.5도 강남역 POPUP 169건 불변. 세부는 스펙 작업 로그
- MSG-437: 팝업·축제 핀 행정 단위 집계(`GET /api/missions/aggregation` — `type`(EVENT·POPUP만, COURSE는 12402)·`unit`(DONG/SIGUNGU/SIDO, 무효 12405 `INVALID_AGGREGATION_UNIT` 신설)·bbox 4종 전부 필수, 검증 순서 bbox 누락→type→unit→정의역·NaN→뒤집힘→`RegionUnit.maxSpanDeg` 상한(1°/4°/10°). 마이그레이션·시더·Owner A 코드 수정 **0** — `RegionUnit`·`ViewportBounds`·`RegionQueryService.resolveByPoint` 계약 소비만. `CachedMission`에 `RegionAnchor`(행정동 코드 10자리·전체 경로 이름·귀속점 위경도) 추가 — 재계산 시 EVENT·POPUP만 판정 사각형 중앙 셀 중심(`GridEncoder.center`)을 `resolveByPoint`(ST_Covers GIST 점 프로브, 건당 ~1.2ms)로 1회 판정하고 미션 id+gridBounds 키로 세대 간 메모이즈. **부트 웜업**(`@EventListener(ApplicationReadyEvent)` + `NOT_SUPPORTED` — 클래스 레벨 트랜잭션이 리스너를 감싸면 DB 장애 시 프록시 단계에서 기동이 죽어서, 후속 커밋)이 기동 직후 스냅숏을 선계산해 사용자 노출 콜드를 없앰. dev 실측(2026-08-20): 웜업 전 콜드 첫 요청 1.51초·웜 12ms, 활성 205건. 실패 분류: `Optional.empty`·6400만 무귀속(regionCode null), 인프라 예외는 전파해 스냅숏 미갱신(공유 스냅숏이라 COURSE 목록도 함께 실패 — 의도, 회귀 테스트 고정). 집계는 요청 시 쿼리 0번 메모리 산술: 접두 자르기 그룹핑 + `nameTokenIndex` 토큰 이름(split_part 미러, 토큰 부족 시 빈 문자열) + 귀속점 평균 좌표 + `missionIds` 오름차순 동봉(좁힘 계약 = 줌인 후 교집합). `MissionRegionAggregateResponseDto`(record), `AGGREGATABLE_TYPES`는 `MissionQueryService` 인터페이스 상수 단일 정의. 목록 조회 경로 동작 불변(새 필드 미사용). 신규 테스트 40건(귀속·메모이즈·부트 웜업 Mockito 12 + 집계 산술 @SpringBootTest 12 + API 경계 MockMvc 16), ADR 정본 위키 `04-decisions/ADR 미션 핀 클러스터링 스냅숏 메모리 집계.md`·cf-40042497)
- MSG-454: 상단 칩 비로그인 개방(`SecurityConfig`에 GET 한정 permitAll 5종 — `/api/hotzones`·`/api/missions/active`·`/api/missions/aggregation`·`/api/missions/{missionId:[0-9]+}`·`/api/missions/{missionId:[0-9]+}/videos`. 상세·영상은 **숫자 제약 변수 매처**라 미래의 한 세그먼트 경로(`/api/missions/summary` 류)는 기본 인증으로 남는다(fail-closed). `progress`는 어떤 매처에도 안 잡혀 `anyRequest().authenticated()` 낙하 — 익명 401 회귀 테스트 고정. `JwtAuthenticationFilter`·`PUBLIC_AUTH_PATHS` 무수정(MSG-443 규칙 — 토큰 실리면 principal 세움). 미션 상세 익명 분기: `getMissionDetail(long, Long)` userId null이면 `findProgress`·`findVisitedGridIds` 2조회 생략(5회→3회), progress null 값(키 유지, `@Schema(nullable = true)` — `ResponseSchemaNullabilityTest` 계약)·spotStats.visited 전부 false. 2026-08-14 "비로그인 미션 열람 폐기"(성민)의 번복 — 정본 `docs/prd/mission-map-explore.md` 8절 2026-08-22 항목(스크래핑 우려는 완화 아닌 수용). 신규 `MissionPublicAccessHttpTest` 5건 + 낡은 401 테스트 정리 3건, 계약 인터페이스·Flyway 변경 0)
- MSG-491: 비로그인 조회 개방 마무리(`SecurityConfig`에 GET 한정 permitAll 3종 + HEAD 1종 추가 — `/api/regions/*/grids`·`/api/regions/explore`·`/api/videos/{videoId:[0-9]+}`(숫자 제약은 fail-open 방어 — `*` 로 두면 이 하위에 붙는 한 세그먼트 GET 이 열거를 거치지 않고 공개가 된다, MSG-454 미션 상세와 같은 형태), 재생의 명시 HEAD 매핑은 GET 매처에 안 잡혀 별도 한 줄. MSG-454·467·469에 이은 마지막 잔여분으로 비로그인 지도의 조회 개방이 닫힌다, 정본 `docs/prd/mission-map-explore.md` 8절 2026-08-26). 동 격자 카드 목록은 `RegionExploreController.getRegionGrids`가 principal 파라미터 자체를 안 받아 매처만. **코드 변경 2곳**: (1) 재생 — `VideoService.getVideoPlayback(long, long)` → `(Long, long)`, `owner = userId != null && userId.equals(video.getUserId())`(양쪽 다 Long 이라 `==` 는 참조 비교가 된다 — Long 캐시 밖 id 에서 소유자 판정이 무너지고 `VideoBlindIntegrationTest` 가 잡았다)·`case FRIENDS -> userId != null && isFriend(...)`로 익명은 친구 판정 쿼리조차 0회. PUBLIC만 통과하고 PRIVATE·FRIENDS는 기존 `VIDEO_FORBIDDEN`("비공개 영상입니다")로 수렴해 응답이 타인 조회와 글자 단위 동일, 신규 에러코드 0. `incrementViewCount`는 `!owner` 조건이라 익명 재생도 +1(2026-08-26 확정 — 세지 않으면 인기순 표본이 로그인 쪽으로 기운다). (2) 전체 지역 목록 — `getExploreRegions(Long, String)`·`VideoRepository.getRegionExplorePage` 첫 인자 `Long`, 익명이면 `personal_regions` CTE(`ug.user_id = :userId`)가 0행이라 LEFT JOIN 전부 null → 정렬이 `grid_count DESC, region_code ASC`로 자연 낙하(별도 쿼리 없음, 비용 대부분인 `content_regions` 집계는 사용자 무관이라 속도 동일). `RegionExploreCursor.userId`를 `Long`으로 열어 익명 커서는 첫 자리가 빈 토큰(`:ts:cnt:code`), 대조는 `Objects.equals` — 익명·로그인 커서는 여전히 서로 안 섞인다. 컨트롤러 익명 전달은 레포 선례 형태(`principal == null ? null : principal.userId()`). 단일 격자 조회·수집률 stats·내 영상 목록·AI 경로 추천은 로그인 유지. `PUBLIC_AUTH_PATHS` 무수정. 검증 `AnonymousReadAccessHttpTest` +3건(개방 3종 200 + 서비스에 null 전달 verify, 재생 쓰기 메서드 401, stats 401)·`VideoPlaybackServiceTest` +3건(PUBLIC 재생·조회수, FRIENDS 403·친구 쿼리 0회, PRIVATE 403)·`RegionExploreQueryTest` +1건(익명 실 DB 정렬), 무효해진 익명 401 기대 2건 교체. 계약 인터페이스·Flyway·응답 계약 변경 0)
- MSG-469: 비로그인 지도 잔여 조회 개방(`SecurityConfig`에 GET 한정 permitAll 6종 추가 — `/api/zones`·`/api/search/places`·`/api/search/trending`·`/api/grids/*/cover`·`/api/grids/*/videos`·`/api/grids/*/hourly-uploads`. MSG-454·MSG-467에 이은 마지막 잔여분, 정본 `docs/prd/mission-map-explore.md` 8절 2026-08-25). 다섯은 principal 미사용이라 매처만, **장소 검색만 코드 변경**: `PlaceSearchService.searchPlaces`·`SearchKeywordCommandService.recordSearch` 첫 인자를 `long userId` → `String searcherKey`(로그인=사용자 id 문자열, 익명=`s:{세션}`, 집계 대상 아니면 null) 교체 — B(route) 소비 1인자 오버로드는 시그니처·동작 불변. `PlaceSearchController`가 `X-Viewer-Session`(64자 이하·공백 아님·콜론 불가)으로 익명 키 생성, 형식 이탈은 검색 200 + 집계만 생략. 익명 dedupe member `s:{세션}:{키워드}`(콜론 금지가 세션·검색어 경계 모호성 차단 — Codex 1R 적발), Lua·TTL 무변경(실제 만료는 날짜 키 마지막 접수 +26h). 단일 격자 조회·AI 경로 추천은 로그인 유지 확정. `ZoneController` 표시명 서버 계산 서술 보존(Codex 2R 적발 — zones는 격자 이름 재료가 아니라 검색바 구역 이동용). 신규 `AnonymousReadAccessHttpTest` 5건 + 확장 8건, 무효해진 익명 401 기대 6건 삭제(`my-videos` 401은 유지). video 패키지 main 무수정. Codex 스펙 4R·구현 1R 수렴)
- MSG-467: 행정동 조회 비로그인 개방(`SecurityConfig`에 GET 한정 permitAll 2종 추가 — `/api/regions/reverse-geocode`·`/api/regions/districts`. MSG-454와 같은 "상단 칩은 비로그인" 원칙의 잔여분(정본 `docs/prd/mission-map-explore.md` 8절 2026-08-24). 경로 열거가 계약 — `/api/regions/**` 로 넓히면 stats 4종 인증이 풀리고 그 넷은 principal null 가드가 없어 익명 NPE 500. `PUBLIC_AUTH_PATHS` 무수정. 자바 로직 변경은 `RegionController` 클래스 주석 갱신뿐, 응답 계약·계약 인터페이스·Flyway 변경 0. 신규 `RegionPublicAccessHttpTest` 5건 + 낡은 districts 익명 401 테스트 1건 삭제)
- MSG-493: 바다에 걸친 격자의 표시 이름 — 단일 격자 조회(`getCell`)에서 품는 행정동이 없으면(해안) `resolveNearestByPoint` 상한 오버로드(MSG-493 신설, 내부 `findNearestRegionWithin` — 2단 KNN 바깥 단 `WHERE distance <= :max`)로 **경계 3km 안이면** 가장 가까운 행정동 이름을 표시 재료로 채운다(3km 밖 바다 한가운데는 종전대로 빈 이름 — 지도 격자망은 아무 바다나 눌려 상한 없이는 90km 떨어진 동이 붙는다, 2026-08-27 정민 확정). **표시 전용** — `grids.region_code` 저장 라벨·수집률·탐험률 귀속·뷰포트/핫구역 응답 전부 불변(2026-08-27 정민 확정: 바다 격자 집계 귀속 안 함, 화면·집계 어긋남은 MSG-492 방식 수용, 범위는 격자 번호가 노출되는 단일 조회 하나). 공유 헬퍼(`resolveRegionName`)가 아니라 `getCell` 전용 `resolveRegionNameWithFallback` 신설 — 행사 위치 벌크(`resolveRegionNames`, MSG-439)로 폴백이 새지 않게(범위 가드 테스트). 마이그레이션·백필 0, 신규 쿼리문 1(`findNearestRegionWithin` — 기존 2단 KNN에 상한 필터만 더한 변형). FR-REGION-10 개정(구현됨→진행 중→구현), 기존 "무귀속=null" 테스트를 개정 요구로 교체
- MSG-450: 미션 집계에서 행사 영상 제외 — `videos` 를 직접 조인하는 미션 쿼리 여섯 곳에 `AND NOT EXISTS (SELECT 1 FROM event_videos ev WHERE ev.video_id = v.id)` 한 줄씩 추가(`MissionRepository.findProgress`·`findCompleted`, `MissionGridRepository.findVisitedGridIds` 는 조인 **ON 절**, `VideoRepository.findMissionVideos`·`findMissionVideosAfter`·`countMissionVideosByGrid` 는 WHERE 절). 진행도는 LEFT JOIN 이라 ON 절이어야 미진행 미션의 0 이 유지된다. 마이그레이션·API·DTO·에러코드·인덱스 **0** — `event_videos.video_id` 가 PK 라 행당 프로브가 최대 한 번이다(설계 근거). 로컬 EXPLAIN 으로 확인한 것은 PostgreSQL 이 NOT EXISTS 를 서브플랜이 아니라 `Nested Loop Anti Join` 노드로 재작성한다는 계획 형태뿐이고, 로컬은 `videos` 0행이라 실제 인덱스 사용과 비용은 dev 배포 후 확인 대상이다. 쿼리 1~3 의 "글자 단위 동일" 계약과 4~6 의 술어 동등 계약을 안티조인 포함으로 갱신(Javadoc 3곳 명문). 쓰기 경로의 판정 시점 가시성은 MSG-440 이 행사 업로드에서 미션 판정 훅 자체를 뺀 구조라(`confirmAndStore` 밖) 이미 성립. 신규 테스트 7건(쿼리 6건 + 진행도 안티조인의 ON 절 배치 회귀 가드 1건 — 오배치를 잡는 유일한 케이스라 돌연변이 실측으로 확인) + 공용 시드 헬퍼 `event/EventVideoFixtures`(event_series → occurrences → locations → location_grids → event_videos FK 사슬을 호출마다 새 회차로 생성)
- MSG-459: 축제·팝업 미션 대표 격자 업로드(V42 `missions.representative_grid_id` — CHECK `chk_missions_rep_grid_type`(EVENT·POPUP AND target_count=1)·DEFERRABLE 복합 FK(`mission_grids`)·부분 인덱스·홀수 직사각형+2이하 변 백필(리졸버 동치를 마이그레이션 파일 실행 대조 테스트로 고정). `RepresentativeGridResolver`·`AreaCell`을 `event.seed` → `global.geo` 이동(로직 무변경, 행사·미션 공용). 시더: `MissionRepresentativeGrids` 신설(재산출 규칙 한 곳 — target_count=1이고 비었거나 새 집합 밖일 때만, null 가드), 축제는 저장 격자 재사용·팝업은 regrid/shrink 직후 재산출+경고 로그. **업로드** `POST /api/missions/{missionId}/videos`(기존 `video.controller.MissionVideoController`에 POST 추가 — 신규 클래스는 빈 이름 충돌, `mission/service/MissionVideoService`+Impl `@Transactional` 판정 7단: 값싼 검증(3424·3401 선행)→미션 조회→멱등 재시도(소유자+격자+저장 recorded_at 기간+**스탬프 보유** 4중 대조 — 행사 경로 확정 키 오인 차단)→판정 5종→`confirmAtGrid`→`awardOnUpload`→**보존 확인**(스탬프 부재 시 12409 롤백 — 종료 정각·재시딩 경합 창 폐쇄, FR-17 무조건 성립). 실패는 `12409 MISSION_UPLOAD_UNAVAILABLE` 단일 수렴(존재 은닉, D-10), 응답 뱃지는 코어+미션 판정 합류(saveVideo 동형). `VideoService.validateUploadRequest` 공개(`validatePendingKey` 추출). **역조회** `GET /api/grids/{gridId}/missions`(비로그인 GET 한정 매처, 대표 격자 컬럼 직조회 — 캐시 우회, 정렬 진행 중→시작 전 임박순→종료 최근순, `VideoRepository.countVideosByMissionIds` 배치 — 상세 videoCount와 술어 글자 동일). SRS FR-MISSION-21~26 등재·rtm 공백 0. 계약 인터페이스 시그니처 변경 0. 신규·갱신 테스트 약 60건(업로드 35·역조회 8·마이그레이션 6·시딩 델타·보존 롤백 실커밋 3). Codex 3라운드 수렴(재시도 스탬프 가드·뱃지 응답 합류 2건 적발 반영))
- MSG-492: 코스 포토스팟 표시 이름(V43 `mission_grids.name VARCHAR(100)` nullable·인덱스 없음 — **저장값은 최종 표시 문자열**이고 이름 출처는 저장하지 않는다). 화면이 스팟마다 `GET /api/regions/reverse-geocode` 를 부르던 것을 응답 필드로 대체 — 산출물이 이미 갖고 있던 명소 이름 417개를 파서가 버리고 있었고(`CourseRecord.Spot` 주석 "미적재"), 나머지는 역지오코딩이 바다에서 빈 값을 줘 "이름 없는 경유 지점"이 떴다(해파랑길 1코스 7곳 전량). **판정은 전부 적재 시점**(D-2 초안 번복 — 조회 시 계산이 메모·TTL·목록/상세 세대 불일치 연쇄를 부른 것을 Codex 2R 에서 확인) — `CourseSpotNameResolver`(빈 아님, 시더 실행당 1회 `new`) 가 ① 산출물 name(`method=tourapi` 만) → ② `ZoneNameResolver` 구역 표시명 → ③ `resolveByPoint` 행정동 **마지막 토큰** → ④ 최근접 행정동 → ⑤ null 순으로 굳혀 저장하고, `MissionQueryServiceImpl.pathShape` 는 `grid.getName()` 통과만 한다(조회 쿼리·의존 증가 0, 스팟 수 무관 문장수 가드로 고정). `"경유점 N"` 자리표시는 파서가 버린다(D-1) — 저장하면 사다리 ① 에서 걸려 폴백이 영영 안 돈다. 시더 skip 분기에 이름 갱신을 얹어 **재실행이 곧 백필**(MSG-383 형태, `name` 값만 변경 — `@DynamicUpdate` 가 없어 UPDATE 문에는 `seq` 도 실리지만 같은 트랜잭션에서 방금 읽은 값이다. 격자 집합·seq·target_count·path 불변). **Owner A 계약 1건 추가**: `RegionQueryService.resolveNearestByPoint`(신규 — `resolveByPoint` 의 `Optional.empty` = "품는 행정동 없음" 신호를 기존 소비처가 쓰고 있어 폴백을 안으로 넣지 않고 분리). 그 native 는 **2단 구조**다 — `ORDER BY 거리, region_code` 로 쓰면 GIST KNN 인덱스를 통째로 잃어(dev 실측 2026-08-27 Seq Scan 1,666ms 대 Index Scan 13ms, 128배) 안쪽에서 거리만으로 `LIMIT 8` 을 인덱스로 뽑고 바깥에서 동률만 `region_code` 로 가른다(Index Scan + Incremental Sort 20.7ms, 실제 읽은 행 2). 거리 상한 없음(D-5 — 대상이 실측 고정 집합이고 최댓값 2,189m, 두면 7칸이 이름 없이 남아 FR-1 을 스스로 깬다). **`grids.region_code` 라벨링 경로는 무접촉** — 동별 수집률의 분모라 파급이 달라 MSG-493 으로 분리(바다 격자 업로드가 어느 동에도 안 잡히는 문제). glossary "표시명" 에 코스 스팟 예외 등재(서버 조립·최근접 폴백은 이 경로만). 신규·갱신 테스트 27건(리졸버 9·파서 6·시더 5·계약 4·PostGIS 3). Codex 2라운드 수렴 — 다만 **인덱스 미사용은 Codex 도 못 잡았다**(박스에 `psql` 없어 DDL 근거만, 스스로 유보 명시). SRS FR-MISSION-19 는 앞 문장만 충족이라 `진행 중`(뒤 문장 "정식 명소가 아님을 구분" 은 D-3 으로 비목표 확정)
- MSG-500: 승인 미션 등재와 노출 숨김 — **V51** `missions.hidden_at`(NULL=노출 중, 기존 행 전부 NULL이라 시드 산출물 동작 불변). `service/MissionRegistrationService`(+Impl, 신설) — 요청 경로에서 미션을 만드는 첫 문이다(지금까지 생성자는 기동 1회 시더뿐). event 승인 서비스가 mission 리포지토리를 직접 쓰지 않고 이 계약을 거치므로 **미션의 규칙이 전부 구현 쪽에 있다**: KST 날짜 라벨 → UTC 순간 변환은 `FestivalMissionSeeder.toUtcStart/toUtcEnd`를 **public 승격해 그대로 호출**(규칙 두 벌이면 시드 미션과 승인 미션의 하루 경계가 다르게 잘린다), 대표 격자는 `MissionRepresentativeGrids.reassignIfOutside`(같은 이유로 public 승격), `source="ORG_SUBMISSION"` 상수는 등재 서비스 보유(시더 정리·dedupe가 전부 source 한정이라 승인 미션이 시더 파이프라인과 절연), `sourceKey`=신청 번호(`(source, source_key)` 부분 유니크가 이중 승인의 DB 백스톱), 공개 주소 조립은 키를 받아 `AwsProperties.publicUrl`. `MissionQueryService.invalidateSnapshot()` 신설 — TTL 1시간 전역 스냅숏이라 무효화가 없으면 승인 미션이 최대 1시간 안 실리고 중지 미션이 최대 1시간 계속 보인다. register·hide 둘 다 **커밋 후**(afterCommit) 호출하고 동기화가 없으면 즉시 실행 폴백(`EventSubmissionImageStore` 패턴). **숨김 소비**(전수 grep 후 확정): 술어 추가는 `MissionRepository.findActive`(칩·뷰포트·클러스터·지역 집계가 전부 이 스냅숏 위 메모리 필터라 여기 하나로 닫힌다)·`findAwardCandidateIds`·`findCompleted`·`findByRepresentativeGridIdAndHiddenAtIsNullOrderById`(개명) + `VideoRepository` 4곳(`findMissionVideos`·`findMissionVideosAfter`·`countMissionVideosByGrid`·`countVideosByMissionIds`), 미션 상세·미션 경유 업로드는 서비스 가드(각각 12404·12409 수렴), 영상 피드는 **빈 200이 아니라 미션 상세와 같은 404**(`VideoRepository.isMissionHidden` 스칼라 조회 — 빈 페이지면 "존재하지만 비어 있다"로 읽혀 은닉이 갈라진다. `VideoServiceImpl` 생성자에 의존을 더하면 테스트 12곳이 함께 깨져 리포지토리 스칼라 조회로 내렸다, `UserRepository.findAllS3KeysByUserId` 선례). 술어를 **일부러 안 건 곳**: `findProgress`·`findVisitedGridIds`·`countMyStampsByType` — 이미 완료한 사용자의 진행 기록과 뱃지 지표는 회수하지 않는다(종료 미션도 진행도가 나오는 D11과 같은 이유). `missions.hidden_at` 세팅은 단일 컬럼 `@Modifying` UPDATE(`hidden_at IS NULL` 술어가 재중지를 0행으로 만들어 첫 중지 시각 보존) — 더티 체킹이면 `@DynamicUpdate` 부재로 전 컬럼 UPDATE가 나가 시더 백필과 되덮기 경합이 생긴다
- **실적재 현황**: 축제·팝업·코스 **dev 적재 완료**(2026-08-14). 활성 기준 축제 222(이미지 221)·팝업 227(이미지·소개문 227)·코스 148(이미지 137)이고 local 도 같은 값이다. 재실행 멱등 확인(2회차 적재 0·갱신 0·정리 0), 외부 도메인 섞임 0, 코스 판정 격자 989 불변, 고아 스탬프 0. 팝업이 345 → 227 로 준 것은 MSG-235 가 설계한 종료 정리다(스탬프 없는 종료분만 삭제). **prod 는 전 종류 미착수** — `missions/*` 공개 읽기 정책이 dev 버킷에만 적용됐고 prod 앱이 미가동이다

### `event` (Owner B · 구현 강정민) — 🟡 부분
- MSG-438: 행사 도메인 데이터 모델·시드·대표 격자(V39 5테이블 — `entity/{EventSeries,EventOccurrence,EventStatus,EventLocation,EventLocationType,EventLocationGrid(+Id),EventVideo}`. EventVideo는 videos 1:1 확장(`@MapsId` 공유 PK, 파일 컬럼 없음 — 신고·처리·소프트 삭제·노출 게이트 전부 기존 재사용). 격자 단일 귀속은 `UNIQUE(event_occurrence_id, grid_id)` DEFERRABLE + 복합 FK, 대표 격자∈영역은 지연 FK. 상태·uploadClosesAt는 저장 안 함 — `statusAt(now)` 반개구간 파생(UPCOMING→LIVE→UPLOAD_GRACE(+30일)→ARCHIVED), `visible_from = starts_at - 14일` DDL CHECK 등식. `repository/` 5종(native는 `acquireEventWriteLock` advisory lock 하나(MSG-442 가 종료 구독 정리에 같은 키를 공유하며 개명), 잠금 계약 `findWithLockByLocationKey` — Hibernate 실측 `FOR NO KEY UPDATE`, 440 업로드가 같은 파생 쿼리 필수). `seed/{AreaCell,RepresentativeGridResolver(3단 결정: 홀수 직사각형 정중앙→지정→중심 최근접, 정수 산술+남서 동률),EventSeed,EventSeeder}` + `seed/events.json` 첫 시드 3행사 7위치(BIFF·부산불꽃·서울불꽃, BTS급 등재 기준), 플래그 `fillmap.event.seed.enabled` dev·prod on·`@Order(30)`. 테스트 45건 — 멱등은 `pg_stat_xact_user_tables` 행 변경 델타 0, 동시성은 `pg_blocking_pids` 결속, 지연 제약은 `pg_constraint` 직접 단언, 기동 진입점은 프록시 `run()` 전량 롤백. Codex 4라운드 수렴. 440 배포 전 미션 안티조인 6쿼리 필수(MSG-450))
- MSG-439: 행사 조회 API 4종 — `GET /api/event-occurrences`(뷰포트 칩 — bbox 4파라미터 `toBounds` 직접 검증(mission 패턴·`ViewportBounds` 재사용), `viewportRange` 4꼭짓점+사방 1칸 보정, 후보 전량 조회 후 자바 필터, 정렬 cityName→startsAt→id) · `/{id}`(상세 — `statusAt(now)` 파생·`uploadClosesAt` 파생·`notificationOn`(MSG-442 배선 후 파생값 — 구독 행 존재 AND 상태가 UPCOMING·LIVE)·`previousOccurrences` 같은 시리즈 `startsAt <` 최신순) · `/{id}/locations`(위치 목록 — `display_order`→id 정렬, `videoCount`는 `JOIN videos` ACTIVE+PUBLIC+READY 단일 GROUP BY·미집계 0) · `GET /api/grids/{gridId}/event-locations`(격자 역조회 — 일치 조회 우선이라 불량 문자열은 표시명 판정 전 빈 배열, 정렬 LIVE>UPCOMING>UPLOAD_GRACE>ARCHIVED·UPCOMING만 startsAt ASC·id DESC·locationId ASC). **존재 은닉**: `UPCOMING && visibleFrom > now` 회차는 4 API 공통으로 목록 제외·id 대입도 13404(`isVisible` 판정 하나 공유 — 칩 경로만 `findByVisibleFromLessThanEqual` SQL 별도 표현, DDL CHECK 등식으로 동치). **비로그인 GET 공개**(2026-08-20 사용자 확정): `SecurityConfig` `requestMatchers(HttpMethod.GET, 4패턴 정확 나열)` — 쓰기(440·441·442)는 로그인 유지. 표시명 재료 위치 단위 동봉(`zoneName`·`zoneCell`·`regionName` — `ZoneNameQueryService.resolver()` + `GridQueryService.resolveRegionNames` 각 요청당 1회). `exception/EventErrorCode`(13400·13401·13404) · `service/EventQueryService`(+impl, Clock 주입) · `dto/*ResponseDto` 4종 · `controller/{EventOccurrenceController,EventLocationController}`. 테스트 50건(서비스 36 — 뷰포트 11·역조회 10·위치 9·상세 6, 컨트롤러 10, 공개 정책은 필터체인 실검증 4). Codex 교차 리뷰 1R 수렴·실서버 스모크 통과
- MSG-443: 행사방 열람 인원 집계 — `POST /api/event-occurrences/{id}/heartbeat`·`GET .../viewer-count`(`EventViewerController`, **둘 다 permitAll — JwtAuthenticationFilter.PUBLIC_AUTH_PATHS엔 미등록**(등록하면 Bearer 파싱이 건너뛰어져 로그인 사용자가 익명 집계됨, 스펙 D4)), `EventViewerService`(+Impl — Redis Sorted Set `eventviewer:{occurrenceId}`, member `u:{userId}`/`s:{세션헤더}`, score epoch초, Lua 원자: 배타 청소→ZADD GT→EXPIRE 120, 조회 ZCOUNT 90초 포함 경계, Clock 주입), `EventViewerCountResponseDto`(viewerCount — 0=빈 방 표시·null=집계 장애 숨김). DB·Flyway·계약 인터페이스 0. occurrenceId 존재 검증 없음(438 테이블과의 연결은 후속 — 없는 id도 키 TTL로 자연 소멸)
- MSG-440: 행사 영상 업로드·위치별 피드·상세 — `POST /api/event-occurrences/{oid}/locations/{lid}/videos`(촬영·갤러리 공통 확정, 좌표·제목·설명·visibility 없음, PUBLIC 고정. 판정 순서: 존재 은닉 13404 → `findWithLockById`(PESSIMISTIC_WRITE)+회차 정합 13405 → `entityManager.refresh(occurrence)`(잠금 대기 중 리시드 스테일 방지) → **멱등 replay 선행**(s3Key가 멱등 키 — `findFirstByOriginalS3KeyStartingWith` 클레임 조회, 같은 사용자·같은 위치면 200 재응답: videoId·gridId 저장 행·processingStatus 실제 값·occupied=false·newBadges=[] 고정. 보장 경계는 교체·삭제 전까지) → 허용 창 판정은 `EventLifecycleGuard.checkUploadOpen`(442 공용 가드, 13410/13409 — 레인 조정 통일로 인라인 술어 대체) → `VideoService.confirmAtGrid`(공용 코어 `confirmAndStore` — grids lazy insert·videos INSERT·S3 복사·점령 upsert·뱃지·스트릭·핫스코어·인코딩 afterCommit 전부 동일, **미션 훅만 코어 밖이라 미호출**) → `event_videos` INSERT 단일 트랜잭션) · `GET .../videos`(keyset 피드 — 노출 술어 ACTIVE·PUBLIC·READY가 `countVisibleByLocationIds`와 글자 단위 동일, `EventVideoCursor` locationId 바인딩 불일치 13402, createdAt·id 내림차순, size 1~50 클램프 기본 20) · `GET /api/event-videos/{id}`(+명시 HEAD 부수효과 0 — 술어 밖·비행사 13406, blurred 우선 재생, 비소유자 view_count +1, zoneName·zoneCell·regionName·uploaderNickname·interactionLocked(`now>=endsAt`)). `setVisibility`는 event_videos 행이면 3427 거절. SecurityConfig GET 2패턴+HEAD 1패턴 permitAll(POST는 로그인). Flyway 0·계약 변경 0. 테스트 47건(동시성 2 — 시더 경합 드리프트·refresh 뮤테이션 검증). Codex: 스펙 4R + 구현 1R 수렴(CORS HEAD 미채택 — VideoController 의도된 이중 방어 선례)
- MSG-442: 행사 생명주기 가드·알림 구독·스케줄러 — **구독**: V40 `event_notification_subscriptions`(복합 PK(user_id, event_occurrence_id)·양방향 FK CASCADE·`created_at` DEFAULT `statement_timestamp() AT TIME ZONE 'utc'`(V33 형상 — 시작 알림 대상 선별 재료라 세션 TZ 스큐 불가)·회차 단위 조회용 보조 인덱스) **행 존재 = ON**(opt_outs의 거울상). `PUT /api/event-occurrences/{id}/notification`(`EventNotificationController` — 행사 permitAll이 GET 한정이라 별도 등록 없이 인증 대상), `EventNotificationService`(+Impl — native ON CONFLICT insert·DELETE 멱등, 종료 회차 ON은 13422 거절·OFF는 상태 무관 성공, 노출 전 예정 회차는 `EventOccurrence.isVisibleAt(now)` 공유 술어로 13404 은닉). **노출 상태는 파생값** — 구독 행 존재 AND 상태 UPCOMING·LIVE라 종료 시각부터 정리 틱을 안 기다리고 즉시 OFF. `isSubscribed(userId, occurrenceId, now)` 오버로드로 439 헤더가 자기 `now`를 넘겨 status와 판정 시각을 일치시킨다. **가드**: `service/EventLifecycleGuard`(`checkUploadOpen`·`checkInteractionOpen`, `EventErrorCode` 13410 UPLOAD_NOT_STARTED·13409 UPLOAD_CLOSED·13422 INTERACTION_LOCKED, 전부 409 — 업로드 두 코드는 440·442 레인 조정으로 440 번호·메시지에 통일(2026-08-21)). 시각 비교식 없이 `statusAt`에 위임 — **업로드 경로는 통일 커밋에서 배선 완료**(440 인라인 창 판정을 `checkUploadOpen` 호출로 교체 — replay 판정 뒤 같은 자리·같은 반개구간), 댓글·도움돼요 배선은 MSG-441. **스케줄러**: `config/EventSchedulingConfig`(조건 없는 `@EnableScheduling` — 기존 두 곳이 다 플래그 게이트 안이라 기본값 환경에선 인프라가 안 뜬다), `service/EventNotificationScheduler`(**빈 무조건 등록**, `fixedDelay`+`initialDelay` 5분 프로퍼티, 정리는 발송 게이트 밖 — 게이트하면 구독 행 무한 축적). 한 틱 2단계: ① 시작 알림(발송 창 24h·`created_at <= starts_at`·키 `EVENT_START:{occKey}:{startsAt epoch}`) ② 종료 구독 벌크 해제(`endsAt <= now` 서브쿼리 DELETE). **두 단계 모두 `acquireEventWriteLock` 선취 트랜잭션 안**(낡은 스냅숏 방어 — 락 없으면 연기된 행사에 옛 키 시작 알림이 나가고 연장된 회차 구독이 전멸한다. 대가로 실패 격리가 사용자→틱 단위, ON CONFLICT라 정상 중복은 비예외). **시더 훅**: `EventOccurrence.scheduleRevision`(시각 변경 시에만 +1) + `EventSeeder`가 종료 경계 지난 회차 터치 시 구독 선삭제 후 갱신, 개정이 오르면 구독자에게 `EVENT_SCHEDULE:{occKey}:{revision}` 기록(시딩 트랜잭션 안 = outbox 원자성). 키가 시각값이 아닌 이유는 일정 왕복 A→B→A→B의 둘째 변경이 dedupe에 억제되기 때문. `acquireSeedLock` → `acquireEventWriteLock` 개명(키 불변 — 시더·정리·발송 3자 공유가 설계의 핵심). 신규 39건(구독 서비스 7·컨트롤러 4·저장소 5·가드 6·스케줄러 9·시더 훅 8 — advisory lock 직렬화 2건은 `pg_blocking_pids` 실결속) + 기존 클래스 편입 6건(컨슈머 2·설정 1·OpenAPI 스키마 1·439 헤더 2). reviewer 2R + Codex 2R 수렴, 수정마다 돌연변이 검증 동반
- MSG-441: 행사 영상 댓글·도움돼요 — V41 두 테이블(`event_video_comments` BIGSERIAL PK·content VARCHAR(500)·`created_at` NOT NULL DEFAULT `statement_timestamp() AT TIME ZONE 'utc'`(V33·V40 형상)·인덱스 `(video_id, id)` keyset 겸 집계·`(user_id)` 탈퇴 CASCADE / `event_video_helpfuls` 복합 PK`(video_id, user_id)` — **video_id 선두**라 영상별 집계가 PK 인덱스를 타고(user_grids·likes 의 user_id 선두와 반대 축), 별도 `(user_id)` 인덱스는 탈퇴 CASCADE 용. 둘 다 videos 가 아니라 **event_videos 참조**라 행사 영상이 아닌 영상에 반응이 붙는 상태를 DDL 이 막는다). **집계 컬럼 없음** — 조회 시점 실측(FR-EVENT-02 선례, 카운터는 탈퇴 CASCADE 를 지나쳐 드리프트). `entity/{EventVideoComment,EventVideoHelpful,EventVideoHelpfulId}` — 댓글 `createdAt` 은 **정적 팩터리가 서비스의 now 를 µs 절단해 대입**(JPA save 가 created_at 을 INSERT 컬럼 목록에 넣어 DEFAULT 가 안 걸린다 — null 이면 전량 NOT NULL 위반. `@CreationTimestamp` 는 JVM 기본존 +9h 라 배제, Report 선례), 도움돼요는 native INSERT 경로라 DEFAULT 가 실저장값. `repository/{EventVideoCommentRepository,EventVideoHelpfulRepository}`(+`EventVideoCommentRow`·`EventVideoReactionCount`) — 목록은 **세타 조인 + 생성자 프로젝션**(`FROM EventVideoComment c, User u`, Report 선례. 닉네임 비정규화 사본 없음), 쓰기 2종은 native 1문장(`INSERT ... ON CONFLICT DO NOTHING`·DELETE — `deleteById` 는 SELECT+DELETE 2문장이라 동시 취소에서 진 쪽이 StaleStateException 500), 파생 쿼리는 **프로퍼티 경로 밑줄 표기**(`countByVideo_VideoId`·`countById_VideoId` — EventVideo 식별자가 `videoId`, 도움돼요는 `@EmbeddedId id` 라 밑줄이 없으면 기동 시 리포지토리 초기화 실패). `EventVideoRepository.findVisibleWithOccurrence`(JOIN FETCH 위치·회차 + ACTIVE·PUBLIC·READY — 기존 카운트·피드 쿼리와 **동등 술어**, 반응 6경로가 이 하나로 대상을 연다). `service/{EventVideoInteractionService,Impl}`(+내부 record `EventVideoDetailReactions`·`EventVideoReactionCounts`) — **판정 순서가 계약**: 노출(13406) → `EventLifecycleGuard.checkInteractionOpen(occurrence, now)`(13422) → 댓글 존재·소속(13407) → 소유자(13403). 가드가 소유자 검증보다 **앞**이라 종료 행사에선 자기 댓글·남의 댓글 응답이 같다(순서가 반대면 잠금이 권한 문제로 읽힌다). MSG-442 가드의 **첫 소비처**(정의만 있고 호출자가 없던 상태 해소). 조회 3경로(목록·상세·피드)엔 가드 호출이 없다(FR-14 구조적 보장). `controller/EventVideoInteractionController` 5+1 엔드포인트(`POST|GET /api/event-videos/{id}/comments` · `PATCH|DELETE .../comments/{cid}` · `PUT|DELETE .../helpful`) — 도움돼요가 POST 가 아니라 **PUT/DELETE 인 것이 멱등의 표현**, 댓글 삭제만 비멱등(재삭제 13407 — id 가 화면 실참조라 화면 상태 불일치를 감추지 않는다). SecurityConfig 는 GET `"/api/event-videos/*/comments"` 한 줄 추가(기존 `"/api/event-videos/*"` 는 세그먼트 하나만 매치. **GET 한정이 계약** — 문자열 패턴이면 같은 경로 댓글 작성 POST 가 익명에 풀린다). 커서 `EventVideoCommentCursor`(`{videoId}:{commentId}` Base64URL, 정렬 키 `id ASC` 단일이라 시각 성분 없음, 발급 영상 바인딩 불일치 13402). 상세 DTO +4(`helpfulCount`·`helpfulByMe`·`commentCount`·`comments` 첫 20건) / 피드 카드 DTO +2(`helpfulCount`·`commentCount`, 카드엔 토글이 없어 helpfulByMe 미포함) — 상세와 피드가 **같은 원천을 부가 술어 없이** 세므로 두 화면 수가 갈릴 수 없다(US-006). 피드 집계는 페이지 videoId 집합 group by 2회로 항목 수와 무관하게 상수(N+1 회귀 가드 테스트 동반). `interactionLocked` 판정을 `!now.isBefore(endsAt)` → `statusAt` 축(**ARCHIVED 하나**)으로 교체 — 표시와 집행이 같은 정본을 본다(기존 테스트 교체). **2026-08-21 정책 번복**: 댓글·도움돼요 잠금이 종료 정각 → **아카이브 전환 정각(종료 + 30일)** 으로 옮겨져 유예 기간이 열렸다(유예는 "나중에 올리기"용인데 그 영상이 반응을 못 받으면 목적과 결과가 깎인다 — 기록 확정 시점이 아카이브로 이동, 업로드 창과 반응 창이 `시작~종료+30일`로 정렬). 가드 조건에서 `UPLOAD_GRACE` 제거 + 표시 파생 동기화 + 테스트 3종(441 잠금·442 가드 판정표·상세 경계) 갱신, PRD §4.2·SRS FR-EVENT-10·glossary 동반 정합. `EventVideoServiceImpl` 이 반응 서비스를 **읽기 목적으로 주입**(단방향 — 반응 서비스는 상세·피드를 모른다). `EventErrorCode` +2(13403 EVENT_COMMENT_FORBIDDEN·13407 EVENT_COMMENT_NOT_FOUND — 권한 실패를 404 로 뭉개지 않는 건 남의 댓글이 목록에 이미 공개된 존재라서, VIDEO_FORBIDDEN 3403 과 같은 결). 신규 테스트 32건(저장소 6·댓글 11·도움돼요 6·동시성 2·잠금 7) + 기존 확장 9건(상세·피드 합류 7·인증 2). reviewer 계약 8항목 통과·Codex 지적 0건
- **없는 것**: 댓글·도움돼요 API(441)와 그 경로의 상호작용 잠금 배선(13422 — 가드는 442에 있음), 익명 heartbeat rate limit(공개 근사값이라 부풀리기 수용 — 스펙 D4)

- MSG-501: 행사 운영자 콘솔 승인 이벤트 목록 — `GET /api/org/events`(`controller/OrgEventController` 신규, city·name 선택 파라미터), `EventQueryService.getApprovedEvents(String, String)` 추가(+Impl — 후보는 `findByEndsAtAfter(now)` 파생 쿼리 하나로 DB에서 거르고(D-1 `ends_at > now`, `statusAt` 미경유), `visible_from` 미적용(D-2 — 콘솔은 노출 전 예정 회차도 노출), 필터·검색(대소문자 무시 부분 일치)·시도별 집계(전체 기준 — totalCount·cityCounts는 필터 무관)·정렬(startsAt→id, 건수 내림차순→이름)은 자바에서. placeLabel은 기존 `findWithOccurrenceByOccurrenceIdIn` 재사용(fetch join, displayOrder→id 정렬의 첫 행 — 승인 흐름산 name 채움 규칙은 MSG-500 접점, 스펙 명시). 쿼리 회차 1 + 위치 일괄 1 고정(빈 후보면 1회). DTO 3종 `Org*ResponseDto`(placeLabel은 required+`@Schema(nullable=true)` 병기 — ResponseSchemaNullabilityTest 규약). SecurityConfig·Flyway·developCode·계약 인터페이스 전부 무변경(/api/org/** matcher가 MSG-496에서 기확정). 테스트 25건(서비스 17·컨트롤러 5 — ORG 200 실경로 포함·리포지토리 3), 전부 `// 검증: FR-EVENT-16`. reviewer 1R(P2 용어 1건 수정)·Codex 1R 수렴(지적 0)
- MSG-498: 행사 운영자 행사 등재 신청 — `event.submission` 하위 패키지 신설(V49 `event_submissions`·`_locations`·`_location_rects`·`_status_history` 4테이블 + 신청 번호 전역 시퀀스, 이력 테이블 CHECK가 REJECTED 행에만 사유 강제 — 반려 사유 저장 원천은 이력 하나 D-3). API 5개 전부 `/api/org/event-submissions`(SecurityConfig 무변경 — 496 matcher·497 mustChange 인터셉터 자동 커버, 인가 테스트는 전제 확인 4건): presign(프로필 미러링, jpg·png 10MB, webp 불허) · 제출 POST(유형 FESTIVAL·POPUP 2종, 유형 밖 필드 13439 거부, 81칸은 사각형 **합집합** 판정(전개 전 단일 사각형 long 선판정으로 OOM 차단), 격자 인덱스 상한 상수는 `RepresentativeGridResolver.GRID_INDEX_UPPER_EXCLUSIVE` 승격 공유(시더 사본 제거), 대표 격자 서버 계산, 신청 번호 `FM-{KST연도}-{4자리}` native `nextval` 채번) · 내 목록(상태별 건수는 별도 쿼리 없이 로드 목록에서 groupingBy 파생 — 스냅숏 불일치 차단, 페이지네이션 도입 시 되돌릴 지점) · 상세(존재 은닉 13430 — 모든 조회가 `findByIdAndUserId` 단일 경로, rejection은 이력 최신 행) · 재제출 PATCH(REJECTED 한정, 유형 불변 전체 교체, `@Modifying(clearAutomatically=true)` 조건부 UPDATE 술어에 userId 포함 → 0행이면 소유 조회 분기(부재 13430/비REJECTED 13434), 재로드는 UPDATE 뒤, imageS3Key 생략=유지). 이미지 확정: HEAD 실측(0바이트 13436)→original 복사(uuid 시도마다 신규 — 객체 공유 차단)→롤백 보상 삭제는 **복사 호출 앞 등록**(응답 유실 고아 차단)→커밋 후 pending 삭제(재확정 차단)·STATUS_UNKNOWN엔 미삭제. 엔티티는 다대일 양방향(자식 `@ManyToOne` 주인 + mappedBy 컬렉션 + 편의 메서드, D-12 — 단방향 왕복 실측 후 주류 관행으로 확정). 리스트 요소 null은 type-use `@NotNull`로 공통 400. errorCode 13430~13439(EventErrorCode 확장). 테스트 58건(단위 25·통합 33, 동시 재제출·복사 응답 유실·게이트 커버 포함). reviewer 3R·Codex 3R 수렴(채택 3건 반영, 멱등 키는 스펙 3회+구현 3회 기각 — 콘솔 수동 제출 수용). 심사·승인(APPROVED 쓰기)은 MSG-500 몫, pending S3 라이프사이클 규칙은 배포 전 런북
- MSG-502: 행사 운영자 이벤트 참여형 신청 — **신규 엔드포인트 0개**. MSG-498의 신청 API 5개가 `EVENT` 유형을 받도록 확장(**V50** `event_submissions` +`parent_event_occurrence_id`(FK `event_occurrences(id)`, 인덱스 없음 — 부모로 거는 조회가 없다)·+`participation_method` TEXT, `chk_event_sub_type` 재정의(FESTIVAL·POPUP·**EVENT**), `chk_event_sub_parent` 신설 — `(type = 'EVENT') = (parent_event_occurrence_id IS NOT NULL)` 로 유형과 부모의 짝을 DB가 강제(MSG-500 approve가 이 불변식 위에서 부모를 역참조)). `EventSubmissionType.EVENT` 추가(주석 필수 — glossary 이벤트 카테고리이고 `MissionType.EVENT`(축제 미션)와 이름만 같고 반대편). 엔티티는 부모를 **연관 없이 id 보관**(읽는 값이 상세의 title 하나, userId 선례)이고 `submit(...)` 인자로만 세팅 — 유형처럼 재제출로 바뀌지 않는다(D-3). 검증은 기존 `validateForm` 을 **삼분기 재작성**: "값이 있다 == 그 유형이다" 등식 3개(programDescription·operatingHours·participationMethod) + 부모 존재 등식이라 유형이 늘어도 줄 하나씩 는다. **부모만 폼이 아니라 별도 인자**(재제출 DTO에 부모 필드가 없다) — 제출은 요청 본문 값, 재제출은 **저장값**이 같은 메서드로 들어가 종료만 재검증된다. 대표 위치 정확히 1곳(D-2)은 `buildLocations` 시그니처를 건드리지 않고 **검증 순서로** 해결(validateForm 이 먼저 도는 자리에서 2곳 이상 13439, 0곳은 손대지 않고 기존 13431로 흘려보냄). `EventErrorCode` +2 — 13440 PARENT_EVENT_NOT_FOUND(404, **존재 은닉 없음** — 승인 이벤트 목록이 ORG 전원에게 같은 전량을 보여줘 회차 존재는 비밀이 아니다)·13441 PARENT_EVENT_CLOSED(409, `endsAt <= now` — MSG-501 노출 조건 `endsAt > now`의 정확한 여집합이라 종료 정각에도 목록과 신청이 갈리지 않는다, Clock 주입 판정). 상세 응답 +2(`participationMethod`·`parentEvent`{occurrenceId,name} — name 원천은 `event_occurrences.title`, 다른 유형은 둘 다 null). 기간과 부모 기간의 교차 검증 없음(D-4 — 부모는 UTC 시각, 신청은 KST 날짜 라벨). 격자 겹침 검사도 없음(D-7 — 승인 시점에 어차피 재판정, MSG-500 한 곳). SecurityConfig·계약 인터페이스 무변경. 신규 테스트 22건(목+고정 Clock 15 — 종료 정각 경계 포함 / 실 DB 7 — 저장 실측·상세 부모 이름·PATCH 본문의 부모 변경 시도 무시·재제출 시점 부모 종료 13441), 전부 `// 검증: FR-EVENT-17`. 테스트 픽스처의 고정 날짜를 상대값으로 교체(498분 포함 — 그날이 지나면 성공 경로가 13433으로 먼저 깨지는 시한폭탄 4곳 해제). reviewer 전수 통과·Codex 2R 수렴(픽스처 상대화 채택, 구버전 롤백 비호환 지적은 프리런치라 기각)
- MSG-500: 관리자 심사·승인 반영·노출 관리 — **V51** `event_submissions` +4컬럼(`approval_no` UNIQUE·`published_mission_id` FK(NO ACTION)·`unpublished_at`·`unpublish_reason`) + CHECK 3개(`(status='APPROVED') = (approval_no IS NOT NULL)` · 중지 기록 짝 · 중지는 승인 행만) + 승인 번호 전역 시퀀스 `event_submission_approval_no_seq`. API 6종: `AdminEventSubmissionController`(`/api/admin/event-submissions` — 심사 큐·상세·승인·반려) + `AdminApprovedEventController`(`/api/admin/events` — 승인 행사 목록·노출 중지). 역할 검사는 컨트롤러에 없다(기존 `/api/admin/**` matcher, SecurityConfig 무변경). **심사 조회**: 큐는 users 세타 조인 + 생성자 프로젝션에 `size(s.locations)` 상관 서브쿼리(항목마다 위치를 다시 읽지 않는다), 건수 3종은 필터 무관 전체 집계, 상세는 신청 계정 정보 + `exposureRect`(전 위치 사각형의 경계 — 조회 시점 계산, 저장 안 함) + 이력. 위치 표현은 `EventSubmissionLocationView`로 **행사 운영자 상세와 같은 조립기**를 쓴다(심사자와 신청자가 다른 그림을 보면 반려 사유가 가리키는 대상이 어긋난다). **승인**(D-1·D-2): 상태·기간 선검사(시퀀스 헛소비 방지) → 조건부 UPDATE로 IN_REVIEW→APPROVED 전이와 승인 번호 부여를 한 문장에(동시 승인의 진짜 판정) → 0행이면 재조회로 13430/13450 분기 → 이력 APPROVED 행 → **재로드 후**(벌크 UPDATE가 영속성 컨텍스트를 우회해 앞서 읽은 엔티티는 스테일) 미션 등재. 지역축제→`MissionType.EVENT`(축제 칩)·팝업스토어→`POPUP`, 판정 격자는 전 위치 셀 **합집합**(`EventSubmissionCells` — 상세의 칸 수와 같은 산술), 대표 격자는 그 합집합에 규칙 재적용, 이미지는 확정본을 `missions/org-submission/{uuid}.{ext}`로 복사해 공개 URL 저장(원본은 심사·콘솔 상세가 계속 읽으므로 유지, 롤백 보상 삭제는 복사 호출 앞 등록). 승인 번호는 `APR-{KST연도}-{4자리}`. **반려**: 항목 코드 1개 이상(빈 배열·허용 밖·중복 전부 13454 — Jackson enum 역직렬화에 맡기면 공통 400으로 뭉개진다)과 서술을 이력 반려 행에 쌓고(D-3 저장 원천 단일), 메일 없음. **승인 행사 목록**(D-11): 원천은 APPROVED 신청 행이고 안정 식별자는 신청 id, 탭은 저장하지 않고 **KST 오늘과 기간의 파생**(`TAB_CASE` 컴파일 상수 한 벌을 필터·표시·건수 3자리가 공유 — 식이 갈리면 뱃지 숫자와 목록이 어긋난다), 중지된 행사도 탭에 남고 `unpublished`로 구분. **노출 중지**(D-3): 조건부 UPDATE(`APPROVED AND unpublished_at IS NULL`) → 같은 트랜잭션에서 `MissionRegistrationService.hide` → 커밋 후 신청 계정 이메일로 사유 발송(실패는 `emailSent:false`, 재발송 API 없음 — 저장된 사유가 수기 재통지 재료). 에러 코드 13450~13456 신설(목록 파라미터 두 코드는 판정·메시지가 MSG-499의 1424·1425와 같고 **대역만 event** — 도메인 예외는 도메인이 갖는다). 테스트 42건(심사 조회 8·승인 반려 12·중지 9·목록 6·비트랜잭션 4 — 동시 승인 13450·승인 원자성 롤백·승인/중지 커밋 후 스냅숏 무효화 + 스토어 단위 3). **참여형(이벤트) 승인 분기**는 MSG-502 머지 후 이어 붙였다 — `publish()`의 유형 switch에 `default`가 없어 MSG-502가 참여형 값을 추가한 순간 컴파일이 깨졌고(의도된 착수 신호), 그 자리에 부모 회차 비관 잠금(`EventOccurrenceRepository.findWithLockById` 신설) → 겹침 **두 방향** 사전 검사(회차 기존 격자 + 이번 신청 위치 상호, 13452 첫 소비) → `EventLocation` 전개(`sub-{submissionNo}-{n}` 자연키·이름 100자 접미사 절단·ETC·displayOrder 이어붙임·저장된 대표 격자) → 참여 속성 6종 복사(V50 신청 컬럼 → V51 위치 컬럼) → 커버 이미지 `event-locations/org-submission/` 공개 복사(신청당 1회, 위치 공유) → 노출 영역 합집합 확장(`EventOccurrence.update` 경유라 scheduleRevision 불변 = 일정 변경 알림 미발송)을 채웠다. `publish()` 반환이 `long` → `Long`(참여형 null — 산출물이 여러 위치라 신청 행에 실을 단일 링크가 없다). 없는 부모는 502의 13440 재사용, 끝난 부모는 13441이 아니라 13451(독자가 신청자가 아니라 승인하는 관리자다). **중지의 참여형 경로**도 함께 닫혔다 — 회차 잠금(승인과 **같은 순서**, AB-BA 방지) → `hideByLocationKeyPrefix` 벌크 UPDATE → 남은 가시 위치로 노출 영역 재계산. 격자 조회를 둘로 가른 것이 계약이다: 겹침 검사는 숨긴 위치 포함 전량(`uq_event_grid_per_occ`가 숨김을 안 본다), 영역 재계산은 가시만. 숨김 소비는 행사 쪽 7곳(회차 상세 위치 목록·위치 벌크·격자의 행사 위치·업로드 위치 확인·위치 영상 목록·영상 상세·댓글/도움돼요 직링크)에 기존 404 수렴으로 추가. `EventLocationResponseDto`에 nullable 5필드(참여 속성, imageUrl은 publicUrl 조립값 — 시드 위치는 전부 null). **업로드 경로의 회차 접근은 읽기 전용인 것이 계약**이다(잠금 없는 `findById`+`refresh`) — 위치 잠금 뒤에 회차 잠금을 더하면 승인·중지와 데드락이다. 테스트 +12(승인 8·중지 3·동시 승인 직렬화 1). SRS는 502와 `FR-EVENT-17`이 충돌해 우리 두 요구를 18·19로 재번호했다
- MSG-457: `EventQueryService.getLocationsBulk(Collection<Long>)` + `LocationPoint(name, representativeGridId)` 신설 — 여러 회차 위치 일괄 조회(JPQL fetch join 1쿼리, 미노출 회차 키 생략 = 단건 13404 은닉과 동결, 정렬 회차 id→display_order→id로 단건 계약 승계). route 후보 수집이 소비(회차별 getLocations 반복의 N+1 차단), `getLocations` 시그니처 불변

### `region` (Owner A · 구현 강정민) — 🟡 부분
- MSG-154: `entity/Region`(boundary_geom 미매핑), `repository/RegionRepository`(native UPSERT + ST_Area 기반 total_grid_count), `seed/{RegionGeoJsonReader,RegionFeature,RegionSeeder}`(플래그 게이트 `fillmap.region.seed.enabled` 기본 off, 전국 3,558 행정동)
- MSG-93: 역지오코딩(`GET /api/regions/reverse-geocode`, ST_Covers/GIST, `service/RegionQueryService`(+impl)·`controller/RegionController`·`dto/RegionResponseDto`·`exception/RegionErrorCode`(6400))
- MSG-155: region_stats 동기 recompute(`repository/RegionRepository.refreshRegionStats` — 계산-시 격자 중심점 ST_Covers 판정·0-UPSERT 유지, `service/RegionStatsCommandService`(+impl) 명령 계약 A→B)
- MSG-156: 수집률 조회(`GET /api/regions/stats` — collectedOnly 기본 true·무LIMIT(구조적 상한 3,558 — Codex 리뷰로 LIMIT 1000 제거)·LEAST 100 clamp·`6404 REGION_NOT_FOUND`, `service/RegionStatsQueryService`(+impl)·`dto/RegionStatResponseDto`)
- MSG-153: 단건 탐험률(`GET /api/regions/stats/by-point`·`/by-grid` — 격자 중심점 축 `resolveByPoint` 재사용, `findStatByRegion` LEFT JOIN 0% 합성·`LEAST(COALESCE)` clamp, no-match 200+null)
- MSG-167: 시딩 직후 `grids.region_code` 멱등 보정 백필(`RegionRepository.backfillGridRegionCodes`, `RegionSeeder.run`에서 호출 — regions 후착 환경의 영구 NULL 라벨 방지, `EXISTS` 가드로 무귀속 격자 NULL→NULL 재기록 차단, Codex 리뷰 P1·2차)
- MSG-406: 전국 탐험률 재료 조회(`GET /api/regions/stats/national` 파라미터 없음 — `findNationalStat` 스칼라 서브쿼리 2개 native(분자 SUM(region_stats.collected_count)·분모 SUM(regions.total_grid_count)), 원값 정수 2필드 `RegionNationalStatResponseDto`(비율 필드 없음·LEAST clamp 없음 — 100% 상한·반올림은 화면 min 규약, SQL에 LEAST·ST_ 부재를 리플렉션 테스트로 회귀 방어), `RegionNationalStatProjection`/`View`. 도감 헤더 행정동 진행바는 by-point(MSG-153) 재사용이라 무변경. EXPLAIN 실측 2.4~2.6ms(지배 비용 regions 전체 스캔), 신규 인덱스·마이그레이션·에러코드 0)
- MSG-435: 검색 지역 필터용 시군구 목록(`GET /api/regions/districts` 파라미터 없음 — `findDistricts` native: regions를 parent_code로 GROUP BY, 이름 `MIN(split_part(region_name,' ',2))`(MSG-356 토큰 규칙 재사용), 격자 수 `SUM(total_grid_count)`(MSG-406 전국 분모와 같은 재료), `HAVING SUM>0`으로 0 시군구 제외, `ORDER BY 이름 COLLATE "C"` 환경 무관 가나다순. `RegionStatsQueryService.findDistricts`·`RegionDistrictProjection`/`View`/`ResponseDto` 신규, 신규 인덱스·마이그레이션·에러코드 0. EXPLAIN 실측 3,558행 9.5~12.3ms Seq Scan 1회 — 인덱스 불요 판정. 사용자 무관 값이라 사용자별 조회 없음)
- MSG-468: 언급 지명 대조 계약(`RegionQueryService.matchMentionedRegions(name, viewport)` + nested record `MentionedRegionMatch`(name·중심·외접 사각형 4좌표·overlapsViewport) — B(route) 소비. `RegionRepository.matchMentionedRegions` native 1문장: UNION ALL 3분기(시도는 첫 토큰 완전 일치 또는 입력+행정 접미 5종 보정, 시군구 둘째·동 셋째 토큰 완전 일치만), `left(region_code, 2/5/10)` GROUP BY(RegionUnit 정본), MIN 이름 단일화, `ST_Centroid(ST_Collect)` 무게중심(섬이 사각형을 늘리는 인천·경북에서 중점 대신), `ST_Extent` 외접 사각형, `BOOL_OR(ST_Intersects(... ::geography))` 실경계 뷰포트 겹침. 3,558행 순차 스캔 — 토큰 인덱스 없음(스펙 판정), `RegionMentionProjection` 신규)
- **없는 것**: 시/도·시군구 상위 레벨 **수집률** 집계 (07-22 유예 유지 — MSG-406 초안의 시군구 부활은 같은 날 행정동 축 정정으로 철회. MSG-435의 시군구 목록은 전체 격자 수 나열이지 수집률이 아니다), regions 시딩·재시딩 시 region_stats 재구축(기왕 한계, 후속 티켓 후보)

### `zone` (Owner A · 구현 강정민) — 🟡 부분
- MSG-234: 격자 표시명 구역 — `entity/Zone`(V8 `zones` 전 컬럼 매핑)·`repository/ZoneRepository`, `GET /api/zones` 전체 목록(`service/ZoneQueryService`(+impl)·`controller/ZoneController`·`dto/ZoneResponseDto`), `seed/{ZoneSeed,ZoneSeeder}`(플래그 게이트 `fillmap.zone.seed.enabled` 기본 off, `resources/seed/zones.json` `zone_key` UPSERT 멱등). 표시명("서면 A-14") 계산은 당초 FE-local(§D3)이었으나 MSG-341로 서버 계산 전환. 장소 검색은 카카오 프록시 MSG-251 이관(2단 폴백 구현분 제거, §D6)
- MSG-259: zones 실데이터 48건 주입(`seed/zones.json` — 공공 상권 17 + 수동 작도 31, 전국·검증기 PASS)·prod/dev 시더 상시 on·명명 계약 언어 중립 픽스처(`src/test/resources/fixtures/zone-naming.json`, `ZoneNamingContractTest` 픽스처 로드 리팩터)·glossary 구역/표시명 등재
- MSG-341: 격자 표시명 서버 계산 전환(MSG-234 §D3 결정 변경) — `service/ZoneNameQueryService`(계약 인터페이스, A 제공·B 소비)·`ZoneNameResolver`(요청 스코프 불변 스냅샷, 매 요청 findAll·캐시 없음, 정렬 1회로 priority DESC·zoneKey ASC 타이브레이크)·`ZoneCellName`(NONE 상수, 쌍 불변식 생성자 강제). 격자 담는 조회 응답 9종에 zoneName·zoneCell 통과(뷰 7종 필드 추가), 업로드 확정·재생엔 regionName도(`VideoRepository.findRegionNameByGridId` — grids.region_code equi 조회). 픽스처 분리 기대값(expectedZoneName/expectedZoneCell) 가산, ZoneNamingContractTest 참조 구현을 실물 승격
- **없는 것**: 장소 검색(MSG-251 카카오 프록시 — 구현 완료로 이관)

### `video` (Owner B · 구현 공동) — 🟡 부분
- MSG-66: `entity`(Video·ProcessingStatus·Visibility·VideoStatus + 상태전이 도메인 메서드), `repository/VideoRepository`(grids·user_grids native UPSERT/롤백), 메타저장 `service`·`controller`(`POST /api/videos`)·`dto`, `support/GeoSupport`, `exception/VideoErrorCode`(3xxx)
- MSG-64: presigned URL 발급(`POST /api/videos/presigned-url`)
- MSG-65: 인코딩 워커(`VideoEncodingService`+`VideoStatusWriter`+`support/FfmpegRunner`+`config/AsyncConfig`, 커밋 후 `@Async` 트리거)
- MSG-494: 커밋 후 `@Async` 트리거를 V44 `video_encoding_jobs` 작업 행으로 교체했다. 업로드 확정·교체와 작업 등록이 한 트랜잭션이고, BE·AI EC2의 `EncodingJobPoller`가 `FOR UPDATE SKIP LOCKED`로 한 건씩 선점한다. `claim_token`·UTC 임대·최대 3회로 늦은 결과와 중복 종결을 막는다. SIGTERM은 `ContextClosedEvent`에서 시도 횟수를 되돌려 즉시 반납하고 `@PreDestroy`는 멱등 fallback이며, 강제 종료는 임대 만료 뒤 회수한다. 세 번째 중단은 DEAD finalizer가 영상 FAILED·알림·작업 완료를 한 트랜잭션으로 끝낸다. AI 노드는 같은 JAR의 `dev,encoding-worker` 프로필로 API·Flyway·시더·알림 스케줄러 없이 8081에서 뜨고, CD는 BE 성공 뒤 워커를 배포한다. 노드별 작업 Counter와 큐 Gauge, Prometheus·Grafana·런북, baseline·SIGTERM·SIGKILL·블러 경합 측정 모드를 추가했다. **dev 실측**: 10~19초 MP4 9편이 전부 READY(최대 25.683초, be 5건·ai 4건, 유실·중복 알림 0), SIGTERM은 be→ai 같은 1회차로 17.764초, SIGKILL은 시험 임대 PT1M에서 ai 1회차→be 2회차로 76.337초에 회수됐다. 블러 PROCESSING과 두 노드 인코딩을 동시에 관측했고 세 편이 58.281초·114.774초·189.634초에 READY가 됐다. 선점 SQL은 dev 21행에서 PENDING 1.836ms·만료 PROCESSING 0.454ms였으며 작은 테이블이라 Seq Scan을 골랐다. 시험 뒤 임대 PT35M·블러 off로 복구했고 두 서비스와 Prometheus 타깃이 UP이다
- MSG-67: MP4 재생 CloudFront 연동(`CloudFrontUtilities` 600초 signed URL, 비활성 시 S3 presign 폴백, OAC+신뢰 키 그룹+10분 캐시 정책 dev 배포). 파생 S3 키를 original 시도별 UUID 경로로 분리하고 교체 커밋 뒤 이전 원본·인코딩본·블러본·썸네일을 삭제한다. dev 실측: 전체 GET 웜30 TTFB 51.8ms·완료 181.0ms, 1MiB Range 웜30 TTFB 48.6ms·완료 129.2ms, S3 직접 대비 각각 51.1%·27.8%와 48.5%·28.9% 개선. 콜드는 S3 직접보다 느림, 미서명·만료 403, Range 206, 캐시 적중 29/30·30/30, 실제 교체 후 경로 변경·옛 파생 파일 삭제 확인
- MSG-495: 운영 영상 전송 자원 구성(`fillmap-video-prod`, 전용 OAC·공개 키·신뢰 키 그룹, CloudFront `E3RTGBXCIBKF2M`, `media.fillmap.kr` ACM·Route 53 A/AAAA). 운영 업로드 IAM 정책은 prod EC2가 없어 미연결 상태다. 실검증: 전체 GET 200·Range 206(`bytes 0-1023/10439`)·반복 요청 캐시 Hit, 미서명·만료·original·pending은 403, dev Range 206 유지. 검증 객체 삭제 완료. HLS·WAF·Origin Shield는 비범위
- MSG-72: 삭제+점령 롤백(`DELETE /api/videos/{videoId}`, cover 재선정)
- MSG-243: 삭제 동시성 정합(`deleteVideo` 도입부 `findWithLockById` 행 잠금 — 동시 삭제 이중 감소·점령 오롤백 차단, 패자 멱등 200 유지, `VideoDeleteConcurrencyTest` pg_blocking_pids 결정적 재현)
- MSG-241: 인코딩 stale completion 차단(`encode(videoId, originalKey)` 시그니처 + 인코딩 라이터 4종 `findWithLockById`·`isCurrentEncodingAttempt`(ACTIVE·originalS3Key 일치) 가드 — 교체 후 옛 태스크의 READY/BLURRING/FAILED 오염·ai_job_id 잔존 차단. 폴러 라이터 3종 무변경, 마이그레이션 불요)
- MSG-247: 확정 롤백 S3 보상(`copyToOriginal` 복사 직후 `deleteOnRollback` — STATUS_ROLLED_BACK만·비활성 no-op·`deleteQuietly` 재사용) + 시도별 유니크 original 키(`{pendingStem}-{attemptUuid}`, `pg_advisory_xact_lock` 확정 직렬화·prefix 중복 검사 — 동시 확정 레이스 근절, 패자 4xx 수렴. prefix 인덱스는 MSG-262)
- MSG-262: 확정 경로 prefix 조회 인덱스(V11 `idx_videos_original_s3_key_pattern` — `original_s3_key varchar_pattern_ops`, 비-C 콜레이션 풀 스캔 방지. EXPLAIN 실증: 인덱스 有 Index Only Scan / 無 Seq Scan)
- MSG-132: s3Key 검증(소유권·headObject 실존·UNIQUE 중복)
- MSG-71: 교체(`PUT /api/videos/{videoId}`, 같은 격자만, 도감 불변)
- MSG-133: S3 정리(presign은 `videos/pending/` 발급 → 확정 시 `videos/original/` 복사, 라이프사이클 1일 만료 / 삭제·교체 시 커밋 후 객체 제거)
- MSG-127: 격자별 내 영상 리스트(`GET /api/grids/{gridId}/my-videos`, `GridVideoController`+`GridVideoResponseDto`, 썸네일 presigned GET — 코드베이스 최초 GET presign)
- MSG-87: 격자 전역 대표 영상(`GET /api/grids/{gridId}/cover`, `findGlobalCover` — `idx_videos_grid_popular` 일치, `GridCoverVideoResponseDto`)
- MSG-155: region_stats 갱신 트리거 배선(첫 점령/롤백 시 `RegionStatsCommandService.refresh` 호출, 같은 트랜잭션 참여)
- MSG-145: AI 결과 저장 스키마(V3: `blurred_s3_key`·`highlights` JSONB 최대3·`ai_job_id`, `applyBlurResult` — READY 전이는 MSG-150)
- MSG-149/150: AI 서버 연동(`AiProperties`/`AiConfig`(RestClient.Builder 빈+타임아웃)/`AiClient`/`AiBlurPoller` — `ai.enabled` 기본 off 게이트, 활성 시 ENCODING→BLURRING→READY, 30s reconcile 폴러·타임아웃/가드(시도 넌스·행 잠금)→FAILED 수렴, 블러본 `videos/blurred/`+블러 썸네일 재추출 업로드, 단일 인스턴스 전제)
- MSG-153: 썸네일 GET presign 공용화(`support/ThumbnailUrlPresigner` 추출 — MSG-127 로직 이동, usergrid 갤러리와 공유)
- MSG-162: 공개 범위 전환(`PATCH /api/videos/{videoId}/visibility`, `INVALID_VISIBILITY` 3420, `@DynamicUpdate`로 교차 컬럼 lost-update 차단, 노출 게이트는 read 경계 READY 강제 원칙)
- MSG-204: 업로드 시 공개범위 지정(`VideoUploadRequestDto.visibility` 선택 필드 — null=**PUBLIC 기본**(§M3 PRIVATE 안전값 대체), `parseVisibility` 재사용·confirmUpload 전 파싱, `Video.create` Visibility 파라미터화(하드코딩 제거), 마이그레이션·계약 인터페이스 무변경. FRIENDS는 MSG-285에서 추가)
- MSG-167: `upsertGrid`(lazy insert)에 `region_code` 중심점 판정 인라인 — 격자 생애 1회(`SELECT … WHERE NOT EXISTS`, 무귀속 NULL). 판정 규칙은 Owner A 자산(93/155 동일), B 레포 호스팅(신설 공유 컬럼 `grids.region_code`)
- MSG-206: 영상 재생 조회(`GET /api/videos/{videoId}` — `VideoPlaybackResponseDto`, 재생 소스 blurred ?? encoded presign, 접근 제어 DELETED→BLINDED→visibility→READY first-match, `incrementViewCount` 원자적 +1 타인·발급 시만, 명시 HEAD no-op 핸들러)
- MSG-242: 교체 시 `recordedAt` 엔티티 반영(`Video.replaceFile` 3-arg — MSG-71의 반영 누락 정정, 미션 기간 판정(MSG-223) 선행)
- MSG-237: 격자 전역 영상 목록(`GET /api/grids/{gridId}/videos` — `idx_videos_grid_popular` 일치 ACTIVE·PUBLIC·READY 필터, 조회수 인기순 keyset opaque 커서(gridId 바인딩·UTC epoch micros), `GridGlobalVideoResponseDto`/`GridVideoPageResponseDto`, `INVALID_CURSOR` 3423)
- MSG-372: 격자 전역 시간대 분포(`GET /api/grids/{gridId}/hourly-uploads` — `videos.created_at` UTC 벽시계를 KST 시로 변환해 ACTIVE·PUBLIC·READY 영상 전체 누적 집계, CTE로 시간 산출과 `GROUP BY hour` 분리, 서비스가 빈 구간을 채워 항상 0~23시 24개 반환. 공개 영상이 없거나 존재하지 않는 gridId도 전 구간 0인 200 응답, 신규 인덱스·마이그레이션·에러코드 0)
- MSG-238: 전역 탐색 API 2종(`GET /api/regions/{regionCode}/grids` 카드+헤더 카운트·`GET /api/regions/explore` — `RegionExploreController`/`Service`, 게이트=ACTIVE·PUBLIC·READY 단일 정의, 커버 87 규칙 3키 정합(`findGlobalCover` id DESC 추가), DTO 3종·프로젝션 3종, sort 대문자 enum·limit null=전부, 신규 에러코드 0)
- MSG-239: 업로드 뱃지 훅(`saveVideo` 2지점 — 항상 UPLOAD_COUNT(생애 카운트·status 무관), 첫 점령 시 TOTAL_GRIDS+refresh 직후 REGION_PERCENT 물질화 값 소비, 같은 트랜잭션)·`VideoUploadResponseDto.newBadges` 동봉(FR-9). 삭제·교체 경로 무변경(비회수 FR-5)
- MSG-200: 업로드 스트릭 훅(`saveVideo` — `!alreadyOccupied` 분기 바깥 1줄, `StreakCommandService.recordUpload` 획득분 `newBadges` 합류. 삭제·교체 무변경 — 소급 차감 없음 §D4)
- MSG-183: 업로드 핫스코어 훅(`saveVideo` 훅 체인 뒤 `afterCommit` 1줄 — `HotScoreCommandService.recordUpload(gridId)` 소비, 유령 증분 차단·응답 계약 무변. 교체·삭제 무배선 — 차감 없음 §D4·D6)
- MSG-283: AI 폴링 404 미제출 복귀(`AiBlurPoller` 결합 분기 분리 — 404=`clearAiJob`(aiJobId null·startedAt 유지) 후 기존 미제출 경로가 다음 주기 재제출, 명시 FAILED만 즉시 실패. `VideoStatusWriter.clearAiJob` 신규(행 잠금+`isCurrentBlurJob` 가드), 배포發 잡 유실이 PT30M 내 READY 수렴. `AiClient`·마이그레이션 무변경)
- MSG-286: 프리체크 탈락 즉시 실패(`AiJobResult`에 `Precheck(passed, reason)` 4번째 컴포넌트 — missing/null/malformed→null 판정 안 함, 폴러 탈락 독립 if FAILED 뒤·DONE 앞이라 `downloadBlurred` 409 미진입, PT30M→다음 주기. **V17 `videos.fail_reason`**(VARCHAR(64) NULL — NULL=시스템 오류, 콜론 앞 코드만·64자 절단·null reason은 `precheck_failed` 폴백), `markBlurFailed` 사유 파라미터 확장. FE 노출 API 비범위)
- MSG-278: recordedAt 미래 시각 거부(`validateRecordedAt` 공유 검증 — 업로드·교체 양 경로 confirmUpload 전, `Clock.systemUTC()` 기준 now+5분 초과만 `RECORDED_AT_IN_FUTURE` 3424, `isAfter`라 정각 통과. 갤러리 과거 시각 인정 유지(PRD §8 확정), 판정 쿼리·마이그레이션 무수정. 테스트 픽스처 recordedAt UTC 정규화 15파일 — zone-less now()는 9h 스큐 오거부)
- MSG-285: 공개범위 FRIENDS(친구만 보기) 확장 — `entity/Visibility` 3값째 `FRIENDS`(V20 CHECK 확장, 컬럼·인덱스 무변경), `getVideoPlayback` 재생 판정을 **enum switch 식**으로 재구성(PUBLIC만 명시 통과 — 기존 "PRIVATE만 차단" 부정형이면 새 값이 조용히 전원 공개된다. 식이라 4값째 추가 시 컴파일 에러로 재검토 강제, 문(statement)은 javac 이 exhaustiveness 를 안 잡아 무효), 비친구는 **기존 `VIDEO_FORBIDDEN` 3403 + "비공개 영상입니다"** 단일 throw 재사용(PRIVATE 실패와 응답 동일 — 신규 에러코드 0), 친구 판정은 `FriendService.isFriend` B-내부 소비(FRIENDS && 비소유자 경로에서만 1회 — PUBLIC·PRIVATE·소유자 쿼리 0회). 지정 경로는 `parseVisibility` 무수정(enum 상수 추가로 업로드·전환 자동 허용), `INVALID_VISIBILITY` 3420 메시지·Swagger 6곳 3값화. 전역 노출 쿼리(`visibility = 'PUBLIC'` 등식 9곳) 무변경 — FRIENDS 는 대표 영상·전역 목록·탐색 집계에서 자동 제외(목록 노출은 MSG-187 이월)
- MSG-313: 처리 종결 알림 배선 — `VideoStatusWriter` 4개 종결 전이(markReady·markBlurReady 완료 / markFailed·markBlurFailed 실패) 내부, 가드 통과 후 `record(VIDEO:{videoId}:{원본 파일명 꼬리 45자})` 1줄씩. **호출자가 아니라 라이터에 배선**(markFailed 호출자만 3곳 — 호출자 측은 누락을 만든다), REQUIRES_NEW 전이 트랜잭션에 record(REQUIRED)가 참여해 전이 커밋 = 알림 커밋, 스테일 가드 skip 시 알림도 skip. 키 꼬리 45자는 실제 파일명이 UUID 2개 연결 77자라 event_key VARCHAR(100)을 넘던 것(Codex 1R)을 막고 attemptUuid는 온전 보존. **잠금 순서 통일**(Codex 2R): videos 행 잠금 전에 `findUserIdById` 무잠금 선독 → `UserRepository.findIdForKeyShare`(FOR KEY SHARE) 선취 — 알림 insert의 FK 검사가 users를 대기하고 탈퇴 CASCADE가 videos를 대기하는 교차 데드락 차단, 행 부재 시 전이·알림 조용히 스킵
- MSG-193: 블라인드 전환·해제 — `service/VideoModerationService`(+Impl, blind/unblind — **HTTP 미노출**, 호출 주체는 MSG-195 관리자 API), `findWithLockById` 행 잠금(실측 SQL은 **FOR NO KEY UPDATE** — 삭제(MSG-243)·신고(MSG-192)와 동일 메서드라 전이 직렬화, FK KEY SHARE와 비충돌), 가드 first-match(미존재·DELETED 3404 재사용, 이미 목표 상태 `ALREADY_IN_TARGET_STATUS` **3409** 신규), `Video.markBlinded()/markActive()` 도메인 메서드(검증은 서비스, 대입은 엔티티), markBlinded가 `reselectCover` native보다 먼저(자동 flush로 재선정 서브쿼리가 최신 status를 봄 — 삭제 경로와 동일 원리), 해제는 대표 원복 없음(D6). 점령·video_count·수집률·핫스코어·스트릭·뱃지 전부 불변, 조회 쿼리 수정 0줄(노출 차단은 기존 ACTIVE 필터가 즉시 수행 — 통합 테스트 16건이 재생 404·전역 대표·전역 목록·내 리스트 4경로 회귀 검증)
- MSG-417: 블라인드 전이 소유자 알림 — `VideoModerationServiceImpl.blind()/unblind()` 서두 `lockOwnerFirst`(무잠금 선독 `findUserIdById` → users FOR KEY SHARE, 빈 결과 3404 — notifications FK CASCADE 잠금 사이클 차단) 후 전이 끝 record(event_key `MODERATION:BLIND|UNBLIND:{videoId}:{UUID}` — 재블라인드마다 새 알림, 해제 문구는 visibility 보존 때문에 "공개" 미사용). 검증 VideoBlindIntegrationTest +5
- MSG-350: 단건 재생 응답에 AI 하이라이트 노출(`VideoPlaybackResponseDto.highlights` 컴포넌트 추가 — `of()` 팩토리에서 저장 혼재(null·`[]`)를 응답 null 하나로 정규화(스펙 D-1, 빈 배열 미노출), Swagger nullable + "배열 순서 = 추천 우선순위" 계약 명시. 컨트롤러·서비스·엔티티·폴러 무변경·마이그레이션 없음 — 저장 경로(MSG-145/150)와는 `Video.getHighlights()` 한 점 접촉. 없음 케이스(READY 이전·FAILED·AI 0구간) 전부 null, FE 길이별 카드 티어는 FE 정책·선분석은 MSG-351)
- MSG-351: 업로드 확정 전 하이라이트 선분석(`POST /api/videos/highlight-preview` — `HighlightPreviewService`(+Impl, **DB 무접촉·무저장·동기**), FE가 `purpose=HIGHLIGHT_PREVIEW` presign(전용 상한 2GiB, 기존 100MB 불변)으로 올린 pending 키를 받아 S3 다운로드(요청 단위 apiCallTimeout 60초)→ffprobe 실측 180초 방어(호출별 30초 시한, `FfmpegRunner.InvalidMediaException` 분류 — 파일 불량만 3426, 인프라 실패는 공통 500)→`AiClient.analyzeHighlights`(전용 JDK HttpClient, 교환 전체 120초, 응답 형태 검증). 전역 Semaphore(2) 게이트 초과 3429, 실패 수렴 3502 — permit 점유 상한 ≈210초×2스레드. confirmUpload에 headObject 실측 크기 3413 방어 추가(선분석 대용량 키의 확정 우회 차단, Codex 1R). 신규 에러코드 3425/3426/3429/3502, 마이그레이션 없음. AI 쪽 계약·구간 품질은 FillMap-AI MSG-353, AI 자체 동시 캡은 MSG-355 후속)
- MSG-363: `videos.visibility` DB 기본값 정정(V30 — `SET DEFAULT 'PRIVATE'` → `'PUBLIC'`). **자바 코드 변경 0줄·기존 행 무변경의 표기 정정**이다: 서버는 MSG-204부터 미지정 업로드를 PUBLIC으로 저장하고 애플리케이션이 INSERT마다 visibility를 명시하므로 이 DEFAULT는 발동한 적이 없다(V1이 반대로 선언해 뒀을 뿐). 고치는 이유는 스키마만 읽는 쪽(ERD·덤프·직접 SQL 시드)이 반대 결론을 내는 것을 막기 위해서 — 메타데이터만 바꾸는 ALTER라 테이블 재작성도 없다
- MSG-371: 전역 영상 응답 작성자 닉네임(`GridCoverVideoResponseDto`·`GridGlobalVideoResponseDto`·`VideoPlaybackResponseDto`에 `nickname` 가산 — 기존 native 조회 3종 SQL 무변경, `findAuthorNickname` 단건 + `findAuthorNicknames` 배치 IN(`AuthorNicknameProjection` 신규)을 같은 트랜잭션 별도 조회로 붙여 목록 왕복 2회 고정(N+1 없음). 탈퇴 경합(READ COMMITTED 밀리초 창)은 **숨김 가드** — 닉네임 조회 빈손이면 목록 항목 제외·대표 없음·재생 기존 3404 재사용(이때 조회수 +1도 롤백 동반), 커서는 숨기기 전 `pageRows` 마지막 행 기준 유지(무한 루프 방지 회귀 테스트 고정). 폐기된 "작성자 비노출(Phase 2+)" 주석 2곳 정정. 신규 에러코드 0·마이그레이션 없음)
- MSG-343: 영상 처리 계측(`service/VideoProcessingMetrics` 신설 — 태그 조합을 생성자에서 선등록해 가변값 태그를 구조로 차단(스펙 D1)·미등록 값 증가는 무시. `video.processing.outcome`(Counter, outcome×path 4조합)·`video.processing.duration`(Timer, 동일 태그 + yml SLO 버킷 5·10·15·30·60·120·300s)·`video.encoding.task`(4종)·`video.ai.job`(6종)). 처리 시간 시작점은 **인메모리** `ConcurrentHashMap<videoId, nanoTime>` — `submitEncoding` 진입(업로드·교체 공용 afterCommit)에서 등록, 삭제는 afterCommit에서 제거, 앱 재시작 시 Timer 표본만 결손되고 Counter는 정확(D2 확정 오차). 종결 계측은 `VideoStatusWriter` 종결 4메서드의 **전이 적용 분기에서만**(스테일 skip 무증가, D5). 인코딩·AI 실패 계상은 `statusWriter.markFailed`·`recordAiJob` **앞**에서 — 인코더는 재시도가 없어 DB 장애 시 태스크가 어떤 result에도 안 남는다(Codex 3R, `resultCounted` 플래그가 이중 계상 차단). 큐 적체는 신규 Gauge 없이 Boot 자동 노출 `executor_queued_tasks{name="encodingExecutor"}` 재사용(D4 실측)
- MSG-382: 인코딩 시도 가드 강화(`VideoStatusWriter.isCurrentEncodingAttempt` + `VideoEncodingServiceImpl`의 쌍둥이 술어 — 기존 ACTIVE·원본 키 일치에 **처리 상태 인코딩 국면(UPLOADED·ENCODING) 조건 추가**). 종결(READY·FAILED)·BLURRING으로 넘어간 시도에 늦게 온 완료/실패/시작 신호를 차단해 종결 Counter 중복 증가(NFR-OPS-08)·READY→ENCODING 회귀·블러 넌스 재발급을 막는다(MSG-343 머지 직후 Codex 적발). 커밋 실패 시 과대 집계는 MSG-343 수용 근사 그대로. 동반: cd-dev/prod 헬스체크에 "포트 리스너 PID 전체 == 유닛 MainPID" 확인 추가
- MSG-390: 미션 영상 목록(`GET /api/missions/{missionId}/videos` — `controller/MissionVideoController`(video 패키지에 미션 경로를 두는 MSG-127 선례, `AuthPrincipal` 안 읽지만 인증 필수)·`support/MissionVideoCursor`(`(missionId, recordedAt, id)` 3성분 Base64URL, `VideoCursor`는 무수정 — 성분 수·의미가 달라 확장하면 발급된 격자 커서가 깨진다)·`VideoRepository.findMissionVideos`/`findMissionVideosAfter` native 2종·`VideoServiceImpl.getMissionVideos`. 후보 술어는 `MissionRepository.findCompleted` 조인에서 파생하되 `v.user_id` 제거·`status <> 'DELETED'`를 게이트 3종(ACTIVE·PUBLIC·READY)으로 교체 — 판정은 BLINDED를 촬영 사실로 인정하고 목록은 감추므로 화면 개수가 판정 근거보다 작을 수 있다(의도된 차이). 정렬 `recorded_at DESC, id DESC`, 기간은 `IS NULL OR` 원형 유지라 무기간 코스가 코드 분기 없이 통과. 커서 시각 하한은 리터럴이 아니라 `LocalDateTime.of(-4712, 1, 1, 0, 0)` 비교 — PostgreSQL 저장 하한이고, 그보다 이른 값은 pgjdbc `setObject` 경로에서 **예외 없이 `-infinity`로 조용히 바뀌어** 빈 페이지가 실패로 보이지 않는다. 상한 가드는 없다(PG 상한이 `long`을 넘어 토큰에 실릴 수 없는 죽은 코드). 신규 마이그레이션·에러코드 0(`INVALID_CURSOR` 3423 재사용), `mission/**`·`grid/**`·`SecurityConfig`·`GridVideoController` 무수정. 공유 DTO 2종은 스키마 중립화만(정렬 축·커서 형식은 엔드포인트별 `@Operation`으로 이동). 총 영상 개수는 이 API가 주지 않는다 — 미션 상세 조회 몫이고 그 집계는 이 문서 후보 술어와 술어 동등이어야 한다
- MSG-456: 블러 일시 비활성화 플래그 분리(`ai.blur-enabled`/`AI_BLUR_ENABLED` 기본 false — `AiProperties`에 `blurEnabled` 컴포넌트(enabled 바로 뒤), `AiBlurPoller`만 `@ConditionalOnProperty(name = {"enabled", "blur-enabled"})` 배열 조건으로 이동(AiConfig·AiClient는 ai.enabled 유지 — 선분석 생존), `VideoEncodingServiceImpl` 분기 2곳을 실효 블러 활성(enabled&&blurEnabled)으로 교체 — 꺼짐이면 원본 썸네일 추출 후 READY 직행. 재생 하이라이트는 **후행 계산**(D-1): markReady·TASK_COMPLETED 계상 먼저, `highlightExecutor`(AsyncConfig 신설, corePool1·maxPool1·queue50·AbortPolicy — ffmpeg 안 돌리는 HTTP 대기 전용이라 t3.small 무충돌)가 S3 인코딩본 다운로드→`analyzeHighlights`→`VideoStatusWriter.recordHighlights`(REQUIRES_NEW, 가드 축 originalS3Key·ACTIVE·READY, lockUploaderFirst 미사용) 저장. 워커·제출부의 모든 실패는 warn 후 하이라이트 null 유지(READY 불가침). 마이그레이션·신규 에러코드 0, dev는 배포만으로 블러 꺼짐 — 배포 후 BLURRING 잔존 점검·전환 SQL은 스펙 데이터 모델 절)
- MSG-392: 확정 시점 컨테이너 검증(`support/VideoSignature.looksLikeVideoContainer(byte[] head, boolean wholeObject)` 신설 — 앞 4096바이트에서 ISO BMFF 최상위 박스 체인을 최대 4개 걸어 허용 목록(`ftyp·moov·mdat·wide·free·skip·pnot·uuid`)만 나오는지 본다. 브랜드 미검사(인코더마다 달라 목록이 낡으면 정상 영상이 막힘), S3·스프링·contentLength 무의존 순수 함수. **크기 타당성 검사가 ftyp/moov 통과 판정보다 앞이라는 순서가 계약** — 뒤집으면 크기 24를 선언한 8바이트 ftyp 객체가 타입만 보고 통과한다(회귀 테스트가 이 순서를 고정). 창 소진(뒤가 더 있음)은 통과, EOF에서 잘린 구조는 거부라는 갈림이 규칙의 뼈대이고, `wholeObject`는 호출부가 `contentLength <= 4096`으로 만들되 null일 때만 받은 바이트 수 폴백 — 폴백만 쓰면 정확히 4096바이트 객체가 우회한다. 64비트 largesize는 `Math.min(boxSize, headLength + 1)` 포화 — 오버플로는 크래시가 아니라 조용한 판정 오염(ftyp 오통과 + mdat 오거부 양방향, 절단값이 항상 창 안이라 AIOOBE 도달 불가)이라 전용 회귀 테스트로만 잡힌다. `VideoServiceImpl.confirmUpload`의 실측 크기 검증 직후에 `requireVideoContainer` 삽입 — `contentLength < 8`은 S3 본문 읽기 없이 즉시 거부, 그 외에는 `GetObjectRequest.range("bytes=0-4095")` 1회(apiCallTimeout 10초, `HighlightPreviewServiceImpl.HEAD_TIMEOUT` 동형). `saveVideo`·`replaceVideo`·`confirmAtGrid`가 이 메서드를 공유해 일반·교체·미션·행사 네 경로가 한 곳에서 닫힌다. `VideoErrorCode.NOT_A_VIDEO_FILE(3428, BAD_REQUEST)` 신설, 416은 여기로 수렴하고 404는 `requireObjectExists` 선례대로 3402(`NoSuchKeyException`을 `S3Exception`보다 먼저 잡는 두 겹 — 목이 만든 예외는 `statusCode()`가 404가 아니라 **0**이라 값 검사만으론 안 걸린다), 그 밖의 `S3Exception`·`SdkClientException`은 전파돼 500. **미션 경로만 3428이 관찰되지 않는다** — `MissionVideoServiceImpl.confirm`이 모든 `ApiException`을 12409로 마스킹(존재 은닉, MSG-459 D-10). Flyway·DTO·계약 인터페이스 변경 0, 기존 확정분 소급 검사 없음. 비목표로 기판정: 완결 헤더를 붙인 쓰레기 파일 통과, 검증↔복사 사이 pending 덮어쓰기(TOCTOU — 확장 지점은 headObject ETag를 `copySourceIfMatch`로 잇기). 테스트 29건(단위 18·통합 11) + 기존 25개 클래스 스텁 정비(`test/video/support/S3VideoObjectStub` 공용 헬퍼 — headObject 스텁만 있던 확정 경로 테스트가 `getObjectAsBytes` 미스텁 NPE로 죽던 것, 컴파일에는 안 드러남). Codex 스펙 4라운드(8바이트 우회·4096 경계·미션 12409·uuid 누락 적발)·구현 1라운드 수렴)
- MSG-471: 컨테이너 판별 빈틈 두 곳 보강(MSG-392 후속, FR-VIDEO-03 복구) — ① `ftyp` 최소 길이 검사 신설: 헤더 뒤에 major_brand 4 + minor_version 4가 반드시 오므로 그보다 짧게 선언한 `ftyp`는 브랜드 자리가 없다. **기본 헤더는 16, 확장 헤더(size 필드 1)는 24로 최소치를 가른다** — 확장 헤더가 16바이트를 다 쓰므로 하나로 두면 largesize를 정확히 16으로 적은 `ftyp`가 브랜드 자리 0인 채 통과해 위장 파일이 표기 방식만 바꿔 빠져나간다(Codex 적발). ② MSG-392 절차 g의 "정확 종료 통과"를 `!wholeObject`로 축소: 파일 전체를 봤는데 `ftyp`도 `moov`도 한 번 못 봤으면 재생에 필요한 것이 아예 없으므로 거부한다(`mdat` 하나로 끝나는 파일). 창 소진은 뒤에 `moov`가 더 있을 수 있어 종전대로 통과. **비교 대상은 포화된 오프셋(`next - p`)이 아니라 박스가 선언한 길이** — `next`는 `Math.min(boxSize, headLength + 1)`을 거쳐 창 길이가 섞여 들어, 창이 16바이트보다 짧은 호출에서 정상 `ftyp`가 오거부된다(컨벤션 리뷰 적발, 회귀 테스트로 고정). `size == 0`은 파일 끝을 봐야 길이가 정해져 창 소진이면 판정 불가로 통과. **MSG-392 정본 번복이라 그 스펙 세 자리에 정정 인용 블록 병기**(원문 삭제 없음). 기존 테스트 3건은 기대값 뒤집기 대신 픽스처 교체 — `mdat` 뒤에 `moov`를 이어 붙여 선언 크기만큼 정확히 건너뛰어야만 통과하게 해 크기 파싱 검증력을 오히려 올렸다(크기 0 케이스만 이 수법이 안 통해 타입을 `ftyp`로 바꾸고 파일 길이만 다른 두 입력 대조). 순수 함수라 DB·S3·빈 무관, 신규 에러 코드 없음(종전대로 3428, 미션 경로만 12409 마스킹). 단위 22건 + 통합 11건 무수정 green. 비목표: `moov` 내부 구조 검사(브랜드처럼 고정 자리가 없어 최소 길이가 규격상 미정)·읽기 창 4096·반복 상한 4·기존 데이터 소급 검사
- MSG-470: 영상 길이를 서버 실측으로 저장(FR-MEDIA-19 신설) — 인코딩 워커가 이미 ffprobe로 재고 있던 `duration`을 30초 초과 판정에만 쓰고 버리던 것을 성공 전이에 실어 `videos.duration_sec`에 덮어쓴다. `VideoStatusWriter.markReady`(ENCODING→READY)·`markEncoded`(ENCODING→BLURRING) 두 전이에 `short measuredDurationSec` 파라미터 추가, 엔티티 쓰기는 `Video.markReady`·`markEncoded`의 더티 체킹이라 **별도 UPDATE문 없음**(기존 상태 UPDATE에 컬럼 하나가 얹힌다). 값은 `(short) Math.max(1, Math.min(30, Math.round(duration)))` — 상한 30은 스키마 CHECK(`duration_sec > 0 AND <= 30`) 준수이자 MSG-370이 허용한 실측 30~31초 여유 구간 대응이고, **하한 1은 실측 0.5초 미만이 반올림으로 0이 되어 같은 CHECK를 위반하는 경계**(신고값 검증 1~30은 파일 실제 길이를 보장하지 않는다). **31초 초과 실패 판정이 클램프보다 앞이라는 순서가 계약** — 뒤집으면 31.4초 영상이 30으로 뭉개져 FR-MEDIA-03이 회귀한다(`길이_초과_판정은_클램프_전_원값으로_한다`가 고정). `markBlurReady`·`markReadyFromBlurring` 무수정 — BLURRING 도달 후 AI가 실패해 FAILED가 되어도 이미 반영된 실측값은 되돌리지 않는다(되돌리려면 신고값을 따로 보관해야 하는데, 실측이 파일의 실제 길이라는 사실은 블러 성공 여부와 무관하다). 확정·교체 경로는 종전대로 신고값 저장이고 교체 재인코딩이 같은 전이를 타 별도 처리 불요. 스테일 가드(`isCurrentEncodingAttempt`)와 행 잠금이 그대로라 가드 skip 시 길이 덮어쓰기도 함께 skip(옛 파일 실측값이 새 파일에 안 붙는다). API 응답 `durationSec` 형태·이름 불변, Flyway·DTO·계약 인터페이스 변경 0. 발단은 QA 결함 "실제 영상 시간과 화면 표시 시간 불일치". 테스트 11건(전이 3·라이터 3·인코더 5) + 시그니처 변경에 따른 기존 호출부 20파일 기계적 갱신. 비범위: 소수점 초 전환(계약 변경)·기존 데이터 백필·`recordedAt` 시각 어긋남

### `search` (Owner A · 구현 강정민) — ✅ 완성 (MVP 범위)
- MSG-251: 장소 검색 카카오 프록시(`GET /api/search/places?q=` — keyword.json 실시간 패스스루(약관: 캐시·저장 금지), `PlaceSearchController`/`Service`(+impl)/`KakaoLocalClient`/`SearchConfig`(완성 RestClient 빈, connect 1s/read 3s)/`KakaoLocalProperties`, gridId=`GridEncoder.encode` 즉석 합성, `SearchErrorCode` 5xxx 신설 `SEARCH_UPSTREAM_ERROR(5502)` 단일 수렴, 키=`${oauth.kakao.client-id:}` 재사용)
- MSG-258: 인기 검색어 순위 — V23 `search_keyword_daily_counts`(PK+`uq_search_keyword_daily UNIQUE(keyword_date, keyword)` — 사용자 식별자·카카오 응답 필드 없음, 약관 경계), `entity/SearchKeywordDailyCount`, `repository/SearchKeywordDailyCountRepository`(`upsertIncrement` native ON CONFLICT +1 · `findTopKeywords` 오늘+어제 SUM DESC·keyword ASC LIMIT 10), `service/SearchKeywordCommandService`(+impl — 정규화 trim·공백압축·소문자·255자 초과 스킵, KST 날짜 요청 스레드 확정, Redis dedupe `searchdedupe:{yyyyMMdd}` SET member `{userId}:{keyword}` Lua SADD+EXPIRE 26h, hotzone 미러 단일 데몬 워커(유계 큐 10,000)·전 예외 warn 삼킴 — 검색 응답 독립, Redis 실패 시 UPSERT 미진행), `TrendingKeywordQueryService`(+impl)·`TrendingKeywordController`(`GET /api/search/trending` — rank 자바 부여)·`dto/TrendingKeywordResponseDto`(rank·keyword만 — count 미노출), 훅=`PlaceSearchServiceImpl.searchPlaces(long userId, String q)` trim 가드 직후·카카오 호출 전(실패 검색도 집계 — 확정 결정 7), 신규 에러코드 0
- MSG-457: 집계 없는 오버로드 `searchPlaces(String q)` 신설 — 경로 추천의 기계 조립 검색어 전용(인기 검색어 오염 차단). 공통 경로(trim 가드·카카오·gridId 합성·표시명)는 1인자가 소유하고 기존 2인자는 trim → 집계 접수 → 위임(실행 순서 보존, 회귀 테스트 확인). B(route) 소비 시작 — 크로스오너 접점
- MSG-481: 장소 검색 위치 기반 랭킹(`GET /api/search/places` 에 선택 파라미터 `lat`·`lng` 추가 — 컨트롤러가 String 으로 받아 짝 검사·`Double.parseDouble`·`KoreaCoordinates.isOutOfService` 순으로 검증하고 위반은 신규 `INVALID_COORDINATE(5400)`. Double 바인딩은 스프링 타입 미스매치가 developCode 400 으로 나가 5400 이 불가능해 String 수신이 필수다. `KakaoLocalClient.search(query, lat, lng)` 가 `x=lng`·`y=lat`·`radius=20000`(카카오 최대)로 요청하고 `sort` 는 미전송(기본 정확도순 유지), 2xx 응답이 0건일 때만 위치 없이 1회 재호출해 전국 결과로 되돌아간다. **예외는 재호출 없이 5502 로 수렴** — 장애 시 호출 수·대기 시간이 현행과 같다. 좌표는 집계·저장·로그 어디에도 남기지 않는다. Flyway·신규 테이블 0, 검색 도메인 테스트 33 → 55건)
- **없는 것**: 배치 확장(주간/월간 집계 — 일별 이력만 축적, MSG-258 비목표)

### `badge` (Owner B · 구현 강정민) — 🟡 부분
- MSG-239: 뱃지 시스템 MVP — V9(`chk_badges_condition`에 MISSION_COUNT 확장·`user_badges.notified_at/featured_rank`+partial UNIQUE·활성 3축 11종 시딩·set-based 소급), `entity/{Badge(conditionValue 미매핑),BadgeConditionType,UserBadge,UserBadgeId}`, `repository/{BadgeRepository.findEligible,UserBadgeRepository(지급 ON CONFLICT·metric 3종·featured lock/clear/set)}`, `service/BadgeAwardService`(+impl, 후보 SELECT+INSERT 2단 — B 내부)·`BadgeFeaturedService`(+impl), `PUT /api/badges/featured`(`BadgeController`·집합 교체 멱등), `dto/{EarnedBadge,FeaturedBadgeRequest,FeaturedBadgeResponse}ResponseDto`, `exception/BadgeErrorCode`(7xxx — 7400·7403)
- MSG-200: V10 꾸준함 뱃지 시딩(STREAK_3/7/30 — DDL 0·소급 블록 없음, §D6 예외 주석. 판정 훅은 streak 도메인이 `award(STREAK_DAYS)` 호출)
- MSG-201: 내 뱃지 조회 — `GET /api/badges`(획득+미획득 전체, badges.id ASC·featuredRank 동봉), `repository/{BadgeRepository.findAllWithMyStatus,MyBadgeProjection,UserBadgeRepository.markMyBadgesNotified}`, `service/BadgeQueryService`(+impl — 조회 시 노출된 미확인분만 IN 리스트 자동 스탬프·isNew 는 SELECT 시점 값), `dto/MyBadgeResponseDto`(마이그레이션·신규 에러 코드 0)
- MSG-181: 발급 알림 배선 — `award()` 안 `insertIgnoreConflict == 1` 직후 `record(BADGE:{badgeId})` 1줄. 전 축(UPLOAD_COUNT·TOTAL_GRIDS·REGION_PERCENT·STREAK_DAYS·MISSION_COUNT)이 이 지점으로 수렴해 새 축도 자동 커버, 발급 커밋과 원자적(FR-3)
- MSG-314: 뱃지 임박 알림 — `award()` 꼬리 `recordNearMiss`(조기 return 제거·공통 꼬리 합류). 4축 allowlist(EnumSet — REGION_PERCENT는 소수 rate, SPECIAL은 비개수 축이라 제외), `BadgeRepository.findNearMiss`(`= :metric + 1` native, 후보 최대 1건, `EligibleBadgeProjection` 재사용), 생애 1회 = `BADGE_NEAR:{badgeId}` dedupe, 하루 1건(KST) = `tryAcquireBadgeNearLock`(**pg_try_advisory_xact_lock 비대기** — 블로킹이면 streaks 행 잠금과 교차 데드락, Codex 정정) + `countRecordedSince` select-then-act. 카테고리 BADGE 재사용이라 마이그레이션·enum·컨슈머·설정 API 무변경, Clock 이중 생성자
- MSG-363: 미션 뱃지 종류별 재편 — V29(`chk_badges_condition`에 EVENT_COUNT·COURSE_COUNT·POPUP_COUNT 확장·`badges.retired_at`(NULL=현역) 추가·종류별 9종 시딩(축별 1/3/10, 축 단위 티어 오름차순 삽입 = badges.id 순 MSG-201 D6)·합산 MISSION_1/5/10 은퇴 UPDATE·소급 지급 set-based(`b.condition_type = m.type || '_COUNT'` 조인 한 벌로 3축 일괄 — 명명 규칙 자체가 매핑이라 축별 블록 복제 없음, ON CONFLICT 멱등·notified_at 미기재라 다음 조회에서 "새 뱃지")), `BadgeConditionType` 3축 추가 — **MISSION_COUNT 상수는 유지**(은퇴한 badges 행 3개가 그 문자열을 들고 있어 `@Enumerated(STRING)` 읽기가 깨진다. javadoc 은퇴 표기 + allowlist 제외로만 무력화), `BadgeRepository` 쿼리 3개에 은퇴 필터(`findEligible`·`findNearMiss` `AND b.retired_at IS NULL` — 신규 지급도 임박 알림도 중단 / `findAllWithMyStatus` `WHERE b.retired_at IS NULL OR ub.user_id IS NOT NULL` — 획득자에게만 남기고 미획득자에겐 행째 숨김, 영원히 못 받는 회색 칸 금지 FR-4), `NEAR_MISS_TYPES` allowlist를 MISSION_COUNT → 종류 3축으로 교체. 행 DELETE는 불가(user_badges FK NO ACTION이 막고, 지우면 획득 이력 박탈이라 FR-4와 정면 충돌 — 기각안 SPECIAL 전환·도달 불가 임계값은 §D2). `Badge` 엔티티에 `retired_at` 미매핑. 신규 API·DTO 필드·에러코드·계약 인터페이스 전부 0(달라지는 건 목록 구성과 `newBadges` 코드값뿐). 실 DB 검증 `MissionTypeBadgeSeedTest` 10건(시딩·CHECK·은퇴·소급 멱등·금지 표현 회귀 그물)
- **없는 것**: 뱃지 그림 — `badges.icon_url`이 26행(V9 11·V10 3·V12 3·V29 9) 전부 NULL이라 진열장이 회색 사각형이다. 주소 UPDATE + NOT NULL 마이그레이션은 **V31 예정·보류**다 — 그림 파일 형식(벡터 한 벌 vs 배율별)과 정적 에셋 버킷(공개 읽기 정책·리전)이 미확정이라 URL 규칙을 못 박는다(MSG-363 §D6·미해결 질문). SPECIAL 시딩(오픈 준비 티켓)

### `streak` (Owner B · 구현 공동) — ✅ 완성 (MVP 범위)
- MSG-200: 스트릭 집계 — `entity/Streak`(전 컬럼 매핑·Setter 없음, 쓰기는 native 전용), `repository/StreakRepository`(`upsertOnUpload` — 3분기 CASE 한 문장 UPSERT·KST 자정 경계·ON CONFLICT 행 잠금 직렬화 + `findCurrentCount`), `service/StreakCommandService`(+impl — 갱신 직후 `BadgeAwardService.award(STREAK_DAYS)` 배선·획득분 반환, B 내부). 조회 API 없음(currentStreak·maxStreak 노출은 도감 summary 티켓 소관 §D8), freeze 미도입·소급 차감 없음 확정
- MSG-181: 스트릭 리마인드 배치 — `service/StreakRemindScheduler`(cron 20~23시 매시 KST, `fillmap.notification.enabled` 게이트 안. **시간별 재발화 = 당일 재시도** — `REMIND:{KST일자}` dedupe가 기기록을 0행 흡수, per-recipient 예외 격리로 한 명 실패가 나머지를 안 막음, Codex 1R P2 반영), `StreakRepository.findRemindTargets`(`last_recorded_date = KST 어제` 한 조건 — 오늘=기업로드·그전=이미 끊김, 어제 날짜는 앱 Clock KST 계산 바인딩)

### `friend` (Owner B · 구현 강정민) — ✅ 완성 (MVP 범위)
- MSG-185: 친구 코드·관계 수명주기 — V18(users.friend_code 추가·기존 행 백필·UNIQUE)·V19(대칭 쌍 유니크 — findPair Optional 계약 보호), `entity/{Friendship,FriendshipId,FriendshipStatus}`(복합 PK @EmbeddedId — UserGrid 미러. status 는 PENDING·ACCEPTED 만 영속 — 거절·삭제는 행 DELETE, §D3 "행 존재 = 활성 관계"), `repository/FriendshipRepository`(`findPair` 양방향·`findReceivedRequests` 생성자 프로젝션 — User FK 매핑 없이 Long 조인), `service/FriendService`(+Impl — 요청 시 역방향 PENDING 자동 수락 = 기존 행 ACCEPTED 승격), `controller/FriendController` 7종(`/api/friends` — code·preview·requests·requests/received·accept·reject·`DELETE /{userId}`), `exception/FriendErrorCode`(9xxx — 9400·9404·9409·9410·9414·9424)
- MSG-186: 친구 목록·친구 프로필 조회 — `FriendshipRepository` 3종 추가(`findFriendsOrderByAcceptedAt`·`findFriendsOrderByNickname` — 양방향 OR 2분기 세타 조인 생성자 프로젝션 1방, SQL UNION 아님. 정렬만 다른 정적 2본(`responded_at DESC NULLS LAST, u.id` / `u.nickname, u.id` — 닉네임 중복 허용 타이브레이크). `existsAcceptedPair` 무잠금 존재 확인 — `findPair`는 PESSIMISTIC_WRITE라 readOnly 트랜잭션에서 PG가 거부해 조회 전용 분리. MSG-285(FRIENDS 공개범위)도 같은 메서드를 소비 — 선언은 이 한 곳(병렬 합의분 중복은 285 리베이스에서 제거·javadoc 병합, 2026-08-04)), `FriendService.getFriends(userId, sort)`(sort null·recent=수락 시각↓ 기본·nickname, 그 외 9420)·`getFriendProfile(userId, targetUserId)`(`existsAcceptedPair` → 프로필 → `UserGridQueryService` 요약·격자, friend→usergrid 단방향 B-내부 read), `FriendController` +2(`GET /api/friends?sort=`·`GET /{userId}/profile` — 기존 7종과 경로 무충돌), `dto/{FriendListItemResponseDto,FriendProfileResponseDto,FriendCollectionGridResponseDto}`(프로필은 단일 응답 — 프로필+요약+최근 격자 30, `summary`는 `CollectionSummaryResponseDto` 중첩 재사용으로 본인·친구 수치 동일 보장. 격자 항목은 본인 갤러리에서 영상 ID 2필드를 뺀 형상 — 비공개 영상 존재 누설 차단), `FriendErrorCode` +1(9420 INVALID_FRIEND_SORT). 프로필 실패는 **전건 기존 9424 단일 응답**(비친구·본인 ID·PENDING 상대·미존재 userId — 관계·계정 존재 은닉), 판정은 요청 시점 실시간(캐시 없음). 마이그레이션 0(조회 전용)
- MSG-285: 친구 여부 read 판정 추가 — `@Transactional(readOnly = true)` 판정(요청 시점 실시간이라 친구 삭제가 다음 요청부터 즉시 반영, 캐시·비정규화 없음). 내부는 MSG-186과 공유하는 무잠금 `existsAcceptedPair` 소비(`findPair`는 `PESSIMISTIC_WRITE` 쓰기용이라 재생 판정마다 행 `FOR UPDATE` 가 걸려 재사용 금지). video 도메인이 FRIENDS 재생 판정에서 소비(B-내부, 계약 인터페이스 아님). **선언 위치는 MSG-312에서 이동** — 당시엔 `FriendService.isFriend` 였고 지금은 `FriendshipQueryService.isFriend` 다
- MSG-187: 친구 도감 레이어 — `FriendController` +2(`GET /{userId}/grids` 뷰포트 커서 · `GET /{userId}/grids/{gridId}/videos` 무페이징 최신순), `FriendService.getFriendGrids`·`getFriendGridVideos`(+Impl — `requireFriend` 가드로 9424 은닉 판정 단일화, getFriendProfile 도 수렴). 뷰포트는 `GridQueryService` 4-인자 **무변경 재사용**(friend 가 신규 크로스 오너 소비자 — userId 에 친구 ID 주입, 검증·에러 4401~4404·`OccupiedGridPageResponseDto` 전부 grid 계약 그대로, grid 패키지 diff 0). 영상 목록 `VideoRepository.findFriendGridVideos`(ACTIVE·READY·visibility IN(PUBLIC,FRIENDS)·`created_at DESC, id DESC`) + `VideoService.getFriendGridVideos`(presign+매핑, 친구 판정은 호출자 책임 명시) + `dto/FriendGridVideoResponseDto`(4필드). D6: `getCollectionGridsForFriend` LATERAL 썸네일 게이트 PUBLIC→IN('PUBLIC','FRIENDS') — MSG-186 예약 TODO 이행(FRIENDS 재생·썸네일 정합). **빈 순환**: MSG-285(video→friend)와 이번(friend→video)으로 상호 의존이 생겨 `FriendServiceImpl`이 `ObjectProvider<VideoService>` 지연 조회로 생성 순환만 회피했었다 — **MSG-312에서 leaf 빈 분리로 구조 해소, 지연 조회 제거**. 마이그레이션·인덱스·전역 PUBLIC 쿼리·신규 에러코드 전부 0
- MSG-312: video↔friend 상호 의존 해소 — `service/FriendshipQueryService`(+Impl) 신설. 친구 판정만 하고 `FriendshipRepository` 하나만 의존하는 leaf 라 어떤 서비스 순환에도 끼지 않는다. `FriendService.isFriend` 는 **제거**(계약에서 삭제 — 소비자였던 `VideoServiceImpl` 이 leaf 로 이동, friend 내부 `requireFriend` 도 leaf 경유해 판정 코드가 한 곳). `FriendServiceImpl` 의 `ObjectProvider<VideoService>` → `VideoService` 생성자 직접 주입 복원. 의존 방향은 `FriendServiceImpl → VideoServiceImpl → FriendshipQueryServiceImpl → FriendshipRepository` 단방향 — 지연 조회 없이 컨텍스트가 뜨는 것이 순환 해소의 증거다(이전 배선은 `BeanCurrentlyInCreationException`). 트랜잭션 경계: leaf 의 `@Transactional(readOnly = true)` 는 전파 REQUIRED 기본이라 호출자(재생 판정·`requireFriend`)가 이미 트랜잭션 안이면 새로 열지 않고 참여한다 — 실행 SQL 은 이전과 같은 `existsAcceptedPair` 단건이고 횟수도 그대로다. 쿼리·응답·에러코드·마이그레이션 전부 0 (동작 무변경 리팩터링)
- MSG-356: 친구 도감 집계 조회 — `FriendService.getFriendGridAggregates(userId, targetUserId, bounds, unit)`(requireFriend 후 `GridQueryService.getOccupiedAggregatesInViewport` 위임 1줄 — 검증 코드 0줄 원칙(MSG-187 D2) 유지), `GET /api/friends/{userId}/grids/aggregation`(`toUnit`은 GridController 동형 사본, 응답은 grid의 `RegionAggregateResponseDto` 재사용). 실패는 기존 9424 단일 응답 재사용(신규 에러코드 0)
- MSG-416: 친구 요청·수락 푸시 알림 — `FriendServiceImpl` 기록 3지점(요청 접수→수신자·직접/자동 수락→원 요청자, 거절 무기록), event_key `FRIEND_REQ:{요청자}:{yyyyMMdd KST}`(UNIQUE dedupe가 같은 상대 하루 1건 상한 겸용)·`FRIEND_ACC:{수락자}:{UUID}`(재수락마다 새 알림), `findIdForKeyShare` 선취 2곳(notifications FK CASCADE 잠금 순서 데드락 차단 — VideoStatusWriter 선례)·빈 Optional → 9404/9414 명시 변환, Clock 프로덕션 생성자(Badge 선례). 검증 FriendIntegrationTest +7(KST 경계 정확 단언)·FriendNotificationRollbackTest 신설(커밋 경계 밖 롤백 동반 소멸)
- MSG-391: 미리보기에 관계 상태 동봉 — `dto/FriendRelation` 신설(5값 SELF·NONE·OUTGOING_PENDING·INCOMING_PENDING·FRIENDS + `of(viewerId, pairOrNull)` 정적 팩토리 — preview·request 공용 판정 단일점, FR-10 구조 보장), `FriendPreviewResponseDto` +relation(additive — nickname·9404 계약 불변), `preview(userId, code)` 시그니처 변경(컨트롤러 `@AuthenticationPrincipal` 주입 — 전역 인증이라 보안 수준 불변), 판정 순서 9404 → SELF(쌍 조회 생략) → `findPairWithoutLock`+`of`. `FriendshipRepository.findPairWithoutLock` 신설(findPair와 JPQL 동일·`@Lock`만 없음 — findPair는 PESSIMISTIC_WRITE라 readOnly에서 PG 거부, 방향 필요라 existsAcceptedPair도 불가). request 분기는 `of` switch 소비로 리팩터링(동작 불변 — 9409/9410·자동 수락·KEY SHARE 선취·잠금 유지). 마이그레이션 0. 검증 FriendIntegrationTest +7·FriendControllerTest 직렬화
- **없는 것**: 도감 공개 범위 설정(Phase 2+)·차단(후속)·코드 재발급(Phase 2+ — MSG-188 종결로 유예 확정)·친구 목록 페이지네이션(수십 명 규모 전제)

### `hotzone` (Owner A · 구현 성민) — ✅ 완성 (MVP 범위)
- MSG-183: 핫스코어 집계 — `service/HotScoreCommandService`(+impl, 평면 service 패키지). 업로드 신호 +1을 UTC 6h 버킷(`hotzone:{bucketId}` Sorted Set, `bucketId=epochSeconds/21600`)에 Lua 원자 스크립트(ZINCRBY+EXPIRE 54h)로 증분. 버킷 키는 호출(커밋) 스레드에서 확정, 실행은 자체 데몬 1스레드 executor(큐 10k, 종료 시 5s 드레인) — 요청 스레드 무블록. 전 실패 삼킴+warn(FR-6, 에러코드 불요). DDL·yml 없음, Redis 전용(D4)
- MSG-184: 핫구역 조회(`GET /api/hotzones` 뷰포트 4파라미터 필수 — `service/HotZoneService`(+impl)·`HotZoneView`, `hotzone:top` 캐시(최근 8버킷 ZUNIONSTORE 균등 합산, TTL 30s, 캐시 보장 Lua 원자 — EXISTS→ZUNIONSTORE→EXPIRE), 상위 K(50)·임계(3)·뷰포트 필터(encode→decode 정수 인덱스, queryByRange 동형), `config/HotZoneProperties`(record, `fillmap.hotzone.top-k/min-score` — topK 양수 기동 검증), `controller/HotZoneController`·DTO 2종, `exception/HotZoneErrorCode`(8400 INVALID_VIEWPORT — 비유한 좌표 NaN 우회 차단 포함), 파라미터 누락은 전역 400(MSG-167 매핑, GridController 구 관행 미답습). 48h 판정은 룩백 몫·TTL은 청소 전용(D4 역할 분리))

- MSG-349: 핫구역 응답에 `regionName` 동봉 — `HotZoneServiceImpl`이 필터 통과 항목의 gridId로 `GridRepository.findRegionNames` 일괄 1회(0건이면 생략) 후 뷰 조립, `HotZoneView`·`HotZoneResponseDto` 필드 가산. grid.repository 직접 주입(A 내부, badge→notification 류 선례). 스케줄 릴레이(`HotZoneEntryDetector`) 경로에 10분당 IN 조회 1회 증가(허용 판정)
- MSG-466: 핫구역 줌아웃 행정 단위 집계(`GET /api/hotzones/aggregation` — unit DONG/SIGUNGU/SIDO + bbox 4좌표, 비로그인 GET permitAll(MSG-454 블록)). 판정 파이프라인을 `passedHotGrids(bounds)`로 추출해 개별 조회(`getHotZones`)와 집계가 같은 통과 집합 공유(D1 — B 소비처 `HotZoneEntryDetector` 전제 불변, 기존 테스트 green 회귀 가드), 귀속은 `GridRepository.findRegionCodeNames` JPQL 엔티티 조인 1회(`Grid.regionCode` 읽기 전용 매핑 신설, `GridRegionCodeNameProjection`), 그룹핑·이름 토큰(`nameToken` — 미션 구현 의도적 복제)·대표 좌표(`GridEncoder.center` 평균)는 메모리 산술. `HotZoneRegionAggregateResponseDto`(count=핫 격자 수·gridIds 동봉), `HotZoneErrorCode`에 8401 VIEWPORT_TOO_LARGE(unit별 상한 — 개별 조회는 종전 무상한)·8405 INVALID_AGGREGATION_UNIT. 검증 `HotZoneAggregateServiceImplTest` 15건·`HotZoneAggregationHttpTest` 17건·`GridRepositoryTest` +1

### `notification` (Owner B · 구현 성민·정민) — 🟡 부분
- MSG-178: FCM 푸시 토큰 등록/해제 — `entity/PushToken`(String 자연키 PK, 조회 매핑 앵커)·`PushPlatform`(IOS/ANDROID/WEB), `repository/PushTokenRepository`(native UPSERT `ON CONFLICT (fcm_token) DO UPDATE` user_id 포함 — 계정 전환 이관 시맨틱 · 해제는 `WHERE fcm_token AND user_id` 소유 검증+멱등), `service/PushTokenService`(+Impl — platform `toUpperCase(Locale.ROOT)` 파싱 → 10400), `controller/PushTokenController`(`/api/notifications/tokens` POST·DELETE), `exception/NotificationErrorCode`(**10xxx 대역** — 9xxx 선확정했으나 MSG-185 병렬 경합(9400 충돌)으로 이동, 대역 정본 = response-pattern.md 표). 마이그레이션 없음(V1 push_tokens 그대로), FCM 호출 없음(발송은 MSG-179)
- MSG-179: FCM 발송 파이프라인 — outbox `notifications`(V21 — V20은 MSG-285 선점으로 리네임, 발송 기록 겸용) → `relay/NotificationRelay`(@Scheduled 5s 폴링, send().get() 동기 확인 후 PUBLISHED — 발행 실패 시 배치 중단, stale PUBLISHED(published_at 30분 초과) 자동 PENDING 복구) → Kafka(단일 브로커 KRaft·토픽 prefix dev./prod.·힙 256M/리밋 512M·thin payload=outbox id) → `consumer/NotificationConsumer`(@KafkaListener earliest·max.poll 600s/10건 — 멱등 2차(종결 상태 검사)→retry_count→설정 필터→전송률 제한(KST 자정 앱 UTC 변환)→토큰 조회→발송, TransactionTemplate 개별 트랜잭션) → `sender/FcmNotificationSender`(sendEachForMulticast 500 청크·UNREGISTERED/INVALID_ARGUMENT 자동 삭제·connect 10s/read 30s). 재시도 = DefaultErrorHandler 백오프(1s×2^n, 8회, 합 255s) → DEAD(DB DLQ — DLT 토픽 없음), 말폼드 비재시도. `relay/StaleTokenCleaner`(일 04시 KST, last_used_at 60일 무갱신 삭제). 접점 2종: `NotificationCommandService.record`(181 소비 — 호출자 트랜잭션 참여 규약)·`NotificationPreferenceService`(MSG-180이 실구현으로 교체 완료). 전부 `fillmap.notification.enabled` 게이트(기본 off — 로컬·CI는 Kafka 없이 green, @EmbeddedKafka 테스트). Codex 5라운드 7건 반영
- MSG-180: 알림 설정 API — `notification_opt_outs`(V22, 복합 PK(user_id, category)·CHECK·FK CASCADE) **행 존재 = off, 행 부재 = on**(기본 전부 on이 스키마 내장 — 시딩·백필 없음). `NotificationPreferenceService`에 `getPreferences`·`update` 추가(isEnabled 시그니처·`NotificationConsumer` 호출부 무변경 — 179 D7 규약 이행), Impl 스텁 → 실구현(`existsById` 부정 1문장 · off 행 조회 후 3종 합성 · 멱등 토글 = off INSERT ON CONFLICT DO NOTHING / on DELETE, 캐시 없음 — 발송량이 전송률 상한으로 눌려 PK 단건 조회로 충분), `controller/NotificationPreferenceController`(GET `/api/notifications/preferences` · PATCH `/{category}` — visibility PATCH 동형 멱등 토글), `INVALID_CATEGORY(10420)`. SecurityConfig·yml·compose 무변경
- MSG-181: 알림 트리거 연동 — record 배선 3종 완성으로 알림 묶음 4티켓 종결. ① 뱃지: `BadgeAwardServiceImpl.award()` 발급 성공 지점 1줄(badge 섹션) ② 핫구역: `relay/HotZoneEntryDetector`(10분 fixedDelay, 게이트 안 — `HotZoneService.getHotZones`에 한국 전역 bbox를 넘겨 전국 상위 K 조회, Redis SET `notification:hotzone:last` 스냅숏 차집합으로 진입 전이 검출, 격자별 `getGridOccupants` 역조회 → 사용자마다 `record(HOTZONE:{KST일자}:{gridId})`, 행정동명 문구·null 폴백. **스냅숏 갱신은 record 이후** — 크래시 재검출을 dedupe가 흡수) ③ 리마인드: `StreakRemindScheduler`(streak 섹션). 신규 REST·에러코드 0, Owner A 코드 변경 0(기존 계약 소비만). Codex 1R P2(리마인드 당일 재시도 부재) 반영
- MSG-314: 임박 알림 지원 — `NotificationRepository.insert`에 created_at 명시 UTC(`statement_timestamp() AT TIME ZONE 'UTC'` — DDL DEFAULT의 세션 타임존 캐스트가 KST 서버에서 KST 벽시계를 저장하던 잠재 스큐 근본 수정, 기존 판독 경로 없음 확인) + `countRecordedSince`(prefix LIKE·기록 기준, 일 상한 판정용) 신설. 배선 자체는 badge 섹션 참조
- MSG-313: 영상 준비 완료·실패 푸시 — `NotificationCategory.VIDEO` 신설(V25가 V21·V22 양쪽 CHECK 재정의), 트리거 배선은 `VideoStatusWriter` 4개 종결 전이(video 섹션), `rateLimited` switch에 `case VIDEO -> null`(무제한 — 자기 영상 결과 통지라 도배 벡터가 없다). 설정 API·릴레이·센더는 코드 무변경(enum 순회 합성이라 조회·토글이 4종으로 자동 확장). 실패 알림은 사실만 전하고 탈락 사유는 싣지 않는다(MSG-286 fail_reason 노출과 접점, 이 티켓 비범위)
- MSG-315: 주간 활동 요약 — `NotificationCategory.WEEKLY` 신설(V26이 V21·V22 양쪽 CHECK를 VIDEO 포함 5종으로 재정의), 배치는 `usergrid/service/WeeklySummaryScheduler`(usergrid 섹션), `rateLimited` switch에 `case WEEKLY -> null`(주 1회라 상한 불요). 설정 API는 enum 순회 합성이라 코드 무변경으로 5종 확장. 알림 확장 3종(313·314·315) 완결
- MSG-343: 발송 계측(`notification.outcome` Counter — `NotificationConsumer`가 sent·skipped(reason 3종 소문자 고정 매핑, 미지 사유 무증가), `NotificationConfig.notificationDeadRecoverer`가 dead. **전이 UPDATE 반환 행 수가 1일 때만 증가** — 발송 중 탈퇴 CASCADE로 행이 지워지면 0행 갱신이라 전이가 없다(Codex 2R). UPDATE 문·WHERE 절은 무변경) + 백로그 Gauge(`relay/NotificationBacklogMetrics` 신설 — `@ConditionalOnProperty(fillmap.notification.enabled)` 게이트라 로컬·CI에선 빈 자체가 안 뜬다, `@Scheduled(fixedDelay=60s)`로 AtomicLong 2개 갱신. `countPending`·`countPublished`는 status **리터럴** native count 2문 — 바인드 파라미터면 generic plan이 partial index(`idx_notifications_pending`·`published`, V21) 일치를 증명 못 한다). 신규 인덱스·마이그레이션 0
- MSG-416: FRIEND 카테고리 신설 — V35가 V21·V22 양쪽 CHECK 재정의(notifications 7종 — MODERATION은 MSG-417 몫 선반영, opt_outs 6종 — MODERATION 제외가 수신 거부 불가의 DB 1차 가드), `rateLimited`에 case FRIEND(무제한 — 행동 직결·상한은 event_key 날짜 꼬리가 겸함), D6 하드코딩 표면 6종화(Controller Swagger 3곳·ResponseDto 3곳·ErrorCode 10420 메시지·Service javadoc), 무효 카테고리 테스트 프로브 FRIEND→NOPE 교체 + FRIEND 토글 케이스
- MSG-417: MODERATION 카테고리 신설(수신 거부 불가) — enum 7종화(CHECK는 V35 선반영·마이그레이션 0), `rateLimited`에 case MODERATION(무제한 — 제재 통지 희소), 3중 가드 = V35 opt_outs CHECK 제외(DB)·`currentPreferences` filter(응답)·`parseCategory` 10420(PATCH — 존재하지 않는 값과 동일 취급), DTO `@Schema(allowableValues)` 6종(springdoc 인라인 스키마 강제로 공유 $ref 유출 없음 — `OpenApiNullableDataTest`에 상시 단언 편입)
- MSG-418: MISSION_NEARBY 카테고리 신설(근접 미션 알림 끄기 설정) — **서버 발송 경로가 없는 최초의 카테고리**(알림은 기기 로컬 생성, off는 기기가 발화 전 설정 조회로 로컬 억제), V36이 opt_outs `category` 폭 VARCHAR(10)→20 선행 확장 후 CHECK 7종 재정의(**notifications CHECK는 의도적 불변** — outbox에 이 카테고리 행이 생기면 결함이고 CHECK가 방어선, MODERATION의 거울상), `rateLimited`에 case MISSION_NEARBY(null — 도달 자체가 결함), 표면 7종화 5곳(Controller 3·DTO·ErrorCode·Service javadoc), ServiceImpl 무변경(values() 순회 설계라 자동 편입), `MissionShape.BoxShape` 설명에 POPUP 명시(FE 혼동 문서 정정, 와이어 불변). 검증 FR-NOTI-16 주석 연결
- MSG-434: 알림함 — `notifications.read_at`(V38, NULL=안읽음) + `controller/NotificationInboxController`(4종: GET `/api/notifications` 커서 목록 · GET `/unread-count` · PATCH `/{id}/read` · PATCH `/read-all`), `service/NotificationInboxService`(+Impl — Clock 이중 생성자, size 클램프 0이하→20·51이상→50), 리포지토리 JPQL 4개(공유 술어 `INBOX_VISIBLE` = 내 알림 · 30일 롤링 창 · `NOT(SKIPPED AND coalesce(last_error,'')='PREF_OFF')`), keyset id 커서(Long.MAX_VALUE 치환·size+1 lookahead), 읽음 쓰기는 `coalesce(read_at,:now)` 부분 UPDATE(멱등·최초 시각 보존·0행=10404 `NOTIFICATION_NOT_FOUND`). 스킵 사유 3종을 리포지토리 상수로 승격(컨슈머 치환, 문자열 값 불변). 발송 파이프라인 동작 무변경. EXPLAIN 실측 최악 1.29ms(기준 30ms) — `(user_id, id DESC)` 복합 인덱스는 첫 페이지 PK 역방향 스캔이 비싸지는 규모에 재측정
- MSG-442: EVENT 카테고리 신설 — V40이 notifications·opt_outs 양쪽 CHECK 재정의(**둘 다 실리는 일반형** — MODERATION 발송만·MISSION_NEARBY 설정만과 대비, `'EVENT'` 5자라 컬럼 폭 확장 불요), enum 8종화, `rateLimited`에 case EVENT(무제한 — 시작 알림은 dedupe가 회차당 1건으로 누르고 일정 변경은 희소), 공개 계약 표면 3곳(`NotificationPageResponseDto`·`NotificationPreferenceResponseDto` allowableValues, `NotificationPreferenceController` 문서 문구 7종→8종), `INVALID_CATEGORY`(10420) 메시지에 EVENT 추가. 발송 트리거는 event 도메인(스케줄러·시더)이고 여기는 카테고리 편입만 — 설정 조회·PATCH·릴레이·센더는 enum 순회라 코드 무변경
- **없는 것**: 딥링크 데이터 페이로드(FCM 전송은 title·body 뿐), 주간 조회수 증분(view_count가 누적값 — 스냅숏 테이블 신설 전엔 산출 불가, MSG-315 FR-8 비목표), 근접 알림의 서버 발송·발화 이력(MSG-418 — 기기 판정형 확정, 지오펜싱·발화는 앱 몫)

### `moderation` (Owner B · 구현 강정민) — 🟡 부분
- MSG-192: 영상 신고 접수 — `POST /api/videos/{videoId}/reports`(`ReportController`, AuthPrincipal 패턴·SecurityConfig 무변경), `entity/Report`(사용 컬럼만 매핑 — reviewed_by/reviewed_at은 MSG-195 몫 D6)·`ReportReason` 5종·`ReportStatus` 4종(V1 CHECK 문자열 일치), `ReportServiceImpl` **검증 순서가 계약**: reason 파싱 11400 → OTHER 상세 11401 → 영상 `findWithLockById` 행 잠금 후 없음·DELETED·BLINDED 3404 은닉(검증과 저장 사이 삭제·블라인드 커밋 창 차단 — Codex 교차 리뷰 수용, D8) → 자기 영상 11402 → 중복 exists 11409 → `saveAndFlush`+`DataIntegrityViolationException` catch 11409(동시 신고 레이스 수렴). **V27**(detail VARCHAR(500) + `uq_reports_reporter_video` — status 무관 전체 유니크 D3, 처리 후 재신고도 불가), `ReportErrorCode` **11xxx 대역**(대역표 행 추가 커밋 선행)
- MSG-195: 관리자 신고 처리 — `controller/AdminReportController`(`/api/admin` 5종: 신고 목록 GET(상태 필터 기본 PENDING·**오프셋 페이징 PageRequest 첫 도입** — 관리자 전용 소량 데이터 예외, 사용자향 커서 관례 불변·닉네임 2종 세타 조인)·승인/기각 POST(reports 행 잠금 → PENDING 확인 11410 → videos 행 잠금 → ACTIVE만 blind 위임 — 잠금 순서 reports→videos 고정 D3, 이미 BLINDED/DELETED는 신고만 종결 FR-5)·해제 POST(unblind 위임, principal 미수취 §D4)·단건 영상 확인 GET(상태·공개범위 무관 재생 URL — 블러본 우선, 조회수·은닉 없음)), `service/AdminReportService`(+Impl), `Report` reviewed_by/reviewed_at 매핑 완성+`resolve`/`reject`(MSG-192 §D6 예약 이행), `ReportErrorCode` +4(11404·11410·11420·11421), DTO 4종(MSG-319 계약), 극단 page 오버플로 11421 가드(Codex 1R). 마이그레이션 0(컬럼 V1 기존재·목록은 `idx_reports_pending` 커버). 관리자 계정 생성 코드 없음 — DB 수동 승격 절차 deploy.md(§D8)
- MSG-417: `approve()`에 소유자 선취 — `findWithLockById`(videos)·`lockPendingReport`(reports) 앞에 users FOR KEY SHARE, 최종 획득 순서 users → videos → reports(탈퇴 CASCADE와 동방향 — AB-BA 데드락 차단), 빈 결과는 주변 경로와 같은 11404
- **없는 것**: 신고 rate limit(같은 사용자의 다수 영상 연속 신고 — 요구 생기면 PRD부터), 사용자 신고(대상은 영상만 2026-08-06 확정 — 사용자 차단 MSG-194는 제품 범위 제외로 종결 2026-08-06), 신고자 처리 결과 알림(후속 논의)

### `route` (Owner 공동 — 주 구현 B · 구현 성민) — ✅ 완성 (MVP 범위, 서버 몫)
- MSG-457: AI 경로 추천 — `POST /api/routes/recommend`(`RouteController`·`RouteRecommendService`+Impl 상시 빈, `RouteIntentClient`만 `@ConditionalOnProperty(route.ai.enabled)` 조건부 + `ObjectProvider` 소비(부재 14503 — HighlightPreviewServiceImpl 선례), 뷰포트 사전 검증 14400(min≥max·범위 밖·비유한값)/14401(0.5도 초과)). 플로우: 검증 → 14503 → 요청 제한(`ConcurrentHashMap.compute` 원자 선점 10초·시도 시각·실패 미롤백, 외부 호출 직전 배치) → parse(해석 캐시: (trim text, 뷰포트 4좌표) 키·TTL 10분·LRU 1,000 `synchronizedMap`) → `RouteCandidateCollector`(미션 3유형 — 스냅샷 캐시 낡음을 주입 Clock 재필터로 보정·대표 좌표 뷰포트 안 검사, 행사 — `getLocationsBulk` 1회·뷰포트 안 첫 위치 대표·전부 밖이면 회차 제외, 장소 — 미충족 관심사(blank 제외) 첫째로 최대 1회 `searchPlaces(String)`·bounds 필터 후 상위 3건·5502는 삼켜 장소 후보 0, 선별 관심사 일치→거리→gridId·상한 8) → `RouteOrderPlanner`(순수 함수: 힌트 선배치→최근접 이웃 하버사인→동률 gridId 사전순, 시작점 origin→힌트 마지막→뷰포트 중심) → facts 조립(출처 상시 1건·기간 KST 표기·관심사·직전 거리, name·facts 100자 절단·facts≤5) → explain → 응답(kind 5값, 표시명 `resolver()` 1회+`resolveRegionNames` 일괄, notice 0~2개). AI 계약 소비는 원본(FillMap-AI docs/MSG-458.md·README) 글자 단위 대조 — snake_case strict, 필수 4키 존재+정의 밖 필드 거부+길이·개수·기간 순서·reasons 규칙, 502/503/504/타임아웃/형태 위반 전부 14502 단일 수렴, 시각 라벨은 KST 변환(`kstDate` 헬퍼 단일 지점). `RouteErrorCode` 14xxx(14400·14401·14429·14502·14503), `RouteAiProperties`(enabled 기본 false·base-url·timeout PT5S — connect 2초·readTimeout 교환 전체), 지표 로그 1줄(outcome·소요·지점 수 — 사용자 문장·AI 원문 미기록, outcome=error 버킷 포함). Flyway·신규 테이블 0(캐시·제한 맵은 프로세스 메모리). 테스트 42메서드(서비스 통합 12·수집 13·클라이언트 9·배열 4·컨트롤러 3·컨텍스트 1). reviewer 4라운드+델타·Codex 7라운드(스펙 4 수렴, 구현 2+타임아웃 1 — 실지적 전부 반영, 공식 수렴 라운드 미확인)
- MSG-468: 언급 지역 신호 — 응답에 `mentionedArea`(name·중심 좌표·외접 사각형 4좌표·kind MOVE/ZOOM_OUT, nullable — notice 방식 required+nullable 병기) 추가. `RouteMentionedAreaResolver`(@Component, 협력자 선례)가 parse(캐시 경유) 직후 판정: 통칭(zones.name) 유일 일치 우선 2단(zone 채택 시 regions 미조회, 동명 통칭·동명 행정구역 그룹 2+·대조 실패 전부 무신호) → 겹침 없으면 MOVE(행정구역은 계약 `overlapsViewport` 실경계, 구역은 4모서리 셀 `GridEncoder.bbox` 점 전체 min/max WGS84 환산 사각형의 엄격 교차) → 겹치면 담김 비율(도 단위 평면 산술) < `ZOOM_OUT_COVERAGE_RATIO = 0.25`에서 ZOOM_OUT, 이상 무신호. 빈 후보 조기 반환(FR-ROUTE-07 분기)·정상 assemble 두 경로 모두 동봉, 판정 예외는 warn(예외 클래스명만 — 지역 이름 비기록) 후 무신호 삼킴, 지표 로그에 `signal=none|move|zoom_out` 키 추가(에러 경로 none). 이름·좌표는 서버 데이터(zones·regions)에서만 — AI 반환 문자열 비노출. Flyway·신규 테이블·인덱스·에러코드 0. 테스트 +18(resolver 8·서비스 nested 6·region 4). reviewer 위반 0 통과·Codex 1라운드 수렴
- MSG-487: 사용 흐름 자동화의 서버 몫 2건 — 전부 `RouteRecommendServiceImpl` 내부, 계약(인터페이스·DTO·에러코드·Flyway) 불변. ① notice 문구 교체: `EMPTY_NOTICE` 상수 교체 + `INSUFFICIENT_NOTICE`는 `%d` 포맷 문자열로 전환(`String.format(Locale.ROOT, ..., points.size())`, 임계 3·분기·조기 반환 불변) — 배너=서버 notice 계약이라 재문구가 서버 몫(0곳 문구는 디자인 확정 대기 제안값, 확정 시 상수 한 줄). ② 자동 이동 재요청 예외(FR-9): 제한 맵 값 타입 교체 `ConcurrentHashMap<Long, Long>` → `<Long, RateLimitState(lastAttemptMillis, moveExempt)>` + `RateLimitClaim(recordedMillis, exemptConsumed)` 반환. 청구는 compute 1회 원자 세 전이(창 통과 = 예외 소거하며 기록 / 창 위반+예외 = 소비하며 기록 / 창 위반+무예외 = 14429 throw·상태 불변), 부여는 `doRecommend` 직후 한 곳(빈 후보 조기 반환 합류 뒤)에서 MOVE + `!exemptConsumed`(연쇄 차단) + `computeIfPresent`로 `lastAttemptMillis == claim.recordedMillis` 시각 바인딩(늦은 MOVE 응답이 남의 창에 예외 부여 차단, 불일치 시 state 그대로 반환 — 남의 예외 소거 함정 회피). ZOOM_OUT·무신호 미부여, 14400/14401/14503은 청구 앞이라 예외 미소모, 실패 미롤백. `MOVE_KIND` 상수(Resolver `KIND_MOVE`와 별도 — 드리프트는 실물 배선 테스트가 잡음, 통합은 가시성 변경이라 보류), 지표 로그 `exempt={}` 키 추가. 시각은 주입 `Clock` 경유. 테스트 +13(notice 문구 4·제한 예외 9, `RouteRecommendServiceTest` 32로 확장·전체 2487). reviewer 모듈 2회 위반 0 통과·Codex 1라운드 수렴(지적 0)
- MSG-483: 보행 경로 표시의 서버 몫 — `POST /api/routes/walk-paths`(로그인 필수, 좌표쌍 1~8개 요청 본문) 세그먼트별 TMap 보행자 경로안내 프록시(appKey 서버 보관). `TmapWalkClient`(`@ConditionalOnProperty(route.walk.enabled)` 조건부 빈, 연결 1초·교환 3초, 형태 검증 — 거리 누락·음수/LineString 없음·총점 2 미만/정의역 밖/5,000점 초과는 그 세그먼트 실패, 연결·타임아웃은 `TmapUnreachableException` 단락 신호), `RouteWalkSegmentCache`(Redis `route:walk:seg:{좌표4}` `SET EX 86400` — 24h는 TMap 약관 보관 상한, 오류·역직렬화 실패는 미스 취급), `RouteWalkDailyLimiter`(`route:walk:daily:{yyyyMMdd}` KST 주입 Clock, Lua INCR+EXPIRE 48h 원자, 기본 900, 초과·Redis 오류 차단 — 캐시와 폴백 방향 반대), `RouteWalkPathService`+Impl 상시 빈(검증 14402 한 코드 수렴: null/빈/9개↑·원소 null·한국 범위 밖·필드 누락 — 좌표는 박싱 `Double`(Jackson 3가 원시 double 누락에 파싱 실패를 던져 공통 400으로 새는 것을 TDD 실측으로 발견, 수렴 우선), 플래그 off 14504, 요청 내 단락 2종: TMap 연결 장애·한도성 거부(거부된 선점도 카운터를 올려 재호출 금지 — 테스트가 카운터 "1" 단언), 캐시 히트는 한도 소진 중에도 성공, 전 실패 갈래 200 + `resolved:false`, 지표 로그 1줄). `RouteWalkProperties`(enabled 기본 false, enabled+빈 appKey면 compact 생성자 기동 실패 — 전 프로파일). FR-10: `facts()` 직전 거리 항목 제거(추천 이유·카드 실거리 어긋남 방지). recommend 응답 계약 불변, Flyway·신규 테이블 0. 실호출 1건으로 TMap 응답 형태 실측 대조(전부 일치). reviewer 5모듈 위반 0 · Codex 스펙 5라운드 9건 반영·구현 1라운드 0건. route+가드 107건 green
- MSG-514: 관심사 동의어 판정·추천 이유 고유 사실(`InterestMatcher` 상시 @Component — 기동 시 `resources/route/interest-synonyms.json` 로드·검증, 위반은 IllegalStateException 기동 실패: 개념 15·근거어 94 전건 dev 시드 실측(레포 `data/mission/`이 코퍼스 정본 — ~/fillmap-data는 md5 다른 상류 스냅숏), key 중복·빈 배열·근거어 2자 미만·alias 공백 금지. 판정 이원화 — 원문 포함(트림 후 한 글자 관심사는 건너뜀: "회"→"시민회관" 오탐 차단)·사전 경유(2자+ alias는 관심사가 포함, 1자 alias는 정확 일치만). "데이트" 근거어는 34회 전부 "업데이트" 부분 문자열이라 제거 — contains 오탐 실버그). `RouteCandidateCollector` 판정 위임·haystack에 placeName 추가·`RouteCandidate.detailFacts` 신설(수집 시점 조립: 코스 "총 14km, 약 5시간 30분, 난이도 보통" 합성+소개문, 축제·팝업 장소명+소개문, 장소 주소 스레딩, 행사 없음), `RouteOrderPlanner` 최근접 이웃 비교 키 (관심사 일치, 거리, gridId) — 힌트 선배치 우선 불변(후보 8개 이하 화면에서 FR-4 성립의 필요 조건), `RouteRecommendServiceImpl.facts()` 순서 재편(출처→관심사→고유 최대 2→기간, 총합 5 캡 산술로 실절단 없음)·지표 로그 `interests=`·`matched=` 키 추가. 외부 호출 수·AI 계약·응답 계약·계약 인터페이스 불변, Flyway 0. 기준선 실측(분기점 코드 대조, 장소 검색 스텁·고정 입력 뷰포트 3×관심사 10): matched/points 20.4% → 30.0%, 감소 셀 0 — 코스 148건 전원 contents 결측이 산책·야경 축 상한(시드 보강 지점, 후속 후보). 테스트 route 123건(신규 매처 9·수집 3·순서 3·서비스 8 포함). reviewer 2라운드(P3 근거어 시드 실측 교체) · Codex 1라운드 지적 0. SRS FR-ROUTE-18·05 구현됨
- MSG-515: 총 이동 거리 상한 — `RouteOrderPlanner`에 `MAX_WALK_DISTANCE_METERS = 10_000`·`WALK_DETOUR_FACTOR = 1.3`(도시 보행 우회 지수 1.2~1.4 중앙값) 상수 고정(설정화 안 함), `order()` 반환 직전 최종 방문 시퀀스 접두 절단 `truncateToWalkBudget` — 누적 1.3×하버사인이 10,000m를 넘게 만드는 지점부터 버림(닫힌 상한: 정확히 10km 허용, origin 있으면 origin→첫 지점이 첫 구간·없으면 첫 지점 누적 0 무조건 담김, 힌트 면제 없음, 선택 루프·순서 규칙·시그니처 불변). 절단 빈 결과는 explain 미호출 `EMPTY_NOTICE`+mentionedArea 조기 반환(기존 빈 후보 분기 합류 — AI 계약이 points 0을 422 거부). 지표 `trimmed={선별 후보-응답 지점}` 추가·`matched`(counts[1]) 계산 시점을 선별 직후→`order()` 뒤 절단 후 목록으로 이동(counts 3칸 — 절단 빈 목록 라인은 points=0 matched=0 trimmed=선별수). `RouteCandidateCollector` 무수정(선별 상한 8 불변, 예산 판정은 순서 확정 후에만 정확 — 스펙 결정 2). 외부 호출 수·응답 계약·Flyway 0. 테스트 route 137건(신규 12 — 닫힌 상한 경계 픽스처는 적도 좌표 ulp 탐색: 위도 35도대엔 정확히 10,000.0인 표현 가능 좌표가 없어서. reviewer가 >= 뮤테이션으로 그 테스트만 깨짐을 실측). 기존 픽스처 1건 축소(origin 시작점 테스트가 실측 13,010m 배치 — 성질 보존 7,228m 재배치). reviewer 1라운드 위반 0 · Codex 스펙 2라운드(matched 재계산 적발)·구현 1라운드 지적 0. SRS FR-ROUTE-13 구현됨
- MSG-513: 무관 문장 안내 — `RouteIntentClient` `PARSE_FIELDS` 단일 5키(허용=필수), `related` boolean만 수용(비불리언·명시적 null·정의 밖 필드·부재 전부 14502 — 부재 true 한시 수용은 MSG-533 배포 확인 후 필수 승격으로 종료), `ParsedIntent` 5성분(related — 해석 캐시 동거로 창 안 판정 재현). `RouteRecommendServiceImpl` cachedParse 직후·resolve 앞 조기 반환(`UNRELATED_NOTICE` 전용 문구 — EMPTY_NOTICE와 구분, mentionedArea null이라 MOVE 예외 미부여)·성공 outcome 3분지(notice 상수 동일성으로 unrelated — points=0 signal=none explain_ms=-1). 무관 경로 외부 호출 parse 1회(resolve·collect·explain 미도달), 요청 제한 소모 유지. 응답 계약·RouteErrorCode·계약 인터페이스·Flyway 0 불변. 테스트 route 147건(신규 10 — AC-513-01~10 전건 `// 검증: FR-ROUTE-19, AC-513-XX` 마커, MSG-526 형식 첫 적용). reviewer 통과(뮤테이션 2종 실증 — 조기 반환 무력화 4건·조건 반전 30건 사망)·P3 2건 반영, Codex 스펙 2라운드(전환 창 캐시 정정)·구현 라운드 코드 결함 0. AI 몫(MSG-533) 배포 완료 후 related 필수 승격(2026-09-01 — PARSE_FIELDS 단일 5키 복원, 부재도 14502, 실배포 실호출 related=false 확인) — SRS FR-ROUTE-19 구현됨
- **없는 것**: FE 화면(입력 패널·지도 렌더·MSG-487 화면 몫(origin 자동 판정·상태줄·MOVE 자동 이동+토스트·자동 재요청·축척 정규화 — `docs/spec/MSG-487.md` 화면 동작 계약) — FR-ROUTE-01·02·11·14·15 진행 중/계획 사유. 출발 토글·이동 제안 UI는 MSG-487로 폐기; MSG-483 화면 몫: 실경로 선 렌더·실거리 카드·직선 폴백 렌더 — FR-ROUTE-16·17 진행 중 사유), 결과 저장(비목표), 다중 인스턴스 요청 제한·캐시(Redis 전환 시 — 예외 상태 record 포함), 플래그 조회 API(칩은 상시 노출 + 14503 안내로 확정 2026-08-24)

## 계약 인터페이스 (Owner A ↔ B 경계면)

`infrastructure.md`가 계약 인터페이스로 명시하지만 **아직 코드에 하나도 없다.** 새로 만들기 전엔
소비하는 쪽에서 import 불가.

| 인터페이스 | 제공자 | 상태 |
|---|---|---|
| `GridQueryService` | Owner A | ✅ built (MSG-73 — 격자 색칠 조회 read · MSG-90 — 4-arg cursor 페이지 시그니처 추가, 2-arg 유지·strategy 오버로드 제거 · MSG-349 — 반환 뷰 2종(`GridCellView`·`OccupiedGridView`)에 regionName 필드 확장, 시그니처 불변·B 소비처(`FriendServiceImpl`) 통과만이라 비파괴 — MSG-167 `CollectionGridView` 선례 동형) |
| `HotZoneService` | Owner A | ✅ built (MSG-184 — `getHotZones(ViewportBounds): List<HotZoneView>` read, D5 예약 이행. `ViewportBounds`는 grid.dto 재사용 — 둘 다 A 소유라 크로스오너 아님, 현 소비자는 `HotZoneController`뿐) |
| `HotScoreCommandService` | Owner A | ✅ built (MSG-183 — `recordUpload(gridId)` write 계약, B(video)의 업로드 확정 `afterCommit` 훅이 소비. 실패 비전파가 계약의 일부 — 호출자는 try-catch 불요) |
| `UserGridQueryService` | Owner B | 🟡 partial (MSG-152 — `getCollectionSummary` 도감 요약 read 계약 신설 B→A · MSG-153 — `getCollectionGrids` B-내부 read 추가, A 미소비·크로스오너 시그니처 불변 · MSG-167 — `CollectionGridView`에 regionName 필드 확장(비파괴) + 신설 공유 컬럼 `grids.region_code` A(쓰기 규칙 권위)↔B(호스팅·소비)) |
| `RegionQueryService` | Owner A | ✅ built (MSG-93 — `resolveByPoint(lat, lon)` 역지오코딩 read. stats 조회는 156에서 별도 서비스로 분리 확정 · MSG-468 — `matchMentionedRegions(name, viewport): List<MentionedRegionMatch>` 언급 지명 대조 read 추가, B(route) 소비) |
| `RegionStatsCommandService` | Owner A | ✅ built (MSG-155 — `refresh(userId, gridId)` 동기 recompute 명령. B의 첫 점령/롤백 훅이 소비, 호출자 트랜잭션 참여) |
| `PlaceSearchService` | Owner A | ✅ built (MSG-251 신설 당시 A 내부 소비 — MSG-457부터 B(route)가 집계 없는 오버로드 `searchPlaces(String)`을 소비해 경계면 편입. MSG-469에서 2인자 메서드 첫 인자가 `long userId` → `String searcherKey`(로그인이면 사용자 id 문자열, 익명이면 `s:` 접두 방문자 세션 값, 집계 대상 아니면 null)로 바뀌었고 **B가 소비하는 1인자 오버로드는 시그니처·동작 불변**. 반환 비null(`List.of()`), 실패는 `ApiException(5502)` 전파 — route는 삼켜 장소 후보 0으로 취급. MSG-481에서 좌표를 받는 4인자 오버로드 `searchPlaces(searcherKey, q, Double lat, Double lng)`가 추가됐고 기존 2인자는 여기에 `(null, null)`로 위임한다 — **B가 소비하는 1인자 오버로드는 이번에도 시그니처·동작 불변**) |
| `ZoneQueryService` | Owner A | ✅ built (MSG-234 신설 당시 A 내부 소비(javadoc "경계면 아님" 명기) — MSG-468부터 B(route)가 `getZones()` 전량 조회를 통칭 대조에 소비해 경계면 편입, 코드·시그니처 무변경(javadoc만 정정)) |
| `UserOidcCommandService` | Owner B | ❌ 미생성 |

## 스키마 vs JPA 엔티티

`V1__init.sql`은 14개 테이블을 정의하고, `V6__mission_schema.sql`(MSG-166)이 미션 3테이블을, `V8__zones.sql`(MSG-234)이 `zones`를 추가했다(V7은 MSG-238 grids.region_code 인덱스가 선점). `V9__badges_seed.sql`(MSG-239)은 badges CHECK 확장(MISSION_COUNT)·`user_badges` 컬럼 2개(notified_at·featured_rank+partial UNIQUE)·활성 3축 11종 시딩·소급 지급을 담는다. `V10__streak_badges_seed.sql`(MSG-200)은 STREAK_3/7/30 시딩만 담는다(DDL 0·소급 없음). `V12__mission_badges_seed.sql`(MSG-223)은 MISSION_1/5/10 시딩만, `V13__mission_source.sql`(MSG-224)은 `missions.source` 적재 출처 컬럼 1개를 추가한다(NULL=수동·불가침). `V14__missions_popup_type_and_source_key.sql`(MSG-235)은 type CHECK에 'POPUP' 추가·`source_key` 컬럼·`(source, source_key)` 부분 유니크 인덱스를 담는다. (V15=MSG-205 reports FK ON DELETE·V16=MSG-310 email nullable·V17=MSG-283 videos fail_reason.) `V18__users_friend_code.sql`(MSG-185)은 `users.friend_code`(혼동 문자 제외 32종 8자) 추가·기존 행 상관 서브쿼리 백필·NOT NULL·UNIQUE 를, `V19__friendships_pair_unique.sql`(MSG-185)은 friendships 대칭 쌍 유니크 인덱스(LEAST/GREATEST — 상호 요청 레이스 DB 백스톱)를 담는다. `V20__videos_visibility_friends.sql`(MSG-285)은 `chk_videos_visibility` 를 DROP 후 3값('PUBLIC','PRIVATE','FRIENDS')으로 재생성한다(CHECK 은 조건 ALTER 불가 — 기존 행 데이터·컬럼 폭·인덱스 전부 무변경, ADD 시 전체 행 검증 스캔 1회. FRIENDS 행이 생긴 뒤 구버전 롤백은 CHECK 위반이라 되돌릴 수 없다). `V21__notifications.sql`(MSG-179)은 알림 outbox 겸 발송 기록 `notifications`(UNIQUE(user_id, event_key) 멱등·PENDING/PUBLISHED partial index 2개·FK CASCADE — V20은 MSG-285가 선점해 리네임)를 담는다. `V22__notification_opt_outs.sql`(MSG-180)은 알림 수신 거부 `notification_opt_outs`(복합 PK(user_id, category)·행 존재 = off — 행 부재가 기본 on이라 시딩·백필 없음, 추가 인덱스 없음)를 담는다. `V23__search_keyword_daily_counts.sql`(MSG-258)은 검색어 일별 집계 테이블을 담는다. `V24__user_grids_grid_index.sql`(MSG-181)은 `idx_user_grids_grid`(grid_id 단독 — 격자 → 점령 사용자 역조회, PK는 선두가 user_id라 못 받치고 FK는 인덱스 자동 생성이 없다)를 담는다. `V25__notification_category_video.sql`(MSG-313)은 `chk_notifications_category`·`chk_notification_opt_outs_category` 양쪽을 DROP 후 VIDEO 포함 4종으로 재생성한다(V20 선례와 같은 상위집합 재정의 — 기존 행·컬럼 폭·인덱스 무변경, ADD 시 전체 행 검증 스캔 1회). `V26__notification_category_weekly.sql`(MSG-315)은 같은 두 CHECK를 WEEKLY 포함 5종으로 다시 재정의한다(V25가 넣은 VIDEO를 목록에서 빠뜨리면 기존 행이 CHECK 위반이라 마이그레이션 자체가 실패한다). `V29__mission_type_badges.sql`(MSG-363)은 `chk_badges_condition`을 종류별 3축(EVENT_COUNT·COURSE_COUNT·POPUP_COUNT) 포함 9값으로 재정의하고 `badges.retired_at`(TIMESTAMP NULL — NULL=현역, 불리언이 아니라 시각인 건 "왜 아무도 못 받나"의 유일한 단서라서. `user_badges.notified_at` 선례) 컬럼 추가·종류별 9종 시딩·합산 MISSION_1/5/10 은퇴 UPDATE·소급 지급을 담는다(V9 1·4번 블록 패턴 복제, 소급은 `b.condition_type = m.type || '_COUNT'` 조인 한 벌로 3축 일괄·ON CONFLICT 멱등. 은퇴는 DELETE가 아니라 UPDATE — FK NO ACTION이 막고 획득 이력도 사라진다). `V30__videos_visibility_default_public.sql`(MSG-363)은 `videos.visibility` DEFAULT를 'PRIVATE'에서 'PUBLIC'으로 바꾼다(서버가 INSERT마다 값을 명시해 와 이 DEFAULT는 발동한 적이 없는 표기 정정 — 메타데이터만 바꾸는 ALTER라 테이블 재작성·기존 행 변경 없음).

| 테이블 | 엔티티 | 상태 |
|---|---|---|
| `users` | `user/entity/User` | ✅ (grid_color 매핑됨 — 구 표기 정정. V16 email nullable·V18 friend_code 반영) |
| `user_grids` | `grid/entity/UserGrid` | ✅ |
| `videos` | `video/entity/Video` | ✅ (MSG-66) |
| `grids` | `grid/entity/Grid` | ✅ (MSG-73 — 조회 전용 최소 매핑: grid_id/grid_y/grid_x, geom 미매핑; MSG-167 — `region_code`(V5) 추가·미매핑, native 접근) |
| `regions` | `region/entity/Region` | ✅ (MSG-154 — region_code/region_name/parent_code/total_grid_count 매핑, boundary_geom 미매핑 — native write 전용) |
| `region_stats` | — | ❌ 엔티티 없음 (native 쿼리로만 접근 — MSG-155/156) |
| `zones` | `zone/entity/Zone` | ✅ (MSG-234 — 전 컬럼 매핑, 정수 사각형·PostGIS 컬럼 없음, V8) |
| `badges` | `badge/entity/Badge` | ✅ (MSG-239 — condition_value JSONB 미매핑, 판정은 native. V9 시딩 11종. MSG-363 — V29 `retired_at` **의도적 미매핑**(은퇴 판정이 전부 native 쿼리 3개이고 부분 매핑이 프로젝트 관례, `ddl-auto=validate`는 미매핑 컬럼을 문제 삼지 않는다). 시딩 누계 26종) |
| `user_badges` | `badge/entity/UserBadge` | ✅ (MSG-239 — 복합 PK `UserBadgeId`, V9 notified_at·featured_rank 추가. 지급은 native ON CONFLICT) |
| `friendships` | `friend/entity/Friendship` | ✅ (MSG-185 — 복합 PK `FriendshipId`, status 는 PENDING·ACCEPTED 만 영속(§D3). V1 테이블 무수정) |
| `likes` | — | ❌ 엔티티 없음 |
| `push_tokens` | `notification/entity/PushToken` | ✅ (MSG-178 — 전 컬럼 매핑, 쓰기는 native UPSERT/DELETE 전용) |
| `notifications` | `notification/entity/Notification` | ✅ (MSG-179 — V21 신설, 전 컬럼 매핑. 쓰기는 native 전용 — 기록 ON CONFLICT·상태 전이·UTC published_at/sent_at) |
| `notification_opt_outs` | `notification/entity/NotificationOptOut` | ✅ (MSG-180 — V22 신설, `@EmbeddedId` 복합 키. 쓰기는 native 전용 — off INSERT ON CONFLICT·on DELETE) |
| `reports` | `moderation/entity/Report` | ✅ (MSG-192 접수 컬럼 → MSG-195 reviewed_by·reviewed_at 매핑 완성, 전이 `resolve`/`reject`. V27 detail+유니크) |
| `sponsor_ads` | — | ❌ 엔티티 없음 |
| `streaks` | `streak/entity/Streak` | ✅ (MSG-200 — 전 컬럼 매핑, 쓰기는 native UPSERT 전용) |
| `missions` | `mission/entity/Mission` | ✅ (MSG-222 조회 매핑 → MSG-224 쓰기 경로: 시드 `@Builder`·`source`(V13)·created_at DB DEFAULT 위임. path JSONB 미매핑 — COURSE 시드는 MSG-225. MSG-235 `sourceKey`(V14) 매핑·빌더 8필드) |
| `mission_grids` | `mission/entity/MissionGrid` | ✅ (MSG-222 — 복합 PK `MissionGridId`, grids FK 없는 논리 참조(lazy insert, MSG-166 §D2). MSG-224 시드 생성자) |
| `user_missions` | `mission/entity/UserMission` | ✅ (MSG-223 — 복합 PK `UserMissionId`, native 전용 최소 매핑. 스탬프 영속 — 비회수) |

## 로드맵 / 백로그

티켓 시퀀싱·의존성·백로그는 **Jira MSG 프로젝트**가 단일 진실 원천이다. 이 문서는 *무엇이 빌드됐는지*만
기록하고 *무엇을 언제 할지*는 다루지 않는다.

## 유지 규칙

패키지나 계약 인터페이스가 planned → partial → built로 바뀌면 해당 행을 즉시 갱신한다.
(spec-driven-dev Phase 5 wrap-up에서 갱신 — 자세히는 해당 스킬 참조.)

- **편집 규칙 (MSG-169)**: 도메인 패키지 섹션은 **티켓당 한 줄 불릿을 append**한다. 기존 줄에
  조각을 이어 붙이지 말 것 — 병렬 PR이 같은 줄을 고치면 병합 충돌이 보장된다. "없는 것" 줄만
  예외적으로 제자리 수정.
